1. Sign in to AWS

Open the AWS Management Console and sign in using your company IAM/SSO account.

Do not use the AWS root account for this project.

2. Select the AWS Region

At the top-right corner of the AWS Console, select the Region provided by the hackathon team.

When no Region was provided, use:

US East (N. Virginia)
us-east-1

We will create all OpsAI resources in the same Region. Bedrock model availability can differ between Regions.

3. Open AWS CloudShell

At the top of the AWS Console, click the CloudShell icon:

>_

CloudShell already contains AWS CLI version 2 and automatically uses your AWS Console permissions. You do not need to create or enter AWS access keys.

4. Run These Commands

Copy and run this complete block in CloudShell:

# Set our project Region
export AWS_REGION=us-east-1
export AWS_DEFAULT_REGION=us-east-1

# Check AWS CLI
aws --version

# Check which AWS account and role you are using
aws sts get-caller-identity

# Confirm the selected Region
echo "AWS Region: $AWS_REGION"

# Check Amazon Bedrock access
aws bedrock list-foundation-models \
  --region "$AWS_REGION" \
  --query 'modelSummaries[0:5].[modelName,providerName]' \
  --output table

# Check S3 access
aws s3api list-buckets \
  --query 'Buckets[0:5].Name' \
  --output table

# Check Lambda access
aws lambda list-functions \
  --region "$AWS_REGION" \
  --max-items 5 \
  --query 'Functions[].FunctionName' \
  --output table

# Check CloudWatch Logs access
aws logs describe-log-groups \
  --region "$AWS_REGION" \
  --limit 5 \
  --query 'logGroups[].logGroupName' \
  --output table

# Check Bedrock Knowledge Base access
aws bedrock-agent list-knowledge-bases \
  --region "$AWS_REGION" \
  --max-results 5 \
  --query 'knowledgeBaseSummaries[].name' \
  --output table

The aws sts get-caller-identity command shows the AWS user or role currently being used.

Expected Result

It is okay when some tables are empty because we have not created resources yet.

The important result is:
No AccessDenied errors



Steps2
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 2 — Verify Bedrock Model Access

AWS now enables most Bedrock foundation models automatically. Anthropic Claude may still ask for use-case details for first-time access.

For the MVP, we will first use:

Main AI model: Amazon Nova Lite
Knowledge embedding model: Amazon Titan Text Embeddings V2
2.1 Open Amazon Bedrock
In the AWS Console search bar, type:
Amazon Bedrock
Open Amazon Bedrock.
Confirm the Region remains:
US East (N. Virginia) — us-east-1
2.2 Test Amazon Nova Lite
From the left menu, select Model catalog.
Search for:
Amazon Nova Lite
Open the model.
Select Open in playground or Try in playground.
Enter this prompt:
Reply with exactly: OpsAI Assistant model access is working.
Select Run.
Expected answer
OpsAI Assistant model access is working.
2.3 Confirm the Embedding Model

Return to Model catalog and search for:

Titan Text Embeddings V2

Confirm that you can open its model page. The model ID should normally appear as:

amazon.titan-embed-text-v2:0

We will use this model later to convert your SOPs, runbooks and operational documents into searchable embeddings.

Step 2 completion requirement

Step 2 passes when:

Nova Lite provides a response in the playground.
Titan Text Embeddings V2 is visible in the Model Catalog.
No AccessDenied message appears.


Steps3
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 3 — Create the OpsAI Project Folders

Open AWS CloudShell and run:

cd ~

mkdir -p opsai-assistant-aws/{data,lambda,schemas,app,scripts,docs,reports}

cd opsai-assistant-aws

pwd

find . -maxdepth 1 -type d | sort


Expected output
/home/cloudshell-user/opsai-assistant-aws
.
./app
./data
./docs
./lambda
./reports
./schemas
./scripts

These folders will be used for:

data — sample incidents, SOPs and knowledge documents
lambda — AWS Lambda code
schemas — Bedrock Agent action schemas
app — chatbot application
scripts — deployment scripts
docs — project documentation
reports — testing and MVP results

Complete only Step 3

Steps4
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Step 4 — Create the S3 Knowledge Bucket

This bucket will store the SOPs, runbooks and incident documents used by the Bedrock Knowledge Base. Amazon Bedrock supports Amazon S3 as a knowledge-base data source.

In the same CloudShell window, copy and run this complete block:


cd ~/opsai-assistant-aws

# Set the AWS Region
export AWS_REGION=us-east-1
export AWS_DEFAULT_REGION=us-east-1

# Get your AWS account ID
ACCOUNT_ID=$(aws sts get-caller-identity \
  --query Account \
  --output text)

# Create a globally unique bucket name
BUCKET_NAME="opsai-assistant-kb-${ACCOUNT_ID}-${AWS_REGION}"

echo "Creating bucket: $BUCKET_NAME"

# Create the S3 bucket
aws s3api create-bucket \
  --bucket "$BUCKET_NAME" \
  --region "$AWS_REGION"

# Block all public access
aws s3api put-public-access-block \
  --bucket "$BUCKET_NAME" \
  --public-access-block-configuration \
  BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true

# Enable bucket versioning
aws s3api put-bucket-versioning \
  --bucket "$BUCKET_NAME" \
  --versioning-configuration Status=Enabled

# Enable server-side encryption
aws s3api put-bucket-encryption \
  --bucket "$BUCKET_NAME" \
  --server-side-encryption-configuration \
  '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'

# Create folders inside the bucket
aws s3api put-object \
  --bucket "$BUCKET_NAME" \
  --key "knowledge/" >/dev/null

aws s3api put-object \
  --bucket "$BUCKET_NAME" \
  --key "uploads/" >/dev/null

aws s3api put-object \
  --bucket "$BUCKET_NAME" \
  --key "reports/" >/dev/null

# Save the bucket information
cat > ~/opsai-assistant-aws/.env <<EOF
AWS_REGION=$AWS_REGION
OPSAI_BUCKET=$BUCKET_NAME
EOF

chmod 600 ~/opsai-assistant-aws/.env


New S3 buckets are private by default, but we are explicitly enabling all Block Public Access settings for additional protection. Versioning will also help recover earlier versions of documents.

Validate Step 4

Run:

source ~/opsai-assistant-aws/.env

echo "Region: $AWS_REGION"
echo "Bucket: $OPSAI_BUCKET"

aws s3 ls "s3://$OPSAI_BUCKET/"

aws s3api get-bucket-versioning \
  --bucket "$OPSAI_BUCKET"

aws s3api get-public-access-block \
  --bucket "$OPSAI_BUCKET"

aws s3api get-bucket-encryption \
  --bucket "$OPSAI_BUCKET"



Expected result
You should see:

knowledge/
uploads/
reports/

Versioning should show:

{
    "Status": "Enabled"
}

All four public-access settings should show:

true

Complete only Step 4




Steps5
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Step 5 — Create and Upload Sample Knowledge Documents

Amazon Bedrock Knowledge Bases can read documents such as text, Markdown, HTML, and PDF files from an S3 data source.

In CloudShell, run this complete block:

Command starts from here
+++++++++++++++++++++++++++++

cd ~/opsai-assistant-aws
source .env

# Create sample knowledge documents
cat > data/kubernetes-node-unavailable.md <<'EOF'
# Kubernetes Node Unavailable Runbook

## Issue

A Kubernetes or RKE2 worker node is showing as NotReady or Unavailable.

## Initial Checks

Run:

kubectl get nodes -o wide

kubectl describe node NODE_NAME

kubectl get pods -A --field-selector spec.nodeName=NODE_NAME

## Common Causes

- RKE2 service stopped
- High CPU or memory usage
- Disk pressure
- Network connectivity issue
- Container runtime issue
- Certificate or time synchronization issue

## Safe Recovery Steps

1. Check the node status.
2. Check CPU, memory and disk usage.
3. Check the RKE2 agent service.
4. Cordon the node before maintenance.
5. Drain workloads when downtime is approved.
6. Restart the RKE2 service or reboot the server.
7. Confirm the node returns to Ready.
8. Uncordon the node.

## Validation

Run:

kubectl get nodes

kubectl get pods -A --field-selector spec.nodeName=NODE_NAME

The node must show Ready and the required application pods must be Running.
EOF

cat > data/disk-pressure-runbook.md <<'EOF'
# Kubernetes Disk Pressure Runbook

## Issue

A Kubernetes node reports DiskPressure or applications cannot create new files.

## Checks

Run:

df -h

df -i

du -xsh /var/lib/rancher/rke2/*

journalctl -u rke2-agent --since "30 minutes ago"

kubectl describe node NODE_NAME

## Common Causes

- Container images using too much disk
- Old log files
- Deleted files still held by a process
- Large application data
- Inode exhaustion

## Recovery Steps

1. Identify the filesystem with high usage.
2. Identify the largest directories.
3. Remove only approved old logs and unused files.
4. Do not manually delete active containerd data.
5. Restart the affected service only when required.
6. Confirm DiskPressure is removed.

## Validation

Run:

kubectl get nodes

kubectl describe node NODE_NAME

The DiskPressure condition should show False.
EOF

cat > data/jenkins-pipeline-failure.md <<'EOF'
# Jenkins Pipeline Failure Runbook

## Issue

A Jenkins build fails, hangs or cannot connect to a destination server.

## Checks

1. Open the Jenkins console output.
2. Identify the failed stage.
3. Check credentials configuration.
4. Test network connectivity.
5. Confirm the destination server is reachable.
6. Check whether the Jenkins agent is online.
7. Check disk space and memory.

## Useful Commands

ping SERVER_NAME

ssh -o ConnectTimeout=30 USER@SERVER_NAME

df -h

free -m

## Common Causes

- Incorrect credentials
- Destination server unavailable
- Jenkins agent offline
- Network or DNS issue
- Script syntax error
- Insufficient disk or memory
- Command timeout

## Recovery Steps

1. Fix the confirmed cause.
2. Test connectivity manually.
3. Restart only the failed pipeline stage when possible.
4. Run the pipeline again.
5. Confirm that all stages complete successfully.
EOF

# Review the files
ls -lh data/

# Upload the documents to S3
aws s3 sync \
  data/ \
  "s3://$OPSAI_BUCKET/knowledge/" \
  --exclude "*" \
  --include "*.md"
Validate Step 5

Run:

aws s3 ls \
  "s3://$OPSAI_BUCKET/knowledge/" \
  --recursive

You should see these three files:

knowledge/kubernetes-node-unavailable.md
knowledge/disk-pressure-runbook.md
knowledge/jenkins-pipeline-failure.md

Complete only Step 5




Steps6
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 6 — Create the Bedrock Knowledge Base

We will use S3 Vectors because it is managed by AWS and is suitable for a cost-effective MVP. Bedrock can automatically create the S3 vector bucket and vector index for us.

6.1 Get the S3 path

In CloudShell, run:

cd ~/opsai-assistant-aws
source .env

echo "s3://$OPSAI_BUCKET/knowledge/"

Copy the displayed S3 path.

6.2 Open Knowledge Bases
Open Amazon Bedrock.
Confirm the Region is:
US East (N. Virginia) — us-east-1
On the left side, expand Build.
Select Knowledge bases.
Select Create.
Choose:
Knowledge Base with vector store
6.3 Enter Knowledge Base Details

Use these values:

Knowledge base name
opsai-assistant-kb
Description
Knowledge base containing approved Kubernetes, Jenkins, cloud and infrastructure operations runbooks for OpsAI Assistant.
IAM permissions

Select:

Create and use a new service role

Allow AWS to generate the role name.

Do not select a custom IAM role for now.

6.4 Configure the Data Source

For Data source type, select:

Amazon S3

Use this data-source name:

opsai-runbooks-s3

For the S3 location, paste the path copied from CloudShell:

s3://<your-bucket-name>/knowledge/

Make sure /knowledge/ is included.

For the remaining options, select:

Parsing strategy: Default parser
Chunking strategy: Default chunking

Do not enable multimodal parsing because our current documents contain only text.

Select Next.

6.5 Select the Embedding Model

For the embedding model, select:

Titan Text Embeddings V2

Confirm the model ID is:

amazon.titan-embed-text-v2:0

Keep the default settings:

Embedding type: Float
Vector dimensions: 1024

S3 Vectors supports floating-point vectors for Bedrock Knowledge Bases.

6.6 Configure the Vector Store

Select:

Quick create a new vector store

For the vector-store type, select:

Amazon S3 Vectors

It may appear as:

S3 vector bucket

Keep the default encryption option. Bedrock will automatically create a separate S3 vector bucket and vector index.

Select Next.

6.7 Review and Create

Confirm:

Knowledge base: opsai-assistant-kb
Data source: Amazon S3
Embedding model: Titan Text Embeddings V2
Vector store: Amazon S3 Vectors
IAM role: New service role

Select:

Create knowledge base

Wait until the knowledge-base status becomes:

Ready

or:

Available

Do not select Sync yet. We will perform the first synchronization and test the runbooks in Step 7. AWS requires the data source to be synchronized after the knowledge base is ready.

Complete only Step 6




Steps7
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 7 — Sync the Runbook Documents
Click the Knowledge Base name:
opsai-assistant-kb
Find the Data source section.
Select the checkbox beside:
opsai-runbooks-s3
Click:
Sync
Confirm by clicking Sync again if AWS asks.

AWS will now read the three Markdown runbooks from S3, divide them into searchable sections, create embeddings, and store them in the S3 vector index.

Wait until the sync status becomes:

Completed




Steps8
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 8 — Test Knowledge Retrieval
Click Test at the top-right.
In the test panel, turn off:
Generate responses

This tests whether Bedrock can retrieve information directly from your uploaded runbooks without using an AI model.

Enter this question:
What should I check when a Kubernetes node becomes unavailable?
Click Run.
Expected result

You should see information such as:

Run kubectl get nodes -o wide
Describe the affected node
Check pods running on the node
Check CPU, memory and disk usage
Check the RKE2 service
Cordon and drain before approved maintenance

The source should point to:

kubernetes-node-unavailable.md

Run only this test



Steps9
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 9A — Select the AI Model for Answer Generation

Do only this part now:

On the left, select:
Standard retrieval with answer generation
Click:
Select model
Choose:
Provider: Amazon
Model: Nova Lite
If AWS asks for an inference option, choose:
Inference profiles

Then select the available US Nova Lite inference profile and click Apply. Amazon Bedrock Knowledge Bases can use a foundation model or inference profile to generate an answer from retrieved information.

Do not run the question yet


___________________

Step 9B — Generate the AI Answer

In the prompt box, enter:

Using only the approved runbook, what should I check when a Kubernetes node becomes unavailable? Give the answer in easy English and numbered steps.

Then click Run.

You should receive a clear AI-generated answer based on the Kubernetes runbook, with source references.

Run only this test



Steps10
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Step 10A — Open the Bedrock Agent Creation Page

For this hackathon MVP, we will use Agents Classic because it is the quickest way to connect the Knowledge Base now and add Lambda actions later. Agents can decide whether to query a Knowledge Base or call an action group.

Click Close at the top-right.
From the left menu, under Build, click:
Agents Classic
Click:
Create agent

Do not enter any details yet. Send me the Create agent screen.

_____________________________

Step 10B — Create the OpsAI Agent

Enter these details:

Name

opsai-assistant-agent

Description

AI operations assistant that uses approved runbooks to support Kubernetes, Jenkins, cloud and infrastructure troubleshooting.

Leave this unchecked:

Enable multi-agent collaboration

Now click Create.

After the agent page opens, do not configure anything yet.

_____________________

Step 10C — Select the Agent Model

Keep this selected:

Create default role

AWS will create the service role needed by the agent.

Now:

Click Select model.
Choose Amazon.
Select Nova Lite.
Keep On-demand if shown.
Click Apply.

Do not enter the agent instructions yet.

__________________________

Step 10D — Add Agent Instructions

Copy and paste this into Instructions for the Agent:

You are OpsAI Assistant, an enterprise IT operations support agent.

Use the connected knowledge base and approved runbooks to answer questions about Kubernetes, RKE2, Jenkins, cloud and infrastructure operations.

Give answers in easy English with clear numbered steps.

Do not invent commands, facts or procedures. If the approved knowledge base does not contain the answer, clearly say that the information is not available.

For any restart, reboot, deletion, scaling, patching or production change, explain the impact and require human approval before the action is performed.

Always provide validation steps after troubleshooting or recovery actions.

Agent instructions tell Bedrock what the agent should do and how it should communicate with users.

After pasting:

Click Save at the top.
Do not click Prepare yet.
Do not add an action group yet.

_________________________

Step 10E — Connect the Knowledge Base

Do not click Prepare yet.

Scroll down until you see Knowledge bases.
Click Add.
Select:
opsai-assistant-kb
For the Knowledge Base instructions, enter:
Use this knowledge base to answer questions about Kubernetes, RKE2, Jenkins, cloud and infrastructure troubleshooting using approved runbooks.
Click Add or Save in the popup.

Complete only this step


_________________________

Step 10E — Connect the Knowledge Base

Do not click Prepare yet.

Scroll down until you see Knowledge bases.
Click Add.
Select:
opsai-assistant-kb
For the Knowledge Base instructions, enter:
Use this knowledge base to answer questions about Kubernetes, RKE2, Jenkins, cloud and infrastructure troubleshooting using approved runbooks.
Click Add or Save in the popup.

Complete only this step


_________________________________

Step 10F — Prepare the Agent

Click Prepare at the top.

Wait until you see a green success message similar to:

Agent preparation completed successfully.

Do not test the agent yet.

_________________________________

Step 10G — Test the Agent

In the Test Agent box on the right, enter:

What should I check when a Kubernetes node becomes unavailable? Give me easy numbered steps.

Then click Run.

Expected result

The agent should answer using your approved Kubernetes runbook and include steps such as:

Check node status
Describe the node
Check pods on the node
Check CPU, memory and disk
Check the RKE2 service
Cordon and drain before approved maintenance
Validate that the node returns to Ready

Run only this test


______________________________




Steps11
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Step 11A — Exit the Agent Builder

Click:

Save and exit

You will return to the Agent Details page. We will create an agent alias there so the OpsAI application can invoke this agent. AWS requires an alias to deploy and call the agent from an application; creating the alias also creates an agent version automatically.

Do only this step

___________________________

Step 11B — Open Alias Creation

Click Create Alias at the top-right.

An alias is required before an application can invoke the agent. AWS will create a numbered agent version when the alias is created.

Do not enter anything yet.

___________________________

Step 11C — Create the Agent Alias

Enter these values:

Alias name

opsai-mvp

Description

MVP alias for invoking the OpsAI Assistant agent.

Keep these selected:

Create a new version and associate it with this alias
On-demand (ODT)

Do not select Provisioned Throughput.

Then click Create alias and wait until its status becomes Prepared or Ready.



Steps12
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 12A — Get the Agent Alias ID

The application will need both the Agent ID and Alias ID to call the agent.

Open CloudShell as opsai-admin and run:

aws bedrock-agent list-agent-aliases \
  --agent-id FJ6R3CBW6H \
  --region us-east-1 \
  --query 'agentAliasSummaries[].{AliasName:agentAliasName,AliasID:agentAliasId,Status:agentAliasStatus}' \
  --output table

You should see:

AliasName: opsai-mvp
AliasID: a 10-character ID
Status: PREPARED

The command lists the aliases connected to your agent.

Run only this command

____________________________________


Step 12B — Save the Configuration

Run this complete block in the current opsai-admin CloudShell:

mkdir -p ~/opsai-assistant-aws/{app,scripts,lambda,schemas,data,docs,reports}

cd ~/opsai-assistant-aws

cat > .env <<'EOF'
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
OPSAI_BUCKET=opsai-assistant-kb-845482522818-us-east-1
BEDROCK_AGENT_ID=FJ6R3CBW6H
BEDROCK_AGENT_ALIAS_ID=LRVAF80EAC
EOF

chmod 600 .env
source .env

echo "Region: $AWS_REGION"
echo "Bucket: $OPSAI_BUCKET"
echo "Agent ID: $BEDROCK_AGENT_ID"
echo "Alias ID: $BEDROCK_AGENT_ALIAS_ID"
Expected output
Region: us-east-1
Bucket: opsai-assistant-kb-845482522818-us-east-1
Agent ID: FJ6R3CBW6H
Alias ID: LRVAF80EAC

Complete only this step


Steps13
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 13A — Verify Agent and Alias IDs
Purpose

Confirm the exact deployed Agent ID, Alias ID, alias status and agent version before calling the agent from Python.

Commands


cd ~/opsai-assistant-aws

export AWS_REGION=us-east-1
export AWS_DEFAULT_REGION=us-east-1

AGENT_ID=$(aws bedrock-agent list-agents \
  --region "$AWS_REGION" \
  --query "agentSummaries[?agentName=='opsai-assistant-agent'].agentId | [0]" \
  --output text)

ALIAS_ID=$(aws bedrock-agent list-agent-aliases \
  --agent-id "$AGENT_ID" \
  --region "$AWS_REGION" \
  --query "agentAliasSummaries[?agentAliasName=='opsai-mvp'].agentAliasId | [0]" \
  --output text)

echo "Agent ID: $AGENT_ID"
echo "Alias ID: $ALIAS_ID"

aws bedrock-agent get-agent-alias \
  --agent-id "$AGENT_ID" \
  --agent-alias-id "$ALIAS_ID" \
  --region "$AWS_REGION" \
  --query 'agentAlias.{AliasName:agentAliasName,AliasID:agentAliasId,Status:agentAliasStatus,Routing:routingConfiguration}' \
  --output json
Verified result
Agent ID: FJ6R3CBW6H
Alias ID: LRVAF80EAC
Alias name: opsai-mvp
Status: PREPARED
Agent version: 1

Step 13A Status: Passed ✅

Step 13B — Test the Deployed Agent from Python
Purpose

Verify that an external application can invoke the deployed OpsAI Assistant agent using the Agent ID and Alias ID.

1. Update the .env file


cd ~/opsai-assistant-aws

sed -i "s/^BEDROCK_AGENT_ID=.*/BEDROCK_AGENT_ID=$AGENT_ID/" .env

sed -i "s/^BEDROCK_AGENT_ALIAS_ID=.*/BEDROCK_AGENT_ALIAS_ID=$ALIAS_ID/" .env

set -a
source .env
set +a

echo "Agent ID: $BEDROCK_AGENT_ID"
echo "Alias ID: $BEDROCK_AGENT_ALIAS_ID"
2. Create the Python test script
cat > app/test_agent.py <<'PY'
import os
import sys
import uuid

import boto3
from botocore.exceptions import BotoCoreError, ClientError


def main() -> int:
    region = os.environ.get("AWS_REGION", "us-east-1")
    agent_id = os.environ.get("BEDROCK_AGENT_ID")
    alias_id = os.environ.get("BEDROCK_AGENT_ALIAS_ID")

    if not agent_id or not alias_id:
        print("ERROR: Agent ID or Alias ID is missing from .env")
        return 1

    client = boto3.client(
        "bedrock-agent-runtime",
        region_name=region,
    )

    question = (
        "What should I check when a Kubernetes node becomes unavailable? "
        "Give me easy numbered steps."
    )

    print("Calling OpsAI Assistant...")
    print(f"Agent ID: {agent_id}")
    print(f"Alias ID: {alias_id}")
    print()

    try:
        response = client.invoke_agent(
            agentId=agent_id,
            agentAliasId=alias_id,
            sessionId=str(uuid.uuid4()),
            inputText=question,
            enableTrace=False,
        )

        answer_parts = []

        for event in response.get("completion", []):
            chunk = event.get("chunk")

            if chunk and "bytes" in chunk:
                answer_parts.append(
                    chunk["bytes"].decode("utf-8")
                )

        answer = "".join(answer_parts).strip()

        if not answer:
            print("No answer was received from the agent.")
            return 1

        print("OpsAI Assistant response:")
        print("-" * 60)
        print(answer)
        print("-" * 60)

        return 0

    except ClientError as error:
        print(f"AWS ERROR: {error}")
        return 1
    except BotoCoreError as error:
        print(f"SDK ERROR: {error}")
        return 1
    except Exception as error:
        print(f"UNEXPECTED ERROR: {error}")
        return 1


if __name__ == "__main__":
    sys.exit(main())
PY


3. Run the test
python3 app/test_agent.py
Final result

The Python application successfully invoked the deployed OpsAI Assistant agent.

The response included:

Kubernetes node-status checks
kubectl describe node
Pod checks for the affected node
Common causes such as RKE2 service, CPU, memory, disk and network issues
Recovery validation commands

No AWS error was reported.

Step 13B Status: Passed ✅

Overall Step 13 Status
Agent ID verification: Passed
Alias verification: Passed
Alias status: PREPARED
Python invocation: Passed
Knowledge Base response: Passed

Steps14
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 14A — Create the Lambda Execution Role

The Lambda function will receive questions from the web UI and invoke your deployed Bedrock agent. Every Lambda function needs an execution role, and the basic Lambda role permits writing logs to CloudWatch. We will separately grant only bedrock:InvokeAgent access to your deployed alias.

Continue as opsai-admin in CloudShell and run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

ACCOUNT_ID=$(aws sts get-caller-identity \
  --query Account \
  --output text)

ROLE_NAME="OpsAILambdaExecutionRole"

cat > lambda-trust-policy.json <<'EOF'
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Service": "lambda.amazonaws.com"
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
EOF

# Create the role only if it does not already exist
aws iam get-role \
  --role-name "$ROLE_NAME" >/dev/null 2>&1 || \
aws iam create-role \
  --role-name "$ROLE_NAME" \
  --assume-role-policy-document file://lambda-trust-policy.json

# Allow Lambda to write logs to CloudWatch
aws iam attach-role-policy \
  --role-name "$ROLE_NAME" \
  --policy-arn arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

# Allow Lambda to invoke only the deployed OpsAI agent alias
cat > lambda-bedrock-policy.json <<EOF
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "InvokeOpsAIAgent",
      "Effect": "Allow",
      "Action": "bedrock:InvokeAgent",
      "Resource": "arn:aws:bedrock:${AWS_REGION}:${ACCOUNT_ID}:agent-alias/${BEDROCK_AGENT_ID}/${BEDROCK_AGENT_ALIAS_ID}"
    }
  ]
}
EOF

aws iam put-role-policy \
  --role-name "$ROLE_NAME" \
  --policy-name OpsAIInvokeBedrockAgent \
  --policy-document file://lambda-bedrock-policy.json

LAMBDA_ROLE_ARN=$(aws iam get-role \
  --role-name "$ROLE_NAME" \
  --query 'Role.Arn' \
  --output text)

if grep -q '^LAMBDA_ROLE_ARN=' .env; then
  sed -i "s|^LAMBDA_ROLE_ARN=.*|LAMBDA_ROLE_ARN=$LAMBDA_ROLE_ARN|" .env
else
  echo "LAMBDA_ROLE_ARN=$LAMBDA_ROLE_ARN" >> .env
fi

echo "Lambda role: $ROLE_NAME"
echo "Role ARN: $LAMBDA_ROLE_ARN"

aws iam list-attached-role-policies \
  --role-name "$ROLE_NAME" \
  --output table

aws iam get-role-policy \
  --role-name "$ROLE_NAME" \
  --policy-name OpsAIInvokeBedrockAgent \
  --query 'PolicyDocument.Statement[0].{Action:Action,Resource:Resource}' \
  --output table
Expected result

You should see:

Lambda role: OpsAILambdaExecutionRole

The attached policy should show:

AWSLambdaBasicExecutionRole

The inline policy should show:

Action: bedrock:InvokeAgent
Resource: arn:aws:bedrock:us-east-1:...:agent-alias/FJ6R3CBW6H/LRVAF80EAC

Complete only Step 14A

_________________________________

Step 14B — Create and Deploy the Lambda Backend

The Lambda function will receive a question from the future chatbot UI, invoke the Bedrock Agent and return the answer. A Lambda ZIP deployment requires function code and an execution role.

Run this complete block:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

FUNCTION_NAME="opsai-assistant-api"

cat > lambda/lambda_function.py <<'PY'
import base64
import json
import logging
import os
import uuid
from typing import Any, Dict

import boto3
from botocore.exceptions import BotoCoreError, ClientError


logger = logging.getLogger()
logger.setLevel(logging.INFO)

AGENT_ID = os.environ["BEDROCK_AGENT_ID"]
AGENT_ALIAS_ID = os.environ["BEDROCK_AGENT_ALIAS_ID"]
ALLOWED_ORIGIN = os.environ.get("ALLOWED_ORIGIN", "*")

bedrock_runtime = boto3.client("bedrock-agent-runtime")


def build_response(status_code: int, payload: Dict[str, Any]) -> Dict[str, Any]:
    """Return a response compatible with Lambda and API Gateway."""
    return {
        "statusCode": status_code,
        "headers": {
            "Content-Type": "application/json",
            "Access-Control-Allow-Origin": ALLOWED_ORIGIN,
            "Access-Control-Allow-Headers": "Content-Type",
            "Access-Control-Allow-Methods": "OPTIONS,POST",
        },
        "body": json.dumps(payload),
    }


def read_payload(event: Dict[str, Any]) -> Dict[str, Any]:
    """Read either a direct Lambda event or an API Gateway request."""
    body = event.get("body")

    if body is None:
        return event

    if event.get("isBase64Encoded") and isinstance(body, str):
        body = base64.b64decode(body).decode("utf-8")

    if isinstance(body, str):
        return json.loads(body)

    if isinstance(body, dict):
        return body

    raise ValueError("Request body must contain valid JSON.")


def lambda_handler(event: Dict[str, Any], context: Any) -> Dict[str, Any]:
    method = (
        event.get("requestContext", {})
        .get("http", {})
        .get("method")
        or event.get("httpMethod")
    )

    if method == "OPTIONS":
        return build_response(200, {"message": "CORS preflight successful"})

    try:
        payload = read_payload(event)

        question = (
            payload.get("question")
            or payload.get("message")
            or payload.get("inputText")
        )

        if not question or not str(question).strip():
            return build_response(
                400,
                {"error": "Please provide a question."},
            )

        session_id = payload.get("sessionId") or str(uuid.uuid4())

        logger.info(
            "Invoking OpsAI agent. Request ID: %s",
            getattr(context, "aws_request_id", "local-test"),
        )

        response = bedrock_runtime.invoke_agent(
            agentId=AGENT_ID,
            agentAliasId=AGENT_ALIAS_ID,
            sessionId=session_id,
            inputText=str(question).strip(),
            enableTrace=False,
        )

        answer_parts = []

        for event_item in response.get("completion", []):
            chunk = event_item.get("chunk")

            if chunk and "bytes" in chunk:
                answer_parts.append(
                    chunk["bytes"].decode("utf-8")
                )

        answer = "".join(answer_parts).strip()

        if not answer:
            return build_response(
                502,
                {"error": "The agent did not return an answer."},
            )

        return build_response(
            200,
            {
                "answer": answer,
                "sessionId": session_id,
            },
        )

    except (json.JSONDecodeError, ValueError) as error:
        return build_response(400, {"error": str(error)})

    except ClientError as error:
        error_code = error.response.get("Error", {}).get(
            "Code",
            "AWSClientError",
        )
        logger.exception("AWS client error: %s", error_code)

        return build_response(
            500,
            {"error": f"AWS service error: {error_code}"},
        )

    except BotoCoreError:
        logger.exception("AWS SDK error")

        return build_response(
            500,
            {"error": "AWS SDK communication error."},
        )

    except Exception:
        logger.exception("Unexpected Lambda error")

        return build_response(
            500,
            {"error": "Unexpected backend error."},
        )
PY

# Create the ZIP deployment package
rm -f lambda-package.zip

cd lambda
zip -q ../lambda-package.zip lambda_function.py
cd ..

echo "Lambda package created:"
ls -lh lambda-package.zip

# Give IAM a few seconds to finish role propagation
sleep 15

# Create the Lambda function, or update it if it already exists
if aws lambda get-function \
  --function-name "$FUNCTION_NAME" \
  --region "$AWS_REGION" >/dev/null 2>&1
then
    echo "Updating existing Lambda function..."

    aws lambda update-function-code \
      --function-name "$FUNCTION_NAME" \
      --region "$AWS_REGION" \
      --zip-file fileb://lambda-package.zip >/dev/null

    aws lambda wait function-updated \
      --function-name "$FUNCTION_NAME" \
      --region "$AWS_REGION"

    aws lambda update-function-configuration \
      --function-name "$FUNCTION_NAME" \
      --region "$AWS_REGION" \
      --role "$LAMBDA_ROLE_ARN" \
      --runtime python3.12 \
      --handler lambda_function.lambda_handler \
      --timeout 60 \
      --memory-size 256 \
      --environment \
      "Variables={BEDROCK_AGENT_ID=$BEDROCK_AGENT_ID,BEDROCK_AGENT_ALIAS_ID=$BEDROCK_AGENT_ALIAS_ID,ALLOWED_ORIGIN=*}" \
      >/dev/null
else
    echo "Creating new Lambda function..."

    aws lambda create-function \
      --function-name "$FUNCTION_NAME" \
      --region "$AWS_REGION" \
      --runtime python3.12 \
      --role "$LAMBDA_ROLE_ARN" \
      --handler lambda_function.lambda_handler \
      --zip-file fileb://lambda-package.zip \
      --timeout 60 \
      --memory-size 256 \
      --environment \
      "Variables={BEDROCK_AGENT_ID=$BEDROCK_AGENT_ID,BEDROCK_AGENT_ALIAS_ID=$BEDROCK_AGENT_ALIAS_ID,ALLOWED_ORIGIN=*}" \
      >/dev/null
fi

aws lambda wait function-active-v2 \
  --function-name "$FUNCTION_NAME" \
  --region "$AWS_REGION"

LAMBDA_FUNCTION_ARN=$(aws lambda get-function-configuration \
  --function-name "$FUNCTION_NAME" \
  --region "$AWS_REGION" \
  --query 'FunctionArn' \
  --output text)

if grep -q '^LAMBDA_FUNCTION_NAME=' .env; then
  sed -i \
    "s/^LAMBDA_FUNCTION_NAME=.*/LAMBDA_FUNCTION_NAME=$FUNCTION_NAME/" \
    .env
else
  echo "LAMBDA_FUNCTION_NAME=$FUNCTION_NAME" >> .env
fi

if grep -q '^LAMBDA_FUNCTION_ARN=' .env; then
  sed -i \
    "s|^LAMBDA_FUNCTION_ARN=.*|LAMBDA_FUNCTION_ARN=$LAMBDA_FUNCTION_ARN|" \
    .env
else
  echo "LAMBDA_FUNCTION_ARN=$LAMBDA_FUNCTION_ARN" >> .env
fi

echo
echo "Lambda function: $FUNCTION_NAME"
echo "Lambda ARN: $LAMBDA_FUNCTION_ARN"

aws lambda get-function-configuration \
  --function-name "$FUNCTION_NAME" \
  --region "$AWS_REGION" \
  --query '{
    FunctionName:FunctionName,
    Runtime:Runtime,
    Handler:Handler,
    State:State,
    Timeout:Timeout,
    Memory:MemorySize
  }' \
  --output table

Expected status:

State: Active
Runtime: python3.12
Handler: lambda_function.lambda_handler
Timeout: 60
Memory: 256
Step 14C — Test the Lambda Backend

The Lambda function uses the Bedrock Agent Runtime. The answer is returned in the bytes field of the agent response chunk. Reusing the same sessionId later allows the UI to continue the same conversation.

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

cat > lambda-test-event.json <<'EOF'
{
  "question": "What should I check when a Kubernetes node becomes unavailable? Give me easy numbered steps.",
  "sessionId": "opsai-lambda-test-001"
}
EOF

aws lambda invoke \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  --region "$AWS_REGION" \
  --cli-binary-format raw-in-base64-out \
  --payload file://lambda-test-event.json \
  lambda-response.json

AWS CLI version 2 requires raw-in-base64-out when literal JSON is supplied as the invocation payload.

Now display the answer clearly:

python3 - <<'PY'
import json

with open("lambda-response.json", encoding="utf-8") as file:
    response = json.load(file)

print("Lambda status code:", response.get("statusCode"))

body = response.get("body", "")

try:
    parsed_body = json.loads(body)
    print(json.dumps(parsed_body, indent=2))
except (json.JSONDecodeError, TypeError):
    print(body)
PY
Expected result
Lambda status code: 200
{
  "answer": "Here are the steps...",
  "sessionId": "opsai-lambda-test-001"
}

Complete Step 14B and Step 14C


Steps15
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 15A — Create the API Gateway HTTP API

API Gateway will provide a public HTTPS endpoint that the chatbot web page can call. HTTP APIs support Lambda proxy integration, CORS and automatic deployment.

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

API_NAME="opsai-assistant-http-api"

cat > api-cors.json <<'EOF'
{
  "AllowOrigins": ["*"],
  "AllowMethods": ["POST", "OPTIONS"],
  "AllowHeaders": ["content-type"],
  "MaxAge": 300
}
EOF

# Check whether the API already exists
API_ID=$(aws apigatewayv2 get-apis \
  --region "$AWS_REGION" \
  --query "Items[?Name=='$API_NAME'].ApiId | [0]" \
  --output text)

if [[ -z "$API_ID" || "$API_ID" == "None" ]]; then
  echo "Creating HTTP API..."

  API_ID=$(aws apigatewayv2 create-api \
    --name "$API_NAME" \
    --protocol-type HTTP \
    --cors-configuration file://api-cors.json \
    --region "$AWS_REGION" \
    --query 'ApiId' \
    --output text)
else
  echo "Using existing HTTP API: $API_ID"

  aws apigatewayv2 update-api \
    --api-id "$API_ID" \
    --cors-configuration file://api-cors.json \
    --region "$AWS_REGION" \
    >/dev/null
fi

echo "API ID: $API_ID"

CORS is needed because the chatbot UI and API will initially use different web addresses. API Gateway can automatically answer browser OPTIONS preflight requests after CORS is configured.

Step 15B — Connect API Gateway to Lambda

Run:

# Find an existing integration
INTEGRATION_ID=$(aws apigatewayv2 get-integrations \
  --api-id "$API_ID" \
  --region "$AWS_REGION" \
  --query 'Items[0].IntegrationId' \
  --output text)

# Create the Lambda integration when it does not exist
if [[ -z "$INTEGRATION_ID" || "$INTEGRATION_ID" == "None" ]]; then
  echo "Creating Lambda integration..."

  INTEGRATION_ID=$(aws apigatewayv2 create-integration \
    --api-id "$API_ID" \
    --integration-type AWS_PROXY \
    --integration-uri "$LAMBDA_FUNCTION_ARN" \
    --payload-format-version 2.0 \
    --timeout-in-millis 30000 \
    --region "$AWS_REGION" \
    --query 'IntegrationId' \
    --output text)
fi

echo "Integration ID: $INTEGRATION_ID"

# Check whether the POST /chat route exists
ROUTE_ID=$(aws apigatewayv2 get-routes \
  --api-id "$API_ID" \
  --region "$AWS_REGION" \
  --query "Items[?RouteKey=='POST /chat'].RouteId | [0]" \
  --output text)

if [[ -z "$ROUTE_ID" || "$ROUTE_ID" == "None" ]]; then
  echo "Creating POST /chat route..."

  ROUTE_ID=$(aws apigatewayv2 create-route \
    --api-id "$API_ID" \
    --route-key "POST /chat" \
    --target "integrations/$INTEGRATION_ID" \
    --region "$AWS_REGION" \
    --query 'RouteId' \
    --output text)
fi

echo "Route ID: $ROUTE_ID"

# Create the default stage with automatic deployment
STAGE_NAME=$(aws apigatewayv2 get-stages \
  --api-id "$API_ID" \
  --region "$AWS_REGION" \
  --query "Items[?StageName=='\$default'].StageName | [0]" \
  --output text)

if [[ -z "$STAGE_NAME" || "$STAGE_NAME" == "None" ]]; then
  aws apigatewayv2 create-stage \
    --api-id "$API_ID" \
    --stage-name '$default' \
    --auto-deploy \
    --region "$AWS_REGION" \
    >/dev/null
else
  aws apigatewayv2 update-stage \
    --api-id "$API_ID" \
    --stage-name '$default' \
    --auto-deploy \
    --region "$AWS_REGION" \
    >/dev/null
fi

The Lambda proxy integration forwards the browser request to Lambda and returns Lambda’s response to the browser. We explicitly selected payload version 2.0, which matches the request format supported by the Lambda code we created.

Grant API Gateway permission

API Gateway requires permission in the Lambda function’s resource policy before it can invoke the function.

Run:

ACCOUNT_ID=$(aws sts get-caller-identity \
  --query Account \
  --output text)

PERMISSION_ID="OpsAIApiGatewayInvoke"

# Remove an old statement with the same name, if present
aws lambda remove-permission \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  --statement-id "$PERMISSION_ID" \
  --region "$AWS_REGION" \
  >/dev/null 2>&1 || true

# Add API Gateway permission
aws lambda add-permission \
  --function-name "$LAMBDA_FUNCTION_NAME" \
  --statement-id "$PERMISSION_ID" \
  --action lambda:InvokeFunction \
  --principal apigateway.amazonaws.com \
  --source-arn \
  "arn:aws:execute-api:${AWS_REGION}:${ACCOUNT_ID}:${API_ID}/*/POST/chat" \
  --region "$AWS_REGION" \
  >/dev/null

API_ENDPOINT="https://${API_ID}.execute-api.${AWS_REGION}.amazonaws.com"
CHAT_API_URL="${API_ENDPOINT}/chat"

# Save API details
if grep -q '^OPSAI_API_ID=' .env; then
  sed -i "s/^OPSAI_API_ID=.*/OPSAI_API_ID=$API_ID/" .env
else
  echo "OPSAI_API_ID=$API_ID" >> .env
fi

if grep -q '^OPSAI_API_URL=' .env; then
  sed -i "s|^OPSAI_API_URL=.*|OPSAI_API_URL=$CHAT_API_URL|" .env
else
  echo "OPSAI_API_URL=$CHAT_API_URL" >> .env
fi

echo
echo "API ID: $API_ID"
echo "Chat API URL: $CHAT_API_URL"
Step 15C — Test the API Endpoint

Run:

set -a
source .env
set +a

curl -sS \
  -X POST "$OPSAI_API_URL" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "What should I check when a Kubernetes node becomes unavailable? Give me easy numbered steps.",
    "sessionId": "opsai-api-test-001"
  }' \
  | python3 -m json.tool
Expected result
{
  "answer": "Here are the steps to check when a Kubernetes node becomes unavailable...",
  "sessionId": "opsai-api-test-001"
}
Optional CORS validation

Run:

curl -i \
  -X OPTIONS "$OPSAI_API_URL" \
  -H "Origin: https://example.com" \
  -H "Access-Control-Request-Method: POST" \
  -H "Access-Control-Request-Headers: content-type"

You should see headers similar to:

access-control-allow-origin: *
access-control-allow-methods: POST,OPTIONS
access-control-allow-headers: content-type

Complete Steps 15A, 15B and 15C,


Steps16
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 16A — Create the Chatbot Web UI

This creates a browser interface with:

New Chat
Conversation history
Suggested questions
User and assistant message bubbles
Session continuity
Mobile-friendly layout

Run this complete block:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

mkdir -p app/web

cat > app/web/index.html <<'EOF'
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >
    <meta
        name="description"
        content="OpsAI Assistant for enterprise IT operations support"
    >
    <title>OpsAI Assistant</title>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <div class="application">
        <aside class="sidebar" id="sidebar">
            <div class="brand">
                <div class="brand-icon">O</div>

                <div>
                    <h1>OpsAI</h1>
                    <p>Assistant</p>
                </div>
            </div>

            <button class="new-chat-button" id="new-chat-button">
                <span>＋</span>
                New Chat
            </button>

            <div class="history-header">
                <span>Chat history</span>

                <button
                    id="clear-history-button"
                    class="icon-button"
                    title="Clear chat history"
                    aria-label="Clear chat history"
                >
                    🗑
                </button>
            </div>

            <div class="conversation-list" id="conversation-list"></div>

            <div class="sidebar-footer">
                <div class="status-row">
                    <span class="status-dot"></span>
                    OpsAI online
                </div>

                <p>Powered by AWS Bedrock</p>
            </div>
        </aside>

        <main class="chat-area">
            <header class="top-header">
                <button
                    class="mobile-menu-button"
                    id="mobile-menu-button"
                    aria-label="Open navigation"
                >
                    ☰
                </button>

                <div>
                    <h2>OpsAI Assistant</h2>
                    <p>Enterprise IT Operations Support</p>
                </div>

                <div class="environment-badge">AWS MVP</div>
            </header>

            <section class="messages" id="messages">
                <div class="welcome-card" id="welcome-card">
                    <div class="assistant-avatar large-avatar">O</div>

                    <h2>Hello, I am OpsAI Assistant</h2>

                    <p>
                        Ask questions about Kubernetes, RKE2, Jenkins,
                        cloud and infrastructure operations.
                    </p>

                    <div class="suggestion-grid">
                        <button class="suggestion">
                            What should I check when a Kubernetes node
                            becomes unavailable?
                        </button>

                        <button class="suggestion">
                            How do I troubleshoot Kubernetes DiskPressure?
                        </button>

                        <button class="suggestion">
                            What should I check when a Jenkins pipeline fails?
                        </button>
                    </div>

                    <div class="approval-notice">
                        Production changes, reboots and deletions require
                        human approval.
                    </div>
                </div>
            </section>

            <footer class="composer-area">
                <form id="chat-form" class="composer">
                    <textarea
                        id="message-input"
                        rows="1"
                        maxlength="4000"
                        placeholder="Ask OpsAI Assistant..."
                        aria-label="Message"
                    ></textarea>

                    <button
                        id="send-button"
                        type="submit"
                        aria-label="Send message"
                    >
                        ➤
                    </button>
                </form>

                <p class="composer-note">
                    OpsAI can make mistakes. Validate commands before
                    running them in production.
                </p>
            </footer>
        </main>
    </div>

    <script src="app.js"></script>
</body>
</html>
EOF

cat > app/web/styles.css <<'EOF'
:root {
    --primary: #132a4f;
    --primary-light: #1f4d82;
    --accent: #3b82f6;
    --background: #f5f7fb;
    --surface: #ffffff;
    --border: #e2e8f0;
    --text: #172033;
    --muted: #64748b;
    --assistant-message: #ffffff;
    --user-message: #dcecff;
    --success: #16a34a;
    --shadow: 0 8px 28px rgba(15, 23, 42, 0.08);
}

* {
    box-sizing: border-box;
}

html,
body {
    height: 100%;
    margin: 0;
    font-family:
        Inter, ui-sans-serif, system-ui, -apple-system,
        BlinkMacSystemFont, "Segoe UI", sans-serif;
    color: var(--text);
    background: var(--background);
}

button,
textarea {
    font: inherit;
}

button {
    cursor: pointer;
}

.application {
    display: flex;
    height: 100vh;
    overflow: hidden;
}

.sidebar {
    width: 270px;
    display: flex;
    flex-direction: column;
    padding: 22px 16px;
    background: var(--primary);
    color: white;
    transition: transform 0.2s ease;
}

.brand {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 0 8px 24px;
}

.brand-icon,
.assistant-avatar {
    display: grid;
    place-items: center;
    flex-shrink: 0;
    width: 38px;
    height: 38px;
    border-radius: 11px;
    background: linear-gradient(135deg, #60a5fa, #2563eb);
    color: white;
    font-weight: 800;
}

.brand h1,
.brand p {
    margin: 0;
}

.brand h1 {
    font-size: 18px;
}

.brand p {
    color: #bfdbfe;
    font-size: 13px;
}

.new-chat-button {
    width: 100%;
    padding: 11px 14px;
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.1);
    color: white;
    text-align: left;
}

.new-chat-button:hover {
    background: rgba(255, 255, 255, 0.17);
}

.history-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 24px;
    padding: 0 6px 8px;
    color: #bfdbfe;
    font-size: 12px;
    text-transform: uppercase;
}

.icon-button {
    border: 0;
    background: transparent;
    color: inherit;
}

.conversation-list {
    flex: 1;
    overflow-y: auto;
}

.conversation-item {
    width: 100%;
    margin-bottom: 6px;
    padding: 11px 12px;
    overflow: hidden;
    border: 0;
    border-radius: 9px;
    background: transparent;
    color: #dbeafe;
    text-align: left;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.conversation-item:hover,
.conversation-item.active {
    background: rgba(255, 255, 255, 0.13);
    color: white;
}

.sidebar-footer {
    padding: 16px 6px 0;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
    color: #bfdbfe;
    font-size: 12px;
}

.sidebar-footer p {
    margin-bottom: 0;
}

.status-row {
    display: flex;
    align-items: center;
    gap: 7px;
}

.status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #4ade80;
}

.chat-area {
    display: flex;
    flex: 1;
    min-width: 0;
    flex-direction: column;
}

.top-header {
    display: flex;
    align-items: center;
    gap: 15px;
    min-height: 74px;
    padding: 12px 26px;
    border-bottom: 1px solid var(--border);
    background: var(--surface);
}

.top-header h2,
.top-header p {
    margin: 0;
}

.top-header h2 {
    font-size: 17px;
}

.top-header p {
    margin-top: 3px;
    color: var(--muted);
    font-size: 12px;
}

.environment-badge {
    margin-left: auto;
    padding: 6px 10px;
    border-radius: 20px;
    background: #e8f1ff;
    color: #1d4ed8;
    font-size: 12px;
    font-weight: 700;
}

.mobile-menu-button {
    display: none;
    border: 0;
    background: transparent;
    font-size: 22px;
}

.messages {
    flex: 1;
    padding: 28px max(24px, calc((100% - 900px) / 2));
    overflow-y: auto;
}

.welcome-card {
    max-width: 680px;
    margin: 7vh auto 0;
    padding: 34px;
    border: 1px solid var(--border);
    border-radius: 18px;
    background: var(--surface);
    box-shadow: var(--shadow);
    text-align: center;
}

.large-avatar {
    width: 54px;
    height: 54px;
    margin: auto;
    border-radius: 16px;
    font-size: 22px;
}

.welcome-card h2 {
    margin: 18px 0 8px;
}

.welcome-card > p {
    margin: 0 auto 24px;
    color: var(--muted);
    line-height: 1.6;
}

.suggestion-grid {
    display: grid;
    gap: 9px;
}

.suggestion {
    padding: 12px 14px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: #f8fafc;
    color: var(--text);
    text-align: left;
}

.suggestion:hover {
    border-color: #93c5fd;
    background: #eff6ff;
}

.approval-notice {
    margin-top: 20px;
    padding: 11px;
    border-radius: 9px;
    background: #fff7ed;
    color: #9a3412;
    font-size: 12px;
}

.message-row {
    display: flex;
    gap: 12px;
    max-width: 900px;
    margin: 0 auto 20px;
}

.message-row.user {
    justify-content: flex-end;
}

.message-bubble {
    max-width: min(760px, 82%);
    padding: 14px 17px;
    border: 1px solid var(--border);
    border-radius: 15px;
    background: var(--assistant-message);
    box-shadow: 0 3px 14px rgba(15, 23, 42, 0.04);
    line-height: 1.6;
    white-space: pre-wrap;
    word-break: break-word;
}

.message-row.user .message-bubble {
    border-color: #bfdbfe;
    background: var(--user-message);
}

.message-meta {
    margin-top: 8px;
    color: var(--muted);
    font-size: 11px;
}

.typing {
    color: var(--muted);
}

.composer-area {
    padding: 14px max(20px, calc((100% - 900px) / 2)) 16px;
    border-top: 1px solid var(--border);
    background: var(--surface);
}

.composer {
    display: flex;
    align-items: flex-end;
    gap: 10px;
    padding: 8px 8px 8px 15px;
    border: 1px solid #cbd5e1;
    border-radius: 14px;
    background: white;
    box-shadow: 0 3px 12px rgba(15, 23, 42, 0.05);
}

.composer:focus-within {
    border-color: var(--accent);
}

.composer textarea {
    flex: 1;
    max-height: 150px;
    resize: none;
    border: 0;
    outline: 0;
    line-height: 1.5;
}

.composer button {
    width: 42px;
    height: 42px;
    border: 0;
    border-radius: 11px;
    background: var(--accent);
    color: white;
}

.composer button:disabled {
    cursor: not-allowed;
    opacity: 0.55;
}

.composer-note {
    margin: 7px 0 0;
    color: var(--muted);
    font-size: 11px;
    text-align: center;
}

@media (max-width: 760px) {
    .sidebar {
        position: fixed;
        z-index: 10;
        inset: 0 auto 0 0;
        transform: translateX(-100%);
    }

    .sidebar.open {
        transform: translateX(0);
    }

    .mobile-menu-button {
        display: inline-block;
    }

    .top-header {
        padding: 12px 16px;
    }

    .messages {
        padding: 20px 14px;
    }

    .welcome-card {
        margin-top: 2vh;
        padding: 22px;
    }

    .composer-area {
        padding: 10px 12px;
    }

    .message-bubble {
        max-width: 90%;
    }
}
EOF

cat > app/web/app.js <<'EOF'
const API_URL = "__API_URL__";
const STORAGE_KEY = "opsai-conversations-v1";

const messagesElement = document.getElementById("messages");
const conversationList = document.getElementById("conversation-list");
const form = document.getElementById("chat-form");
const input = document.getElementById("message-input");
const sendButton = document.getElementById("send-button");
const newChatButton = document.getElementById("new-chat-button");
const clearHistoryButton = document.getElementById("clear-history-button");
const sidebar = document.getElementById("sidebar");
const mobileMenuButton = document.getElementById("mobile-menu-button");

let conversations = loadConversations();
let activeConversationId = null;
let isSending = false;

function createId(prefix = "id") {
    return `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}

function loadConversations() {
    try {
        const saved = localStorage.getItem(STORAGE_KEY);
        return saved ? JSON.parse(saved) : [];
    } catch (error) {
        console.error("Unable to read chat history:", error);
        return [];
    }
}

function saveConversations() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(conversations));
}

function getActiveConversation() {
    return conversations.find(
        conversation => conversation.id === activeConversationId
    );
}

function createConversation() {
    const conversation = {
        id: createId("chat"),
        sessionId: createId("session"),
        title: "New conversation",
        messages: [],
        updatedAt: Date.now()
    };

    conversations.unshift(conversation);
    activeConversationId = conversation.id;
    saveConversations();
    renderAll();
}

function setConversationTitle(conversation, question) {
    if (conversation.messages.length === 1) {
        conversation.title =
            question.length > 42
                ? `${question.slice(0, 42)}...`
                : question;
    }
}

function renderConversationList() {
    conversationList.innerHTML = "";

    conversations.forEach(conversation => {
        const button = document.createElement("button");
        button.className = "conversation-item";
        button.textContent = conversation.title;

        if (conversation.id === activeConversationId) {
            button.classList.add("active");
        }

        button.addEventListener("click", () => {
            activeConversationId = conversation.id;
            renderAll();
            sidebar.classList.remove("open");
        });

        conversationList.appendChild(button);
    });
}

function renderWelcome() {
    messagesElement.innerHTML = `
        <div class="welcome-card">
            <div class="assistant-avatar large-avatar">O</div>
            <h2>Hello, I am OpsAI Assistant</h2>
            <p>
                Ask questions about Kubernetes, RKE2, Jenkins,
                cloud and infrastructure operations.
            </p>
            <div class="suggestion-grid">
                <button class="suggestion">
                    What should I check when a Kubernetes node becomes unavailable?
                </button>
                <button class="suggestion">
                    How do I troubleshoot Kubernetes DiskPressure?
                </button>
                <button class="suggestion">
                    What should I check when a Jenkins pipeline fails?
                </button>
            </div>
            <div class="approval-notice">
                Production changes, reboots and deletions require human approval.
            </div>
        </div>
    `;

    bindSuggestionButtons();
}

function addMessageElement(message) {
    const row = document.createElement("div");
    row.className = `message-row ${message.role}`;

    if (message.role === "assistant") {
        const avatar = document.createElement("div");
        avatar.className = "assistant-avatar";
        avatar.textContent = "O";
        row.appendChild(avatar);
    }

    const bubble = document.createElement("div");
    bubble.className = "message-bubble";

    const content = document.createElement("div");
    content.textContent = message.content;
    bubble.appendChild(content);

    const meta = document.createElement("div");
    meta.className = "message-meta";
    meta.textContent =
        message.role === "assistant"
            ? "OpsAI Assistant"
            : "You";

    bubble.appendChild(meta);
    row.appendChild(bubble);
    messagesElement.appendChild(row);
}

function renderMessages() {
    const conversation = getActiveConversation();

    if (!conversation || conversation.messages.length === 0) {
        renderWelcome();
        return;
    }

    messagesElement.innerHTML = "";
    conversation.messages.forEach(addMessageElement);
    scrollToBottom();
}

function renderAll() {
    renderConversationList();
    renderMessages();
}

function scrollToBottom() {
    messagesElement.scrollTop = messagesElement.scrollHeight;
}

function showTyping() {
    const row = document.createElement("div");
    row.id = "typing-row";
    row.className = "message-row assistant";

    row.innerHTML = `
        <div class="assistant-avatar">O</div>
        <div class="message-bubble typing">
            OpsAI Assistant is checking the approved runbooks...
        </div>
    `;

    messagesElement.appendChild(row);
    scrollToBottom();
}

function removeTyping() {
    document.getElementById("typing-row")?.remove();
}

async function sendMessage(question) {
    const cleanedQuestion = question.trim();

    if (!cleanedQuestion || isSending) {
        return;
    }

    if (!activeConversationId) {
        createConversation();
    }

    const conversation = getActiveConversation();

    conversation.messages.push({
        role: "user",
        content: cleanedQuestion
    });

    setConversationTitle(conversation, cleanedQuestion);
    conversation.updatedAt = Date.now();

    saveConversations();
    renderAll();

    input.value = "";
    resizeInput();

    isSending = true;
    sendButton.disabled = true;
    showTyping();

    try {
        const response = await fetch(API_URL, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                question: cleanedQuestion,
                sessionId: conversation.sessionId
            })
        });

        let result;

        try {
            result = await response.json();
        } catch {
            throw new Error("The backend returned an invalid response.");
        }

        if (!response.ok) {
            throw new Error(
                result.error || `Request failed with status ${response.status}`
            );
        }

        conversation.sessionId =
            result.sessionId || conversation.sessionId;

        conversation.messages.push({
            role: "assistant",
            content: result.answer || "No answer was returned."
        });
    } catch (error) {
        console.error(error);

        conversation.messages.push({
            role: "assistant",
            content:
                "I could not connect to the OpsAI backend. " +
                "Please try again or contact the platform administrator."
        });
    } finally {
        removeTyping();
        conversation.updatedAt = Date.now();
        saveConversations();

        isSending = false;
        sendButton.disabled = false;

        renderAll();
        input.focus();
    }
}

function bindSuggestionButtons() {
    document.querySelectorAll(".suggestion").forEach(button => {
        button.addEventListener("click", () => {
            sendMessage(button.textContent.trim());
        });
    });
}

function resizeInput() {
    input.style.height = "auto";
    input.style.height = `${Math.min(input.scrollHeight, 150)}px`;
}

form.addEventListener("submit", event => {
    event.preventDefault();
    sendMessage(input.value);
});

input.addEventListener("input", resizeInput);

input.addEventListener("keydown", event => {
    if (event.key === "Enter" && !event.shiftKey) {
        event.preventDefault();
        form.requestSubmit();
    }
});

newChatButton.addEventListener("click", () => {
    createConversation();
    sidebar.classList.remove("open");
    input.focus();
});

clearHistoryButton.addEventListener("click", () => {
    if (!window.confirm("Delete all saved chat history?")) {
        return;
    }

    conversations = [];
    activeConversationId = null;
    saveConversations();
    createConversation();
});

mobileMenuButton.addEventListener("click", () => {
    sidebar.classList.toggle("open");
});

if (conversations.length === 0) {
    createConversation();
} else {
    activeConversationId = conversations[0].id;
    renderAll();
}

input.focus();
EOF

# Add the real API URL without exposing any secret credentials
sed -i \
  "s|__API_URL__|$OPSAI_API_URL|g" \
  app/web/app.js

echo "Frontend files created:"
find app/web -maxdepth 1 -type f -printf '%f\n' | sort

echo
echo "API configured in frontend:"
grep 'const API_URL' app/web/app.js

Expected output:

app.js
index.html
styles.css

const API_URL = "https://...execute-api.us-east-1.amazonaws.com/chat";

The frontend calls the HTTP API from a different website origin, which is why the CORS settings you validated are required.

Step 16B — Create the Amplify Hosting Application

Amplify static applications use platform type WEB. We will create a manually deployed application without connecting GitHub.

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

AMPLIFY_APP_NAME="opsai-assistant-ui"
AMPLIFY_BRANCH_NAME="main"

# Create a ZIP with index.html at the root
rm -f app/opsai-assistant-ui.zip

cd app/web
zip -qr ../opsai-assistant-ui.zip .
cd ../..

echo "Frontend deployment package:"
unzip -l app/opsai-assistant-ui.zip

# Find an existing Amplify application
AMPLIFY_APP_ID=$(aws amplify list-apps \
  --region "$AWS_REGION" \
  --query "apps[?name=='$AMPLIFY_APP_NAME'].appId | [0]" \
  --output text)

# Create the application when it does not exist
if [[ -z "$AMPLIFY_APP_ID" || "$AMPLIFY_APP_ID" == "None" ]]; then
    echo "Creating Amplify application..."

    AMPLIFY_APP_ID=$(aws amplify create-app \
      --name "$AMPLIFY_APP_NAME" \
      --description \
      "Browser interface for the OpsAI Assistant AWS MVP" \
      --platform WEB \
      --no-enable-branch-auto-build \
      --region "$AWS_REGION" \
      --query 'app.appId' \
      --output text)
else
    echo "Using existing Amplify application: $AMPLIFY_APP_ID"
fi

# Check whether the main branch exists
if ! aws amplify get-branch \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" >/dev/null 2>&1
then
    echo "Creating Amplify main branch..."

    aws amplify create-branch \
      --app-id "$AMPLIFY_APP_ID" \
      --branch-name "$AMPLIFY_BRANCH_NAME" \
      --description "OpsAI MVP production branch" \
      --stage PRODUCTION \
      --framework "Web" \
      --no-enable-auto-build \
      --region "$AWS_REGION" \
      >/dev/null
else
    echo "Amplify main branch already exists."
fi

DEFAULT_DOMAIN=$(aws amplify get-app \
  --app-id "$AMPLIFY_APP_ID" \
  --region "$AWS_REGION" \
  --query 'app.defaultDomain' \
  --output text)

AMPLIFY_URL="https://${AMPLIFY_BRANCH_NAME}.${DEFAULT_DOMAIN}"

if grep -q '^AMPLIFY_APP_ID=' .env; then
    sed -i \
      "s/^AMPLIFY_APP_ID=.*/AMPLIFY_APP_ID=$AMPLIFY_APP_ID/" \
      .env
else
    echo "AMPLIFY_APP_ID=$AMPLIFY_APP_ID" >> .env
fi

if grep -q '^AMPLIFY_BRANCH_NAME=' .env; then
    sed -i \
      "s/^AMPLIFY_BRANCH_NAME=.*/AMPLIFY_BRANCH_NAME=$AMPLIFY_BRANCH_NAME/" \
      .env
else
    echo "AMPLIFY_BRANCH_NAME=$AMPLIFY_BRANCH_NAME" >> .env
fi

if grep -q '^AMPLIFY_URL=' .env; then
    sed -i \
      "s|^AMPLIFY_URL=.*|AMPLIFY_URL=$AMPLIFY_URL|" \
      .env
else
    echo "AMPLIFY_URL=$AMPLIFY_URL" >> .env
fi

echo
echo "Amplify App ID: $AMPLIFY_APP_ID"
echo "Branch: $AMPLIFY_BRANCH_NAME"
echo "Planned website URL: $AMPLIFY_URL"
Step 16C — Deploy the Chatbot UI

For a manual Amplify deployment, create-deployment returns a ZIP upload URL and a job ID. After uploading the ZIP, start-deployment starts the hosting job.

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

echo "Deployment job ID: $JOB_ID"
echo "Uploading frontend package..."

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

echo
echo "Starting Amplify deployment..."

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

echo "Waiting for deployment to complete..."

while true
do
    DEPLOYMENT_STATUS=$(aws amplify get-job \
      --app-id "$AMPLIFY_APP_ID" \
      --branch-name "$AMPLIFY_BRANCH_NAME" \
      --job-id "$JOB_ID" \
      --region "$AWS_REGION" \
      --query 'job.summary.status' \
      --output text)

    echo "Deployment status: $DEPLOYMENT_STATUS"

    case "$DEPLOYMENT_STATUS" in
        SUCCEED)
            break
            ;;
        FAILED|CANCELLED)
            echo "Amplify deployment failed."
            exit 1
            ;;
        *)
            sleep 10
            ;;
    esac
done

echo
echo "OpsAI chatbot URL:"
echo "$AMPLIFY_URL"

Amplify Hosting publishes static sites through its CDN and provides HTTPS hosting.

Final validation

After the deployment shows SUCCEED, open the URL displayed by the command.

Test:

What should I check when a Kubernetes node becomes unavailable?

You should see the answer inside the browser-based chatbot page

____________________________________

Step 16D — Update CGI Branding

Run this in CloudShell:

cd ~/opsai-assistant-aws

# Replace the existing footer text
sed -i \
  's|<p>Powered by AWS Bedrock</p>|<p class="cgi-powered">Powered by CGI</p>|' \
  app/web/index.html

# Add bold red CGI styling
cat >> app/web/styles.css <<'EOF'

.cgi-powered {
    margin-top: 10px;
    color: #ff0000;
    font-size: 14px;
    font-weight: 800;
    letter-spacing: 0.3px;
}
EOF

# Verify the change
grep -n "Powered by CGI" app/web/index.html
tail -n 10 app/web/styles.css
Step 16E — Redeploy the Updated UI

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

# Rebuild the frontend ZIP
rm -f app/opsai-assistant-ui.zip

cd app/web
zip -qr ../opsai-assistant-ui.zip .
cd ../..

# Create a new Amplify deployment
DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

# Upload the updated UI
curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

# Start deployment
aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

echo "Waiting for deployment..."

while true
do
  STATUS=$(aws amplify get-job \
    --app-id "$AMPLIFY_APP_ID" \
    --branch-name "$AMPLIFY_BRANCH_NAME" \
    --job-id "$JOB_ID" \
    --region "$AWS_REGION" \
    --query 'job.summary.status' \
    --output text)

  echo "Deployment status: $STATUS"

  case "$STATUS" in
    SUCCEED)
      break
      ;;
    FAILED|CANCELLED)
      echo "Deployment failed."
      exit 1
      ;;
    *)
      sleep 10
      ;;
  esac
done

echo
echo "Updated OpsAI URL:"
echo "$AMPLIFY_URL"

After it shows SUCCEED, refresh the browser using Ctrl + F5. The bottom-left corner will show:

Powered by CGI

in bold red text.

________________________________________

Add the creator and contact details below Powered by CGI in the bottom-left corner.

Step 16F — Add Creator and Contact Details

Run:

cd ~/opsai-assistant-aws

python3 - <<'PY'
from pathlib import Path

html_file = Path("app/web/index.html")
html = html_file.read_text(encoding="utf-8")

old_content = '<p class="cgi-powered">Powered by CGI</p>'

new_content = '''<p class="cgi-powered">Powered by CGI</p>

                <div class="creator-information">
                    <p>
                        Created by
                        <strong>Ravi Kiran Maroju</strong>
                    </p>

                    <p>
                        For suggestions or concerns, reach out to
                        <a href="mailto:ravi.maroju@cgi.com">
                            ravi.maroju@cgi.com
                        </a>
                    </p>
                </div>'''

if new_content in html:
    print("Creator information is already present.")
elif old_content in html:
    html = html.replace(old_content, new_content)
    html_file.write_text(html, encoding="utf-8")
    print("Creator information added successfully.")
else:
    raise SystemExit("Unable to find the Powered by CGI footer.")
PY

cat >> app/web/styles.css <<'EOF'

.creator-information {
    margin-top: 14px;
    padding-top: 12px;
    border-top: 1px solid rgba(255, 255, 255, 0.16);
    color: #dbeafe;
    font-size: 11px;
    line-height: 1.5;
}

.creator-information p {
    margin: 5px 0;
}

.creator-information strong {
    color: #ffffff;
    font-weight: 700;
}

.creator-information a {
    color: #93c5fd;
    font-weight: 600;
    text-decoration: none;
    word-break: break-word;
}

.creator-information a:hover {
    color: #ffffff;
    text-decoration: underline;
}
EOF

grep -n -A15 "Powered by CGI" app/web/index.html

It will display:

Powered by CGI

Created by Ravi Kiran Maroju

For suggestions or concerns, reach out to
ravi.maroju@cgi.com

The email address will also be clickable.

Step 16G — Redeploy the Updated UI

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

rm -f app/opsai-assistant-ui.zip

cd app/web
zip -qr ../opsai-assistant-ui.zip .
cd ../..

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

while true
do
  STATUS=$(aws amplify get-job \
    --app-id "$AMPLIFY_APP_ID" \
    --branch-name "$AMPLIFY_BRANCH_NAME" \
    --job-id "$JOB_ID" \
    --region "$AWS_REGION" \
    --query 'job.summary.status' \
    --output text)

  echo "Deployment status: $STATUS"

  case "$STATUS" in
    SUCCEED)
      break
      ;;
    FAILED|CANCELLED)
      echo "Deployment failed."
      exit 1
      ;;
    *)
      sleep 10
      ;;
  esac
done

echo "Updated OpsAI URL: $AMPLIFY_URL"

After SUCCEED, open the URL and press Ctrl + F5 to load the updated design.

Steps17
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 17 — Fix Chat Response Formatting

This will display:

**text** as bold
Commands such as kubectl get nodes in code format
Numbered and bullet lists properly
Multiline code blocks clearly

It will also safely escape HTML to protect the chatbot page.

Step 17A — Update the JavaScript Formatter

Run this in CloudShell:

cd ~/opsai-assistant-aws

python3 - <<'PY'
from pathlib import Path

app_file = Path("app/web/app.js")
content = app_file.read_text(encoding="utf-8")

marker = '''function createId(prefix = "id") {
    return `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}
'''

formatter = r'''
function escapeHtml(value) {
    const element = document.createElement("div");
    element.textContent = String(value ?? "");
    return element.innerHTML;
}

function formatInlineMarkdown(value) {
    let safeText = escapeHtml(value);

    safeText = safeText.replace(
        /`([^`\n]+)`/g,
        "<code>$1</code>"
    );

    safeText = safeText.replace(
        /\*\*([^*]+)\*\*/g,
        "<strong>$1</strong>"
    );

    safeText = safeText.replace(
        /\*([^*\n]+)\*/g,
        "<em>$1</em>"
    );

    return safeText;
}

function renderMarkdown(markdown) {
    const lines = String(markdown ?? "").replace(/\r/g, "").split("\n");
    const output = [];

    let listType = null;
    let insideCodeBlock = false;
    let codeLines = [];

    function closeList() {
        if (listType) {
            output.push(`</${listType}>`);
            listType = null;
        }
    }

    function openList(type) {
        if (listType === type) {
            return;
        }

        closeList();
        output.push(`<${type}>`);
        listType = type;
    }

    for (const line of lines) {
        if (line.trim().startsWith("```")) {
            closeList();

            if (insideCodeBlock) {
                output.push(
                    `<pre><code>${escapeHtml(codeLines.join("\n"))}</code></pre>`
                );
                codeLines = [];
                insideCodeBlock = false;
            } else {
                insideCodeBlock = true;
            }

            continue;
        }

        if (insideCodeBlock) {
            codeLines.push(line);
            continue;
        }

        const headingMatch = line.match(/^(#{1,3})\s+(.+)$/);

        if (headingMatch) {
            closeList();

            const level = headingMatch[1].length + 2;

            output.push(
                `<h${level}>${formatInlineMarkdown(headingMatch[2])}</h${level}>`
            );

            continue;
        }

        const orderedMatch = line.match(/^\s*\d+\.\s+(.+)$/);

        if (orderedMatch) {
            openList("ol");
            output.push(
                `<li>${formatInlineMarkdown(orderedMatch[1])}</li>`
            );
            continue;
        }

        const bulletMatch = line.match(/^\s*[-•]\s+(.+)$/);

        if (bulletMatch) {
            openList("ul");
            output.push(
                `<li>${formatInlineMarkdown(bulletMatch[1])}</li>`
            );
            continue;
        }

        if (!line.trim()) {
            closeList();
            continue;
        }

        closeList();
        output.push(`<p>${formatInlineMarkdown(line)}</p>`);
    }

    closeList();

    if (insideCodeBlock && codeLines.length) {
        output.push(
            `<pre><code>${escapeHtml(codeLines.join("\n"))}</code></pre>`
        );
    }

    return output.join("");
}
'''

if "function renderMarkdown(markdown)" not in content:
    if marker not in content:
        raise SystemExit("Could not find the JavaScript insertion point.")

    content = content.replace(marker, marker + formatter)
    print("Markdown formatter added.")
else:
    print("Markdown formatter already exists.")

old_block = '''    const content = document.createElement("div");
    content.textContent = message.content;
    bubble.appendChild(content);
'''

new_block = '''    const content = document.createElement("div");

    if (message.role === "assistant") {
        content.className = "markdown-content";
        content.innerHTML = renderMarkdown(message.content);
    } else {
        content.textContent = message.content;
    }

    bubble.appendChild(content);
'''

if new_block in content:
    print("Assistant response rendering is already updated.")
elif old_block in content:
    content = content.replace(old_block, new_block)
    print("Assistant response rendering updated.")
else:
    raise SystemExit("Could not find the message rendering section.")

app_file.write_text(content, encoding="utf-8")
PY

grep -n "renderMarkdown" app/web/app.js
Step 17B — Add Formatting Styles

Run:

cat >> ~/opsai-assistant-aws/app/web/styles.css <<'EOF'

/* OpsAI response formatting */

.markdown-content p {
    margin: 0 0 11px;
}

.markdown-content p:last-child {
    margin-bottom: 0;
}

.markdown-content h3,
.markdown-content h4,
.markdown-content h5 {
    margin: 17px 0 8px;
    color: #132a4f;
    line-height: 1.35;
}

.markdown-content ol,
.markdown-content ul {
    margin: 8px 0 14px;
    padding-left: 25px;
}

.markdown-content li {
    margin: 6px 0;
    padding-left: 3px;
}

.markdown-content strong {
    color: #132a4f;
    font-weight: 750;
}

.markdown-content code {
    padding: 2px 6px;
    border: 1px solid #dbeafe;
    border-radius: 5px;
    background: #eff6ff;
    color: #1e3a8a;
    font-family:
        "Cascadia Code",
        "Consolas",
        monospace;
    font-size: 0.92em;
}

.markdown-content pre {
    margin: 12px 0;
    padding: 14px;
    overflow-x: auto;
    border-radius: 10px;
    background: #0f172a;
}

.markdown-content pre code {
    padding: 0;
    border: 0;
    background: transparent;
    color: #e2e8f0;
    white-space: pre;
}
EOF
Step 17C — Redeploy the Updated UI

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

rm -f app/opsai-assistant-ui.zip

cd app/web
zip -qr ../opsai-assistant-ui.zip .
cd ../..

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

while true
do
    STATUS=$(aws amplify get-job \
      --app-id "$AMPLIFY_APP_ID" \
      --branch-name "$AMPLIFY_BRANCH_NAME" \
      --job-id "$JOB_ID" \
      --region "$AWS_REGION" \
      --query 'job.summary.status' \
      --output text)

    echo "Deployment status: $STATUS"

    case "$STATUS" in
        SUCCEED)
            break
            ;;
        FAILED|CANCELLED)
            echo "Deployment failed."
            exit 1
            ;;
        *)
            sleep 10
            ;;
    esac
done

echo "Updated chatbot URL: $AMPLIFY_URL"

After it shows SUCCEED, press Ctrl + F5 in the browser and ask:

What are the benefits of OpsAI? Give the answer with bold headings and numbered points.

The ** symbols should disappear, and the headings should display in bold.


Steps18
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 18A — Add OpsAI Overview and Benefits

Run this in CloudShell:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

cat > data/opsai-assistant-overview.md <<'EOF'
# OpsAI Assistant Overview

## What is OpsAI Assistant?

OpsAI Assistant is an enterprise IT operations support chatbot created to help cloud operations, Kubernetes, DevOps, SRE, infrastructure, service desk and application support teams.

It uses approved operational documents and runbooks stored in an Amazon Bedrock Knowledge Base.

## Current MVP Capabilities

The current AWS MVP can:

1. Answer questions using approved runbooks.
2. Support Kubernetes, RKE2 and Jenkins troubleshooting.
3. Provide easy numbered troubleshooting steps.
4. provide useful validation commands.
5. Maintain conversation context within the same chat session.
6. Show previous conversations in browser chat history.
7. Warn users that production changes require human approval.
8. Be accessed through a browser-based chatbot interface.

## Benefits of OpsAI Assistant

### 1. Faster Troubleshooting

OpsAI Assistant quickly searches approved operational runbooks and provides relevant troubleshooting steps. This can reduce the time spent manually searching documents.

### 2. Consistent Support

The assistant provides standardized guidance from approved sources. This helps different support engineers follow the same troubleshooting process.

### 3. 24x7 Self-Service Access

Users can access operational guidance at any time through the chatbot interface, subject to AWS service availability.

### 4. Knowledge Sharing

Important operational knowledge can be made available to support teams, new employees and project members through a common chatbot.

### 5. Faster Employee Onboarding

New team members can use OpsAI Assistant to understand approved procedures, troubleshooting commands and validation steps.

### 6. Reduced Dependency on Individual Experts

Operational knowledge is stored in an approved central Knowledge Base instead of depending only on the availability of specific engineers.

### 7. Human Approval for Critical Changes

OpsAI Assistant must request human approval before recommending or performing restarts, reboots, deletions, scaling, patching or production changes.

### 8. Validation After Troubleshooting

The assistant provides validation steps so that users can confirm whether the issue is resolved and services are operating correctly.

### 9. Reusable Across Projects

The solution can be reused for cloud operations, Kubernetes, DevOps, infrastructure and application-support requirements across different delivery teams and clients.

### 10. Future Integration Possibilities

Future versions can integrate with approved APIs for Jira, Jenkins, Kubernetes, monitoring tools, email and automation platforms.

These integrations are not enabled for automatic production actions in the current MVP.

## Security and Governance

The current MVP:

- Uses approved Knowledge Base documents.
- Does not automatically execute production changes.
- Requires human approval for critical actions.
- Uses IAM permissions to control AWS access.
- Stores application logs in Amazon CloudWatch.
- Provides validation steps after troubleshooting.

## Current MVP Architecture

The request flow is:

User browser interface  
→ Amazon Amplify Hosting  
→ Amazon API Gateway  
→ AWS Lambda  
→ Amazon Bedrock Agent  
→ Amazon Bedrock Knowledge Base  
→ Approved runbooks stored in Amazon S3

## Creator and Contact

OpsAI Assistant was created by Ravi Kiran Maroju.

For suggestions or concerns, contact:

ravi.maroju@cgi.com
EOF

aws s3 cp \
  data/opsai-assistant-overview.md \
  "s3://$OPSAI_BUCKET/knowledge/opsai-assistant-overview.md"

echo
echo "Uploaded documents:"
aws s3 ls "s3://$OPSAI_BUCKET/knowledge/"

You should see:

opsai-assistant-overview.md
Step 18B — Sync the Updated Knowledge Base

Amazon Bedrock checks new, changed and deleted S3 documents when the data source is synchronized. The start-ingestion-job operation starts this sync and indexing process.

Run:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

KB_ID=$(aws bedrock-agent list-knowledge-bases \
  --region "$AWS_REGION" \
  --query \
  "knowledgeBaseSummaries[?name=='opsai-assistant-kb'].knowledgeBaseId | [0]" \
  --output text)

if [[ -z "$KB_ID" || "$KB_ID" == "None" ]]; then
  echo "ERROR: Could not find opsai-assistant-kb."
  exit 1
fi

DATA_SOURCE_ID=$(aws bedrock-agent list-data-sources \
  --knowledge-base-id "$KB_ID" \
  --region "$AWS_REGION" \
  --query \
  "dataSourceSummaries[?name=='opsai-runbooks-s3'].dataSourceId | [0]" \
  --output text)

if [[ -z "$DATA_SOURCE_ID" || "$DATA_SOURCE_ID" == "None" ]]; then
  echo "ERROR: Could not find opsai-runbooks-s3."
  exit 1
fi

echo "Knowledge Base ID: $KB_ID"
echo "Data Source ID: $DATA_SOURCE_ID"

INGESTION_JOB_ID=$(aws bedrock-agent start-ingestion-job \
  --knowledge-base-id "$KB_ID" \
  --data-source-id "$DATA_SOURCE_ID" \
  --description "Add OpsAI overview and benefits document" \
  --region "$AWS_REGION" \
  --query 'ingestionJob.ingestionJobId' \
  --output text)

echo "Ingestion Job ID: $INGESTION_JOB_ID"
echo "Waiting for Knowledge Base sync..."

while true
do
  STATUS=$(aws bedrock-agent get-ingestion-job \
    --knowledge-base-id "$KB_ID" \
    --data-source-id "$DATA_SOURCE_ID" \
    --ingestion-job-id "$INGESTION_JOB_ID" \
    --region "$AWS_REGION" \
    --query 'ingestionJob.status' \
    --output text)

  echo "Sync status: $STATUS"

  case "$STATUS" in
    COMPLETE)
      break
      ;;
    FAILED|STOPPED)
      echo "Knowledge Base sync was not successful."

      aws bedrock-agent get-ingestion-job \
        --knowledge-base-id "$KB_ID" \
        --data-source-id "$DATA_SOURCE_ID" \
        --ingestion-job-id "$INGESTION_JOB_ID" \
        --region "$AWS_REGION" \
        --query 'ingestionJob.failureReasons' \
        --output json

      exit 1
      ;;
    *)
      sleep 10
      ;;
  esac
done

aws bedrock-agent get-ingestion-job \
  --knowledge-base-id "$KB_ID" \
  --data-source-id "$DATA_SOURCE_ID" \
  --ingestion-job-id "$INGESTION_JOB_ID" \
  --region "$AWS_REGION" \
  --query \
  'ingestionJob.{Status:status,Statistics:statistics,FailureReasons:failureReasons}' \
  --output json

The expected final status is:

COMPLETE

AWS provides the Knowledge Base ID, data-source ID and ingestion-job ID to start and check an ingestion job.

Step 18C — Test from the UI

After the sync shows COMPLETE:

Open the OpsAI chatbot.
Press Ctrl + F5.
Click New Chat.
Ask:
What are the benefits of OpsAI? Give the answer with bold headings and numbered points.

Also test:

Who created OpsAI Assistant and whom should I contact for suggestions?

The assistant should now provide the benefits with properly formatted bold headings and identify Ravi Kiran Maroju with the contact address ravi.maroju@cgi.com.

_____________________

Step 18D — Fix HTML Tags in the UI

Run this in CloudShell:

cd ~/opsai-assistant-aws

python3 - <<'PY'
from pathlib import Path

app_file = Path("app/web/app.js")
content = app_file.read_text(encoding="utf-8")

old_code = '''function renderMarkdown(markdown) {
    const lines = String(markdown ?? "").replace(/\\r/g, "").split("\\n");
'''

new_code = r'''function normalizeModelOutput(value) {
    let text = String(value ?? "");

    // Convert HTML code blocks to Markdown code blocks.
    text = text.replace(
        /<pre[^>]*>\s*<code[^>]*>([\s\S]*?)<\/code>\s*<\/pre>/gi,
        (_, code) => `\`\`\`\n${code.trim()}\n\`\`\``
    );

    // Convert HTML headings to Markdown headings.
    text = text.replace(
        /<h([1-6])[^>]*>([\s\S]*?)<\/h\1>/gi,
        (_, level, heading) =>
            `${"#".repeat(Math.min(Number(level), 3))} ${heading.trim()}\n`
    );

    // Convert ordered HTML lists to numbered Markdown lists.
    text = text.replace(
        /<ol[^>]*>([\s\S]*?)<\/ol>/gi,
        (_, body) => {
            let number = 0;

            return body.replace(
                /<li[^>]*>([\s\S]*?)<\/li>/gi,
                (_, item) => `${++number}. ${item.trim()}\n`
            );
        }
    );

    // Convert unordered HTML lists to Markdown bullet lists.
    text = text.replace(
        /<ul[^>]*>([\s\S]*?)<\/ul>/gi,
        (_, body) =>
            body.replace(
                /<li[^>]*>([\s\S]*?)<\/li>/gi,
                (_, item) => `- ${item.trim()}\n`
            )
    );

    // Convert basic formatting tags.
    text = text.replace(
        /<strong[^>]*>([\s\S]*?)<\/strong>/gi,
        "**$1**"
    );

    text = text.replace(
        /<b[^>]*>([\s\S]*?)<\/b>/gi,
        "**$1**"
    );

    text = text.replace(
        /<em[^>]*>([\s\S]*?)<\/em>/gi,
        "*$1*"
    );

    text = text.replace(
        /<i[^>]*>([\s\S]*?)<\/i>/gi,
        "*$1*"
    );

    text = text.replace(
        /<code[^>]*>([\s\S]*?)<\/code>/gi,
        "`$1`"
    );

    text = text.replace(/<br\s*\/?>/gi, "\n");

    text = text.replace(
        /<p[^>]*>([\s\S]*?)<\/p>/gi,
        "$1\n"
    );

    // Remove remaining common layout tags.
    text = text.replace(
        /<\/?(?:div|span|section|article|header|footer|html|body)[^>]*>/gi,
        ""
    );

    return text.replace(/\n{3,}/g, "\n\n").trim();
}

function renderMarkdown(markdown) {
    const normalized = normalizeModelOutput(markdown);
    const lines = normalized.replace(/\r/g, "").split("\n");
'''

if new_code in content:
    print("HTML normalization is already configured.")
elif old_code in content:
    content = content.replace(old_code, new_code)
    app_file.write_text(content, encoding="utf-8")
    print("HTML normalization added successfully.")
else:
    raise SystemExit(
        "Could not find renderMarkdown. Do not continue with deployment."
    )
PY

grep -n "normalizeModelOutput" app/web/app.js
Redeploy the UI
cd ~/opsai-assistant-aws

set -a
source .env
set +a

rm -f app/opsai-assistant-ui.zip

cd app/web
zip -qr ../opsai-assistant-ui.zip .
cd ../..

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

while true
do
    STATUS=$(aws amplify get-job \
      --app-id "$AMPLIFY_APP_ID" \
      --branch-name "$AMPLIFY_BRANCH_NAME" \
      --job-id "$JOB_ID" \
      --region "$AWS_REGION" \
      --query 'job.summary.status' \
      --output text)

    echo "Deployment status: $STATUS"

    case "$STATUS" in
        SUCCEED)
            break
            ;;
        FAILED|CANCELLED)
            echo "Deployment failed."
            exit 1
            ;;
        *)
            sleep 10
            ;;
    esac
done

echo "UI deployment completed: $AMPLIFY_URL"
Step 18E — Strengthen the Agent Instructions

The current alias uses a numbered agent version. Changes made in Agent Builder first apply to the working draft; after saving, the agent must be prepared and the deployed alias must be moved to a new version.

Open:

Amazon Bedrock
→ Agents Classic
→ opsai-assistant-agent
→ Edit in Agent Builder

Replace the complete Instructions for the Agent with:

You are OpsAI Assistant, an enterprise IT operations support agent.

GROUNDING RULES:

1. Always search the connected Knowledge Base before answering questions about Kubernetes, RKE2, Jenkins, cloud, infrastructure, OpsAI Assistant, its benefits, creator or contact details.

2. Use only information available in the approved Knowledge Base and runbooks.

3. Do not invent names, email addresses, commands, procedures, capabilities or contact information.

4. If the Knowledge Base does not contain the answer, clearly say that the information is not available in the approved Knowledge Base.

OPSAI IDENTITY:

OpsAI Assistant was created by Ravi Kiran Maroju.

For suggestions or concerns, the approved contact address is ravi.maroju@cgi.com.

Never provide a different creator name, generic support team or another email address.

RESPONSE FORMAT:

Use Markdown only.

Never return HTML or XML tags such as h1, h2, strong, ol, ul, li, div or span.

Give answers in easy English using clear headings, numbered steps and bullet points.

Put commands inside Markdown backticks.

SAFETY:

For any restart, reboot, deletion, scaling, patching or production change, explain the possible impact and require human approval before the action is performed.

Always provide validation steps after troubleshooting or recovery actions.

Then:

Click Save.
Click Prepare.
Wait for successfully prepared.
Test in the Agent Builder:
Who created OpsAI Assistant and whom should I contact for suggestions?

The working-draft test should answer:

OpsAI Assistant was created by Ravi Kiran Maroju.

For suggestions or concerns, contact ravi.maroju@cgi.com.
Step 18F — Update the Deployed Alias

Preparing updates the working draft used for testing. Your application invokes the opsai-mvp alias, so that alias must be associated with the updated numbered version.

After the Agent Builder test succeeds:

Click Save and exit.
Scroll to Aliases.
Select opsai-mvp.
Click Edit.
Choose Create a new version and associate it with this alias.
Keep On-demand.
Click Save.
Wait until the alias status becomes PREPARED.

Finally, open the chatbot, press Ctrl + F5, click New Chat, and test:

What are the benefits of OpsAI? Give the answer with bold headings and numbered points.
Who created OpsAI Assistant and whom should I contact for suggestions?

The HTML tags should disappear, and the creator/contact answer should be correct.

Steps19
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 19A — Test DiskPressure Retrieval Directly

Run this in CloudShell:

cd ~/opsai-assistant-aws

set -a
source .env
set +a

KB_ID=$(aws bedrock-agent list-knowledge-bases \
  --region "$AWS_REGION" \
  --query "knowledgeBaseSummaries[?name=='opsai-assistant-kb'].knowledgeBaseId | [0]" \
  --output text)

echo "Knowledge Base ID: $KB_ID"

aws bedrock-agent-runtime retrieve \
  --knowledge-base-id "$KB_ID" \
  --retrieval-query \
  '{"text":"How do I troubleshoot Kubernetes DiskPressure and what commands should I run?"}' \
  --retrieval-configuration \
  '{"vectorSearchConfiguration":{"numberOfResults":10}}' \
  --region "$AWS_REGION" \
  --query 'retrievalResults[].{
    Score:score,
    Source:location.s3Location.uri,
    Text:content.text
  }' \
  --output json \
  > diskpressure-retrieval.json

python3 - <<'PY'
import json

with open("diskpressure-retrieval.json", encoding="utf-8") as file:
    results = json.load(file)

print(f"Retrieved results: {len(results)}")
print()

for number, result in enumerate(results, start=1):
    print(f"Result {number}")
    print(f"Score : {result.get('Score')}")
    print(f"Source: {result.get('Source')}")
    print(f"Text  : {str(result.get('Text', ''))[:500]}")
    print("-" * 80)
PY
Correct result

At least one source should contain:

disk-pressure-runbook.md

and text such as:

df -h
df -i
kubectl describe node NODE_NAME
DiskPressure condition should show False
Also do this quick UI test

Click New Chat before asking:

According to the Kubernetes Disk Pressure Runbook, what commands should I run to diagnose DiskPressure?

Using a new chat prevents an earlier topic from influencing the current session.

Steps20
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 20 — Add Demo Worker Nodes and Approvals
1. Download the setup script

add_opsai_demo_tabs.sh

2. Upload it to CloudShell

In AWS CloudShell:

Click Actions.
Select Upload file.
Upload add_opsai_demo_tabs.sh.
3. Run the script
chmod +x ~/add_opsai_demo_tabs.sh
bash ~/add_opsai_demo_tabs.sh

The script will:

Back up your current UI.
Add Worker Nodes and Approvals to the sidebar.
Add three dummy worker nodes.
Add approval and rejection buttons.
Add simulated validation after approval.
Redeploy the updated UI to Amplify.

Wait until you see:

Deployment status: SUCCEED

Demo tabs added successfully.
4. Refresh the chatbot

Open your existing OpsAI URL and press:

Ctrl + F5

You should see:

+ New Chat
▦ Worker Nodes
✓ Approvals
Demo flow
Open Worker Nodes.
Find the NotReady or DiskPressure node.
Click Request Approval.
Open the Approvals tab.
Click Approve.
Click Simulate Approved Action.

The request will become COMPLETED and show dummy validation:

Node status: Ready
DiskPressure: False
Application pods: Running
No real action was executed

A clear DEMO ONLY warning will remain visible so nobody thinks the page is connected to a real production cluster.

Below is the content for add_opsai_demo_tabs.sh ( so create a file and save it as add_opsai_demo_tabs.sh and copy and paste below words and upload it)


2. Upload it to CloudShell

In AWS CloudShell:

Click Actions.
Select Upload file.
Upload add_opsai_demo_tabs.sh.
3. Run the script
chmod +x ~/add_opsai_demo_tabs.sh
bash ~/add_opsai_demo_tabs.sh

The script will:

Back up your current UI.
Add Worker Nodes and Approvals to the sidebar.
Add three dummy worker nodes.
Add approval and rejection buttons.
Add simulated validation after approval.
Redeploy the updated UI to Amplify.

Wait until you see:

Deployment status: SUCCEED

Demo tabs added successfully.
4. Refresh the chatbot

Open your existing OpsAI URL and press:

Ctrl + F5

You should see:

+ New Chat
▦ Worker Nodes
✓ Approvals
Demo flow
Open Worker Nodes.
Find the NotReady or DiskPressure node.
Click Request Approval.
Open the Approvals tab.
Click Approve.
Click Simulate Approved Action.

The request will become COMPLETED and show dummy validation:

Node status: Ready
DiskPressure: False
Application pods: Running
No real action was executed

A clear DEMO ONLY warning will remain visible so nobody thinks the page is connected to a real production cluster.


_________________

copy below content

#!/usr/bin/env bash
set -euo pipefail

cd "$HOME/opsai-assistant-aws"

if [[ ! -f .env ]]; then
  echo "ERROR: $HOME/opsai-assistant-aws/.env was not found."
  exit 1
fi

set -a
source .env
set +a

required_vars=(AWS_REGION AMPLIFY_APP_ID AMPLIFY_BRANCH_NAME AMPLIFY_URL)
for var in "${required_vars[@]}"; do
  if [[ -z "${!var:-}" ]]; then
    echo "ERROR: $var is missing from .env"
    exit 1
  fi
done

timestamp=$(date +%Y%m%d_%H%M%S)
cp -a app/web "app/web-backup-before-demo-tabs-${timestamp}"

python3 - <<'PY'
from pathlib import Path

index_file = Path("app/web/index.html")
html = index_file.read_text(encoding="utf-8")

if 'href="demo-ops.css"' not in html:
    html = html.replace(
        '<link rel="stylesheet" href="styles.css">',
        '<link rel="stylesheet" href="styles.css">\n'
        '    <link rel="stylesheet" href="demo-ops.css">'
    )

if 'src="demo-ops.js"' not in html:
    html = html.replace(
        '<script src="app.js"></script>',
        '<script src="app.js"></script>\n'
        '    <script src="demo-ops.js"></script>'
    )

index_file.write_text(html, encoding="utf-8")
print("index.html updated.")
PY

cat > app/web/demo-ops.css <<'EOF'
.demo-nav {
    display: grid;
    gap: 7px;
    margin-top: 9px;
}

.demo-nav-button {
    display: flex;
    align-items: center;
    gap: 9px;
    width: 100%;
    padding: 11px 14px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 10px;
    background: transparent;
    color: #dbeafe;
    text-align: left;
}

.demo-nav-button:hover,
.demo-nav-button.active {
    background: rgba(255, 255, 255, 0.13);
    color: #ffffff;
}

.demo-count {
    min-width: 21px;
    margin-left: auto;
    padding: 2px 6px;
    border-radius: 999px;
    background: #ef4444;
    color: white;
    font-size: 11px;
    font-weight: 800;
    text-align: center;
}

.demo-panel {
    flex: 1;
    padding: 26px max(24px, calc((100% - 1120px) / 2));
    overflow-y: auto;
}

.demo-hidden {
    display: none !important;
}

.demo-view-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 16px;
}

.demo-view-header h2,
.demo-view-header p {
    margin: 0;
}

.demo-view-header p {
    margin-top: 5px;
    color: #64748b;
}

.demo-warning {
    margin-bottom: 18px;
    padding: 13px 15px;
    border: 1px solid #fed7aa;
    border-radius: 10px;
    background: #fff7ed;
    color: #9a3412;
    line-height: 1.5;
}

.demo-summary-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
    margin-bottom: 18px;
}

.demo-summary-card {
    padding: 17px;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    background: #ffffff;
}

.demo-summary-card span {
    display: block;
    color: #64748b;
    font-size: 12px;
}

.demo-summary-card strong {
    display: block;
    margin-top: 6px;
    font-size: 25px;
}

.demo-summary-card.healthy {
    border-left: 4px solid #16a34a;
}

.demo-summary-card.issue {
    border-left: 4px solid #dc2626;
}

.demo-summary-card.pending {
    border-left: 4px solid #f59e0b;
}

.demo-node-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 15px;
}

.demo-node-card,
.demo-approval-card,
.demo-empty-state {
    padding: 18px;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    background: #ffffff;
    box-shadow: 0 4px 18px rgba(15, 23, 42, 0.05);
}

.demo-node-card.healthy {
    border-top: 4px solid #16a34a;
}

.demo-node-card.issue {
    border-top: 4px solid #dc2626;
}

.demo-node-card-header,
.demo-approval-card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 15px;
}

.demo-node-card h3,
.demo-node-card p,
.demo-approval-card h3,
.demo-approval-card p {
    margin: 0;
}

.demo-node-card-header p,
.demo-approval-card-header p {
    margin-top: 4px;
    color: #64748b;
    font-size: 12px;
}

.demo-status {
    display: inline-flex;
    padding: 5px 9px;
    border-radius: 999px;
    background: #e2e8f0;
    color: #334155;
    font-size: 11px;
    font-weight: 800;
}

.demo-status.ready,
.demo-status.completed,
.demo-status.approved {
    background: #dcfce7;
    color: #166534;
}

.demo-status.notready,
.demo-status.rejected {
    background: #fee2e2;
    color: #991b1b;
}

.demo-status.pending {
    background: #fef3c7;
    color: #92400e;
}

.demo-node-metrics {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin: 15px 0;
}

.demo-node-metrics div,
.demo-approval-details div {
    padding: 11px;
    border-radius: 9px;
    background: #f8fafc;
}

.demo-node-metrics span,
.demo-approval-details strong {
    display: block;
    color: #64748b;
    font-size: 11px;
}

.demo-node-metrics strong {
    display: block;
    margin-top: 4px;
}

.demo-node-issue {
    margin-top: 12px;
}

.demo-node-issue p {
    margin-top: 5px;
    color: #475569;
    line-height: 1.5;
}

.demo-primary-button,
.demo-secondary-button,
.demo-approve-button,
.demo-reject-button,
.demo-execute-button {
    padding: 10px 14px;
    border-radius: 9px;
    font-weight: 700;
}

.demo-primary-button {
    width: 100%;
    margin-top: 16px;
    border: 0;
    background: #2563eb;
    color: #ffffff;
}

.demo-primary-button:disabled {
    background: #e2e8f0;
    color: #64748b;
    cursor: default;
}

.demo-secondary-button {
    border: 1px solid #cbd5e1;
    background: #ffffff;
    color: #334155;
}

.demo-approval-list {
    display: grid;
    gap: 14px;
}

.demo-approval-details {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
    margin: 15px 0;
}

.demo-approval-details p {
    margin-top: 5px;
    line-height: 1.45;
}

.demo-approval-actions {
    display: flex;
    justify-content: flex-end;
    gap: 9px;
}

.demo-approve-button {
    border: 0;
    background: #16a34a;
    color: #ffffff;
}

.demo-reject-button {
    border: 1px solid #fecaca;
    background: #fff1f2;
    color: #b91c1c;
}

.demo-execute-button {
    border: 0;
    background: #2563eb;
    color: #ffffff;
}

.demo-validation {
    margin: 14px 0;
    padding: 13px;
    border-radius: 10px;
    background: #f0fdf4;
    color: #166534;
}

.demo-validation ul {
    margin: 8px 0 0;
    padding-left: 20px;
}

.demo-empty-state {
    padding: 40px;
    color: #64748b;
    text-align: center;
}

@media (max-width: 850px) {
    .demo-summary-grid,
    .demo-node-grid,
    .demo-approval-details {
        grid-template-columns: 1fr;
    }

    .demo-panel {
        padding: 18px 14px;
    }

    .demo-view-header {
        align-items: flex-start;
        flex-direction: column;
    }
}
EOF

cat > app/web/demo-ops.js <<'EOF'
(() => {
  const APPROVALS_KEY = 'opsai-demo-approvals-v1';
  const RECOVERED_KEY = 'opsai-demo-recovered-v1';

  const nodes = [
    {
      name: 'lin-advqa-rke2-node1.ams.com',
      status: 'Ready', condition: 'Healthy', pods: 18,
      issue: 'No active issue', action: 'No action required', impact: 'None'
    },
    {
      name: 'lin-advqa-rke2-node2.ams.com',
      status: 'NotReady', condition: 'RKE2 agent unavailable', pods: 7,
      issue: 'The RKE2 agent is not responding and application pods may be affected.',
      action: 'Cordon, drain and restart the RKE2 agent',
      impact: 'Pods may move to other workers. A short impact is possible if capacity is insufficient.'
    },
    {
      name: 'lin-advqa-rke2-node3.ams.com',
      status: 'Ready', condition: 'DiskPressure=True', pods: 14,
      issue: 'Root filesystem usage is 92%.',
      action: 'Clean approved old logs and revalidate DiskPressure',
      impact: 'No reboot is planned. Only approved files are included in this demo request.'
    }
  ];

  let approvals = read(APPROVALS_KEY, []);
  let recovered = read(RECOVERED_KEY, []);

  const sidebar = document.getElementById('sidebar');
  const newChat = document.getElementById('new-chat-button');
  const messages = document.getElementById('messages');
  const composer = document.querySelector('.composer-area');
  const chatArea = document.querySelector('.chat-area');
  const title = document.querySelector('.top-header h2');
  const subtitle = document.querySelector('.top-header p');

  if (!sidebar || !newChat || !messages || !composer || !chatArea) return;

  const nav = document.createElement('div');
  nav.className = 'demo-nav';
  nav.innerHTML = `
    <button class="demo-nav-button" data-view="workers">▦ Worker Nodes</button>
    <button class="demo-nav-button" data-view="approvals">
      ✓ Approvals <span id="approval-count" class="demo-count">0</span>
    </button>`;
  newChat.insertAdjacentElement('afterend', nav);

  const workersPanel = document.createElement('section');
  const approvalsPanel = document.createElement('section');
  workersPanel.className = 'demo-panel demo-hidden';
  approvalsPanel.className = 'demo-panel demo-hidden';
  chatArea.insertBefore(workersPanel, composer);
  chatArea.insertBefore(approvalsPanel, composer);

  function read(key, fallback) {
    try { return JSON.parse(localStorage.getItem(key)) || fallback; }
    catch { return fallback; }
  }

  function save() {
    localStorage.setItem(APPROVALS_KEY, JSON.stringify(approvals));
    localStorage.setItem(RECOVERED_KEY, JSON.stringify(recovered));
  }

  function esc(value) {
    return String(value).replace(/[&<>"']/g, char => ({
      '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;'
    }[char]));
  }

  function currentNode(node) {
    return recovered.includes(node.name)
      ? {...node, status: 'Ready', condition: 'Healthy',
          issue: 'Demo recovery completed', action: 'No action required'}
      : node;
  }

  function show(view) {
    const isChat = view === 'chat';
    messages.style.display = isChat ? '' : 'none';
    composer.style.display = isChat ? '' : 'none';
    workersPanel.classList.toggle('demo-hidden', view !== 'workers');
    approvalsPanel.classList.toggle('demo-hidden', view !== 'approvals');

    nav.querySelectorAll('button').forEach(button =>
      button.classList.toggle('active', button.dataset.view === view));

    if (title && subtitle) {
      title.textContent = view === 'workers' ? 'Worker Node Health'
        : view === 'approvals' ? 'Approvals' : 'OpsAI Assistant';

      subtitle.textContent = view === 'workers'
        ? 'Dummy cluster health for the demo'
        : view === 'approvals'
          ? 'Human approval workflow demonstration'
          : 'Enterprise IT Operations Support';
    }

    if (view === 'workers') renderWorkers();
    if (view === 'approvals') renderApprovals();
    sidebar.classList.remove('open');
  }

  function renderWorkers() {
    const displayNodes = nodes.map(currentNode);
    const issues = displayNodes.filter(node => node.condition !== 'Healthy').length;

    workersPanel.innerHTML = `
      <div class="demo-view-header">
        <div><h2>Worker Nodes</h2><p>Dummy data for the hackathon demonstration.</p></div>
        <button class="demo-secondary-button" data-reset>Reset Demo</button>
      </div>

      <div class="demo-warning">
        <strong>DEMO ONLY:</strong>
        No live cluster connection and no real command is executed.
      </div>

      <div class="demo-summary-grid">
        <div class="demo-summary-card"><span>Total workers</span><strong>${displayNodes.length}</strong></div>
        <div class="demo-summary-card healthy"><span>Healthy</span><strong>${displayNodes.length - issues}</strong></div>
        <div class="demo-summary-card issue"><span>Issues</span><strong>${issues}</strong></div>
        <div class="demo-summary-card pending"><span>Pending</span><strong>${approvals.filter(a => a.status === 'PENDING').length}</strong></div>
      </div>

      <div class="demo-node-grid">${displayNodes.map(nodeCard).join('')}</div>`;
  }

  function nodeCard(node) {
    const healthy = node.condition === 'Healthy';
    const openRequest = approvals.find(
      item => item.node === node.name &&
      ['PENDING', 'APPROVED'].includes(item.status)
    );

    return `
      <article class="demo-node-card ${healthy ? 'healthy' : 'issue'}">
        <div class="demo-node-card-header">
          <div><h3>${esc(node.name)}</h3><p>Worker</p></div>
          <span class="demo-status ${node.status.toLowerCase()}">${esc(node.status)}</span>
        </div>

        <div class="demo-node-metrics">
          <div><span>Condition</span><strong>${esc(node.condition)}</strong></div>
          <div><span>Pods</span><strong>${node.pods}</strong></div>
        </div>

        <div class="demo-node-issue">
          <strong>Finding</strong><p>${esc(node.issue)}</p>
        </div>

        <div class="demo-node-issue">
          <strong>Recommended action</strong><p>${esc(node.action)}</p>
        </div>

        ${healthy
          ? '<button class="demo-primary-button" disabled>No approval required</button>'
          : openRequest
            ? '<button class="demo-primary-button" data-open-approvals>View approval request</button>'
            : `<button class="demo-primary-button" data-request="${esc(node.name)}">Request Approval</button>`}
      </article>`;
  }

  function requestApproval(nodeName) {
    const node = nodes.find(item => item.name === nodeName);
    if (!node) return;

    const exists = approvals.some(
      item => item.node === nodeName &&
      ['PENDING', 'APPROVED'].includes(item.status)
    );

    if (!exists) {
      approvals.unshift({
        id: `APR-${Date.now()}`,
        node: node.name,
        action: node.action,
        reason: node.issue,
        impact: node.impact,
        status: 'PENDING',
        requestedBy: 'OpsAI Demo User',
        requestedAt: new Date().toLocaleString(),
        approver: 'Not assigned',
        validation: []
      });
      save();
    }

    updateCount();
    show('approvals');
  }

  function renderApprovals() {
    approvalsPanel.innerHTML = `
      <div class="demo-view-header">
        <div><h2>Approvals</h2><p>Dummy human-in-the-loop workflow.</p></div>
        <button class="demo-secondary-button" data-reset>Reset Demo</button>
      </div>

      <div class="demo-warning">
        <strong>DEMO ONLY:</strong>
        Approve, reject and simulate update this browser only.
      </div>

      <div class="demo-approval-list">
        ${approvals.length
          ? approvals.map(approvalCard).join('')
          : '<div class="demo-empty-state"><h3>No approval requests</h3><p>Open Worker Nodes and request approval for a dummy issue.</p></div>'}
      </div>`;
  }

  function approvalCard(item) {
    const buttons = item.status === 'PENDING'
      ? `<button class="demo-reject-button" data-reject="${item.id}">Reject</button>
         <button class="demo-approve-button" data-approve="${item.id}">Approve</button>`
      : item.status === 'APPROVED'
        ? `<button class="demo-execute-button" data-execute="${item.id}">Simulate Approved Action</button>`
        : '';

    const validation = item.validation.length
      ? `<div class="demo-validation">
           <strong>Demo validation</strong>
           <ul>${item.validation.map(value => `<li>${esc(value)}</li>`).join('')}</ul>
         </div>`
      : '';

    return `
      <article class="demo-approval-card">
        <div class="demo-approval-card-header">
          <div><span>${esc(item.id)}</span><h3>${esc(item.action)}</h3><p>${esc(item.node)}</p></div>
          <span class="demo-status ${item.status.toLowerCase()}">${item.status}</span>
        </div>

        <div class="demo-approval-details">
          <div><strong>Reason</strong><p>${esc(item.reason)}</p></div>
          <div><strong>Possible impact</strong><p>${esc(item.impact)}</p></div>
          <div><strong>Requested by</strong><p>${esc(item.requestedBy)}</p></div>
          <div><strong>Approver</strong><p>${esc(item.approver)}</p></div>
        </div>

        ${validation}
        <div class="demo-approval-actions">${buttons}</div>
      </article>`;
  }

  function decide(id, status) {
    const item = approvals.find(value => value.id === id);
    if (!item) return;

    item.status = status;
    item.approver = 'Demo Project Approver';
    save();
    updateCount();
    renderApprovals();
  }

  function simulate(id) {
    const item = approvals.find(value => value.id === id);
    if (!item || item.status !== 'APPROVED') return;

    item.status = 'COMPLETED';
    item.validation = [
      'Node status: Ready',
      'DiskPressure: False',
      'Application pods: Running',
      'No real action was executed'
    ];

    if (!recovered.includes(item.node)) recovered.push(item.node);

    save();
    updateCount();
    renderApprovals();
  }

  function reset() {
    if (!window.confirm('Reset dummy nodes and approval requests?')) return;

    approvals = [];
    recovered = [];
    save();
    updateCount();
    renderWorkers();
    renderApprovals();
  }

  function updateCount() {
    const badge = document.getElementById('approval-count');
    if (badge) {
      badge.textContent = approvals.filter(
        item => item.status === 'PENDING'
      ).length;
    }
  }

  nav.addEventListener('click', event => {
    const button = event.target.closest('[data-view]');
    if (button) show(button.dataset.view);
  });

  workersPanel.addEventListener('click', event => {
    const request = event.target.closest('[data-request]');
    if (request) requestApproval(request.dataset.request);
    if (event.target.closest('[data-open-approvals]')) show('approvals');
    if (event.target.closest('[data-reset]')) reset();
  });

  approvalsPanel.addEventListener('click', event => {
    const approve = event.target.closest('[data-approve]');
    const reject = event.target.closest('[data-reject]');
    const execute = event.target.closest('[data-execute]');

    if (approve) decide(approve.dataset.approve, 'APPROVED');
    if (reject) decide(reject.dataset.reject, 'REJECTED');
    if (execute) simulate(execute.dataset.execute);
    if (event.target.closest('[data-reset]')) reset();
  });

  newChat.addEventListener('click', () => show('chat'));
  updateCount();
})();
EOF

node --check app/web/demo-ops.js

rm -f app/opsai-assistant-ui.zip
(
  cd app/web
  zip -qr ../opsai-assistant-ui.zip .
)

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

echo "Waiting for deployment..."

while true; do
  STATUS=$(aws amplify get-job \
    --app-id "$AMPLIFY_APP_ID" \
    --branch-name "$AMPLIFY_BRANCH_NAME" \
    --job-id "$JOB_ID" \
    --region "$AWS_REGION" \
    --query 'job.summary.status' \
    --output text)

  echo "Deployment status: $STATUS"

  case "$STATUS" in
    SUCCEED)
      break
      ;;
    FAILED|CANCELLED)
      echo "Deployment failed."
      exit 1
      ;;
    *)
      sleep 10
      ;;
  esac
done

echo
echo "Demo tabs added successfully."
echo "Open: $AMPLIFY_URL"
echo "Refresh the browser using Ctrl + F5."


Steps21
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step 20 — Add Infrastructure Assets and Redeploy the UI

Run the complete block in AWS CloudShell:

cd ~/opsai-assistant-aws

set -e
export AWS_PAGER=""

# Load the AWS and Amplify variables.
set -a
source .env
set +a

# Confirm the required files exist.
if [[ ! -f app/web/demo-ops.js ]]; then
    echo "ERROR: app/web/demo-ops.js was not found."
    exit 1
fi

if [[ ! -f app/web/demo-ops.css ]]; then
    echo "ERROR: app/web/demo-ops.css was not found."
    exit 1
fi

# Create backups before changing the UI.
BACKUP_TIME=$(date +%Y%m%d-%H%M%S)

cp app/web/demo-ops.js \
   "app/web/demo-ops.js.backup-$BACKUP_TIME"

cp app/web/demo-ops.css \
   "app/web/demo-ops.css.backup-$BACKUP_TIME"

echo "Backup completed: $BACKUP_TIME"

# ------------------------------------------------------------
# Update the Infrastructure Assets JavaScript
# ------------------------------------------------------------

python3 - <<'PY'
from pathlib import Path
import re

file_path = Path("app/web/demo-ops.js")
content = file_path.read_text(encoding="utf-8")

new_nodes = r"""  const nodes = [
    {
      name: 'lin-advqa-rke2-node1.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'QA',
      status: 'Compliant',
      condition: 'Healthy',
      workloads: '18 pods',
      severity: 'None',
      issue: 'No active issue',
      action: 'No action required',
      impact: 'None'
    },
    {
      name: 'lin-advqa-rke2-node2.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'QA',
      status: 'NotReady',
      condition: 'RKE2 agent unavailable',
      workloads: '7 pods',
      severity: 'High',
      issue: 'The RKE2 agent is not responding and application pods may be affected.',
      action: 'Cordon, drain and restart the RKE2 agent',
      impact: 'Pods may move to other workers. Application impact is possible if sufficient capacity is not available.'
    },
    {
      name: 'lin-advqa-rke2-node3.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'QA',
      status: 'Vulnerable',
      condition: 'DiskPressure=True',
      workloads: '14 pods',
      severity: 'High',
      issue: 'Root filesystem usage is 92% and security updates are pending.',
      action: 'Clean approved old logs, apply security updates and validate DiskPressure',
      impact: 'Application pods may be affected if the filesystem becomes full.'
    },
    {
      name: 'lin-advdev-rke2-node6.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'Development',
      status: 'PatchRequired',
      condition: 'Kernel security updates pending',
      workloads: '12 pods',
      severity: 'Critical',
      issue: 'Critical operating-system and kernel security updates are pending.',
      action: 'Cordon, drain, apply approved patches and reboot during the maintenance window',
      impact: 'Workloads will move to other available workers during maintenance.'
    },
    {
      name: 'lin-advdev-rke2-node7.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'Development',
      status: 'RebootRequired',
      condition: 'Kernel updated',
      workloads: '9 pods',
      severity: 'High',
      issue: 'Security patches are installed, but the updated kernel is not active.',
      action: 'Cordon, drain and reboot the node after downtime approval',
      impact: 'The node will be temporarily unavailable while it reboots.'
    },
    {
      name: 'lin-advdev-rke2-node17.ams.com',
      assetType: 'RKE2 Worker',
      environment: 'Development',
      status: 'Vulnerable',
      condition: 'OpenSSL updates pending',
      workloads: '11 pods',
      severity: 'High',
      issue: 'Security updates for OpenSSL and operating-system libraries are pending.',
      action: 'Apply approved security updates and restart affected services',
      impact: 'A brief service interruption may occur when affected services restart.'
    },
    {
      name: 'lin-adv-ran-1rke2.ams.com',
      assetType: 'RKE2 Control Plane',
      environment: 'Management',
      status: 'PatchRequired',
      condition: 'Security remediation required',
      workloads: 'Control plane',
      severity: 'Critical',
      issue: 'Remotely exploitable vulnerabilities with exposure paths require remediation.',
      action: 'Apply approved security patches and reboot one control-plane node at a time',
      impact: 'The cluster should remain available when quorum is maintained and nodes are patched sequentially.'
    },
    {
      name: 'lin-adv-ran-2rke2.ams.com',
      assetType: 'RKE2 Control Plane',
      environment: 'Management',
      status: 'Compliant',
      condition: 'Healthy',
      workloads: 'Control plane',
      severity: 'None',
      issue: 'Required security patches are applied and validation has passed.',
      action: 'No action required',
      impact: 'None'
    },
    {
      name: 'lin-adv-ran-3rke2.ams.com',
      assetType: 'RKE2 Control Plane',
      environment: 'Management',
      status: 'RebootRequired',
      condition: 'Patching completed',
      workloads: 'Control plane',
      severity: 'High',
      issue: 'Security patches are installed and a reboot is required to activate the updated kernel.',
      action: 'Reboot after confirming cluster quorum and maintenance approval',
      impact: 'Temporary control-plane capacity reduction during the reboot.'
    },
    {
      name: 'lin-advqa-nfs.ams.com',
      assetType: 'Linux NFS Server',
      environment: 'QA',
      status: 'PatchRequired',
      condition: 'NFS and kernel updates pending',
      workloads: 'NFS storage',
      severity: 'Critical',
      issue: 'Operating-system, kernel and NFS security updates are pending.',
      action: 'Confirm application downtime, apply patches and reboot the server',
      impact: 'Applications using this NFS server may be unavailable during the reboot.'
    },
    {
      name: 'lin-rundeck-new.ams.com',
      assetType: 'Automation Server',
      environment: 'Shared Services',
      status: 'Vulnerable',
      condition: 'OpenSSH and Python updates pending',
      workloads: 'Ansible and Rundeck',
      severity: 'High',
      issue: 'Security updates are required for OpenSSH, Python and operating-system packages.',
      action: 'Apply approved updates and validate Rundeck and Ansible jobs',
      impact: 'Automation jobs may be unavailable while services are restarted.'
    },
    {
      name: 'w10jenkinstest1.ams.com',
      assetType: 'Windows Jenkins Server',
      environment: 'Testing',
      status: 'PatchRequired',
      condition: 'Windows cumulative update pending',
      workloads: 'Jenkins',
      severity: 'Critical',
      issue: 'Critical Windows security and cumulative updates are pending.',
      action: 'Apply Windows updates and schedule a controlled reboot',
      impact: 'Jenkins jobs will be unavailable during the reboot.'
    },
    {
      name: 'w10jenkinstest.ams.com',
      assetType: 'Windows Application Server',
      environment: 'Testing',
      status: 'RebootRequired',
      condition: 'Windows patches installed',
      workloads: 'Application services',
      severity: 'High',
      issue: 'Windows security patches are installed, but a reboot is pending.',
      action: 'Confirm application downtime and reboot the server',
      impact: 'Application services will be temporarily unavailable.'
    },
    {
      name: 'lin-advperf-ub-master1.ams.com',
      assetType: 'Ubuntu RKE2 Server',
      environment: 'Performance',
      status: 'Vulnerable',
      condition: 'Ubuntu security updates pending',
      workloads: 'RKE2 control plane',
      severity: 'High',
      issue: 'Ubuntu operating-system and container-runtime updates are pending.',
      action: 'Apply approved updates and validate RKE2 services',
      impact: 'Control-plane capacity may be temporarily reduced during maintenance.'
    },
    {
      name: 'lin-advtech-rke2ub-local1.ams.com',
      assetType: 'Ubuntu RKE2 Server',
      environment: 'Technology',
      status: 'Compliant',
      condition: 'Healthy',
      workloads: 'RKE2 control plane and worker',
      severity: 'None',
      issue: 'Security patches are current and system validation has passed.',
      action: 'No action required',
      impact: 'None'
    }
  ];"""

# Replace the existing worker-node or infrastructure-asset list.
content, count = re.subn(
    r"  const nodes = \[[\s\S]*?\n  \];",
    new_nodes,
    content,
    count=1
)

if count != 1:
    raise SystemExit(
        "ERROR: Unable to locate and replace the existing nodes list."
    )

# Update the visible navigation and page wording.
replacements = {
    "▦ Worker Nodes":
        "▦ Infrastructure Assets",

    "'Worker Node Health'":
        "'Infrastructure Health'",

    "'Worker node health and operational status'":
        "'Patch, vulnerability and worker-node status'",

    "'Dummy cluster health for the demo'":
        "'Patch, vulnerability and worker-node status'",

    "<h2>Worker Nodes</h2>":
        "<h2>Infrastructure Assets</h2>",

    "<span>Total workers</span>":
        "<span>Total assets</span>",

    "Reset Demo":
        "Reset View",

    "Simulate Approved Action":
        "Run Approved Action",

    "OpsAI Demo User":
        "OpsAI User",

    "Demo Project Approver":
        "Project Approver",

    "Demo validation":
        "Validation results",

    "Dummy human-in-the-loop workflow.":
        "Human approval workflow for operational changes.",

    "Open Worker Nodes and request approval for a dummy issue.":
        "Open Infrastructure Assets and request approval for a remediation item.",

    "Open Worker Nodes and request approval for an item.":
        "Open Infrastructure Assets and request approval for a remediation item.",

    "No real action was executed":
        "Remediation workflow completed"
}

for old, new in replacements.items():
    content = content.replace(old, new)

# Update the asset-card heading.
old_header = """<div><h3>${esc(node.name)}</h3><p>Worker</p></div>"""

new_header = """<div>
            <h3>${esc(node.name)}</h3>
            <p>${esc(node.assetType)}</p>
          </div>"""

if old_header in content:
    content = content.replace(old_header, new_header)

# Update the card metrics.
old_metrics = """<div class="demo-node-metrics">
          <div><span>Condition</span><strong>${esc(node.condition)}</strong></div>
          <div><span>Pods</span><strong>${node.pods}</strong></div>
        </div>"""

new_metrics = """<div class="demo-node-metrics">
          <div>
            <span>Condition</span>
            <strong>${esc(node.condition)}</strong>
          </div>
          <div>
            <span>Environment</span>
            <strong>${esc(node.environment)}</strong>
          </div>
          <div>
            <span>Workloads</span>
            <strong>${esc(node.workloads)}</strong>
          </div>
          <div>
            <span>Severity</span>
            <strong>${esc(node.severity)}</strong>
          </div>
        </div>"""

if old_metrics in content:
    content = content.replace(old_metrics, new_metrics)
elif "node.workloads" not in content:
    raise SystemExit(
        "ERROR: Unable to locate the card metrics section."
    )

# Update the recovered asset status.
content = re.sub(
    r"""function currentNode\(node\) \{[\s\S]*?\n  \}""",
    """function currentNode(node) {
    return recovered.includes(node.name)
      ? {
          ...node,
          status: 'Compliant',
          condition: 'Healthy',
          severity: 'None',
          issue: 'Remediation completed and validated',
          action: 'No action required'
        }
      : node;
  }""",
    content,
    count=1
)

# Update the completion validation result.
content, validation_count = re.subn(
    r"""    item\.validation = \[[\s\S]*?\n    \];""",
    """    item.validation = [
      'Asset status: Compliant',
      'Required security patches: Applied',
      'Critical vulnerabilities: Resolved',
      'Service validation: Passed',
      'Remediation workflow: Completed'
    ];""",
    content,
    count=1
)

if validation_count != 1:
    raise SystemExit(
        "ERROR: Unable to update the approval validation results."
    )

# Remove subtitles under the Infrastructure Assets heading.
content = re.sub(
    r"""<div>
        <h2>(?:Infrastructure Assets|Worker Nodes)</h2>
        <p>[^<]*</p>
        </div>""",
    r"<div><h2>Infrastructure Assets</h2></div>",
    content,
    flags=re.VERBOSE
)

content = re.sub(
    r"""<div><h2>(?:Infrastructure Assets|Worker Nodes)</h2>
        <p>[^<]*</p></div>""",
    "<div><h2>Infrastructure Assets</h2></div>",
    content,
    flags=re.VERBOSE
)

# Remove warning or information boxes from both infrastructure
# and approval pages.
content = re.sub(
    r'<div class="demo-warning">[\s\S]*?</div>',
    "",
    content
)

file_path.write_text(content, encoding="utf-8")

print("Infrastructure Assets UI updated successfully.")
print("Total infrastructure assets configured: 15")
PY

# ------------------------------------------------------------
# Add the infrastructure status colours
# ------------------------------------------------------------

if ! grep -q "Infrastructure patch and vulnerability statuses" \
     app/web/demo-ops.css
then
cat >> app/web/demo-ops.css <<'EOF'

/* Infrastructure patch and vulnerability statuses */

.demo-status.compliant {
    background: #dcfce7;
    color: #166534;
}

.demo-status.patchrequired {
    background: #fee2e2;
    color: #991b1b;
}

.demo-status.rebootrequired {
    background: #fef3c7;
    color: #92400e;
}

.demo-status.vulnerable {
    background: #ffedd5;
    color: #9a3412;
}

.demo-status.notready {
    background: #fee2e2;
    color: #991b1b;
}
EOF
fi

# ------------------------------------------------------------
# Validate the updated files
# ------------------------------------------------------------

node --check app/web/demo-ops.js

echo
echo "Checking configured infrastructure assets..."

ASSET_COUNT=$(grep -c "assetType:" app/web/demo-ops.js)

echo "Infrastructure asset count: $ASSET_COUNT"

if [[ "$ASSET_COUNT" -ne 15 ]]; then
    echo "ERROR: Expected 15 infrastructure assets."
    exit 1
fi

echo
echo "Checking that the unwanted wording is removed..."

if grep -nE \
   "Sample operational data|PROTOTYPE|DEMO ONLY|Dummy data|No live cluster" \
   app/web/demo-ops.js
then
    echo "ERROR: Unwanted wording is still present."
    exit 1
else
    echo "Confirmed: unwanted wording is removed."
fi

echo
echo "Configured statuses:"

grep -oE \
"status: '(Compliant|PatchRequired|RebootRequired|Vulnerable|NotReady)'" \
app/web/demo-ops.js |
sort |
uniq -c

# ------------------------------------------------------------
# Package the updated UI
# ------------------------------------------------------------

rm -f app/opsai-assistant-ui.zip

(
    cd app/web
    zip -qr ../opsai-assistant-ui.zip .
)

echo
echo "UI package created:"
ls -lh app/opsai-assistant-ui.zip

# ------------------------------------------------------------
# Create and start the Amplify deployment
# ------------------------------------------------------------

DEPLOYMENT_OUTPUT=$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)

JOB_ID=$(echo "$DEPLOYMENT_OUTPUT" |
  python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')

UPLOAD_URL=$(echo "$DEPLOYMENT_OUTPUT" |
  python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')

echo "Amplify Job ID: $JOB_ID"

curl -sS \
  --upload-file app/opsai-assistant-ui.zip \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

echo
echo "Waiting for Amplify deployment..."

while true
do
    STATUS=$(aws amplify get-job \
      --app-id "$AMPLIFY_APP_ID" \
      --branch-name "$AMPLIFY_BRANCH_NAME" \
      --job-id "$JOB_ID" \
      --region "$AWS_REGION" \
      --query 'job.summary.status' \
      --output text)

    echo "Deployment status: $STATUS"

    case "$STATUS" in
        SUCCEED)
            break
            ;;
        FAILED|CANCELLED)
            echo "ERROR: Amplify deployment failed."
            exit 1
            ;;
        *)
            sleep 10
            ;;
    esac
done

echo
echo "=================================================="
echo "Infrastructure Assets UI deployed successfully."
echo "=================================================="
echo
echo "OpsAI URL:"
echo "$AMPLIFY_URL"
echo
echo "Open the URL and press Ctrl + F5."


Steps
Option for HR Portal

Upload One File

In AWS CloudShell, click:

Actions
→ Upload file

Upload:

install_opsai_hr_portal_final.sh

Confirm:

cd ~

ls -lh ~/install_opsai_hr_portal_final.sh
Step 2 — Run the Final Installation
chmod +x ~/install_opsai_hr_portal_final.sh

bash ~/install_opsai_hr_portal_final.sh


below is the script

#!/usr/bin/env bash
set -Eeuo pipefail
export AWS_PAGER=""

PROJECT_DIR="$HOME/opsai-assistant-aws"
WEB_DIR="$PROJECT_DIR/app/web"
ENV_FILE="$PROJECT_DIR/.env"

cd "$PROJECT_DIR"

if [[ ! -f "$ENV_FILE" ]]; then
  echo "ERROR: $ENV_FILE was not found."
  exit 1
fi

set -a
source "$ENV_FILE"
set +a

AWS_REGION="${AWS_REGION:-us-east-1}"
AMPLIFY_BRANCH_NAME="${AMPLIFY_BRANCH_NAME:-main}"

if [[ ! -f "$WEB_DIR/index.html" ]]; then
  echo "ERROR: $WEB_DIR/index.html was not found."
  exit 1
fi

if [[ -z "${AMPLIFY_APP_ID:-}" ]]; then
  AMPLIFY_APP_ID="$(aws amplify list-apps \
    --region "$AWS_REGION" \
    --query "apps[?name=='opsai-assistant-ui'].appId | [0]" \
    --output text)"
fi

if [[ -z "$AMPLIFY_APP_ID" || "$AMPLIFY_APP_ID" == "None" ]]; then
  echo "ERROR: Unable to find the Amplify app ID."
  exit 1
fi

DEFAULT_DOMAIN="$(aws amplify get-app \
  --app-id "$AMPLIFY_APP_ID" \
  --region "$AWS_REGION" \
  --query 'app.defaultDomain' \
  --output text)"

AMPLIFY_URL="${AMPLIFY_URL:-https://${AMPLIFY_BRANCH_NAME}.${DEFAULT_DOMAIN}}"

BACKUP_TIME="$(date +%Y%m%d-%H%M%S)"
BACKUP_DIR="$PROJECT_DIR/backups/hr-portal-$BACKUP_TIME"
mkdir -p "$BACKUP_DIR"

cp "$WEB_DIR/index.html" "$BACKUP_DIR/index.html"

if [[ -f "$WEB_DIR/hr-portal.js" ]]; then
  cp "$WEB_DIR/hr-portal.js" "$BACKUP_DIR/hr-portal.js"
fi

if [[ -f "$WEB_DIR/hr-portal.css" ]]; then
  cp "$WEB_DIR/hr-portal.css" "$BACKUP_DIR/hr-portal.css"
fi

echo "Existing HR Portal backup: $BACKUP_DIR"

# -------------------------------------------------------------------
# Remove the old HR Portal references and files.
# -------------------------------------------------------------------
python3 - <<'PY'
from pathlib import Path
import re

index_file = Path.home() / "opsai-assistant-aws" / "app" / "web" / "index.html"
content = index_file.read_text(encoding="utf-8")

content = re.sub(
    r'\s*<link[^>]+href=["\'][^"\']*hr-portal\.css[^"\']*["\'][^>]*>\s*',
    '\n',
    content,
    flags=re.IGNORECASE
)

content = re.sub(
    r'\s*<script[^>]+src=["\'][^"\']*hr-portal\.js[^"\']*["\'][^>]*>\s*</script>\s*',
    '\n',
    content,
    flags=re.IGNORECASE
)

index_file.write_text(content, encoding="utf-8")
print("Old HR Portal references removed from index.html.")
PY

rm -f "$WEB_DIR/hr-portal.js" "$WEB_DIR/hr-portal.css"

# -------------------------------------------------------------------
# Create the final HR Portal stylesheet.
# -------------------------------------------------------------------
cat > "$WEB_DIR/hr-portal.css" <<'CSSEOF'
/* OpsAI Internal HR Portal - Final */

.hr-panel {
  width: 100%;
  max-width: 1320px;
  margin: 0 auto;
  padding: 24px;
  overflow-y: auto;
}

.hr-portal-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 18px;
}

.hr-portal-header h2,
.hr-section-header h3,
.hr-job-title,
.hr-profile-card h4,
.hr-manager-card h4 {
  margin: 0;
  color: #0f172a;
}

.hr-portal-header p,
.hr-section-header p,
.hr-profile-card p,
.hr-manager-card p {
  color: #64748b;
}

.hr-portal-header p,
.hr-section-header p {
  margin: 5px 0 0;
}

.hr-header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.hr-role-switch {
  display: inline-flex;
  gap: 6px;
  padding: 5px;
  border: 1px solid #dbe3ef;
  border-radius: 12px;
  background: #ffffff;
}

.hr-role-button,
.hr-reset-button {
  border-radius: 8px;
  padding: 9px 14px;
  font-weight: 700;
  cursor: pointer;
}

.hr-role-button {
  border: 0;
  background: transparent;
  color: #475569;
}

.hr-role-button.active {
  background: #2563eb;
  color: #ffffff;
}

.hr-reset-button {
  border: 1px solid #cbd5e1;
  background: #ffffff;
  color: #334155;
}

.hr-summary-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12px;
  margin-bottom: 18px;
}

.hr-summary-card,
.hr-profile-card,
.hr-manager-card,
.hr-job-card {
  border: 1px solid #dbe3ef;
  border-radius: 12px;
  background: #ffffff;
}

.hr-summary-card {
  padding: 14px;
}

.hr-summary-card span,
.hr-job-meta span,
.hr-manager-project span {
  display: block;
  color: #64748b;
  font-size: 11px;
}

.hr-summary-card strong {
  display: block;
  margin-top: 7px;
  color: #0f172a;
  font-size: 25px;
}

.hr-section {
  margin-top: 18px;
  border: 1px solid #dbe3ef;
  border-radius: 14px;
  background: #ffffff;
  overflow: hidden;
}

.hr-section-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  padding: 16px 18px;
  border-bottom: 1px solid #e7edf5;
}

.hr-search-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 10px;
  padding: 16px 18px;
  border-bottom: 1px solid #e7edf5;
  background: #f8fafc;
}

.hr-field label {
  display: block;
  margin-bottom: 5px;
  color: #475569;
  font-size: 12px;
  font-weight: 700;
}

.hr-field input,
.hr-field select,
.hr-stage-select {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #cbd5e1;
  border-radius: 9px;
  padding: 10px 11px;
  background: #ffffff;
  color: #0f172a;
  outline: none;
}

.hr-field input:focus,
.hr-field select:focus,
.hr-stage-select:focus {
  border-color: #2563eb;
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.12);
}

.hr-job-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
  padding: 18px;
}

.hr-job-card {
  display: flex;
  flex-direction: column;
  padding: 16px;
}

.hr-job-card:hover {
  border-color: #93b4f5;
  box-shadow: 0 8px 22px rgba(15, 23, 42, 0.07);
}

.hr-job-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 10px;
}

.hr-job-id {
  margin: 0 0 5px;
  color: #2563eb;
  font-size: 12px;
  font-weight: 800;
}

.hr-job-title {
  font-size: 16px;
}

.hr-job-meta {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 9px;
  margin: 14px 0;
}

.hr-job-meta div,
.hr-manager-project {
  border-radius: 9px;
  padding: 9px;
  background: #f8fafc;
}

.hr-job-meta strong,
.hr-manager-project strong {
  display: block;
  margin-top: 3px;
  color: #1e293b;
  font-size: 13px;
}

.hr-job-description {
  color: #475569;
  line-height: 1.5;
  font-size: 13px;
}

.hr-skill-list {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin: 12px 0;
}

.hr-skill {
  border-radius: 999px;
  padding: 5px 8px;
  background: #eff6ff;
  color: #1d4ed8;
  font-size: 11px;
  font-weight: 700;
}

.hr-contact {
  margin-top: auto;
  padding-top: 12px;
  border-top: 1px solid #edf1f7;
  color: #64748b;
  font-size: 12px;
  line-height: 1.55;
}

.hr-card-actions,
.hr-table-actions,
.hr-quick-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 10px;
}

.hr-primary-button,
.hr-secondary-button,
.hr-success-button,
.hr-danger-button,
.hr-warning-button,
.hr-neutral-button {
  border-radius: 9px;
  padding: 9px 12px;
  font-weight: 800;
  cursor: pointer;
}

.hr-primary-button {
  flex: 1;
  border: 0;
  background: #2563eb;
  color: #ffffff;
}

.hr-primary-button:disabled {
  background: #cbd5e1;
  color: #64748b;
  cursor: default;
}

.hr-secondary-button,
.hr-neutral-button {
  border: 1px solid #cbd5e1;
  background: #ffffff;
  color: #334155;
}

.hr-success-button {
  border: 0;
  background: #16a34a;
  color: #ffffff;
}

.hr-danger-button {
  border: 1px solid #fecaca;
  background: #fff1f2;
  color: #b91c1c;
}

.hr-warning-button {
  border: 1px solid #fde68a;
  background: #fffbeb;
  color: #92400e;
}

.hr-profile-grid,
.hr-manager-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
  padding: 18px;
}

.hr-profile-card,
.hr-manager-card {
  padding: 15px;
}

.hr-profile-card p,
.hr-manager-card p {
  margin: 6px 0 0;
  font-size: 13px;
  line-height: 1.5;
}

.hr-table-wrap {
  overflow-x: auto;
  padding: 0 18px 18px;
}

.hr-table {
  width: 100%;
  min-width: 1050px;
  border-collapse: collapse;
}

.hr-table th,
.hr-table td {
  padding: 12px 10px;
  border-bottom: 1px solid #e7edf5;
  text-align: left;
  vertical-align: top;
  font-size: 12px;
}

.hr-table th {
  color: #475569;
  background: #f8fafc;
  font-weight: 800;
}

.hr-table td {
  color: #1e293b;
}

.hr-empty {
  padding: 40px 20px;
  color: #64748b;
  text-align: center;
}

.hr-placeholder-note {
  margin: 0 18px 18px;
  border-radius: 10px;
  padding: 10px 12px;
  background: #f8fafc;
  color: #64748b;
  font-size: 12px;
}

.hr-status {
  display: inline-flex;
  align-items: center;
  border-radius: 999px;
  padding: 5px 9px;
  font-size: 10px;
  font-weight: 800;
  white-space: nowrap;
}

.hr-status.open,
.hr-status.applied,
.hr-status.shortlisted,
.hr-status.selected,
.hr-status.documentsverification {
  background: #dcfce7;
  color: #166534;
}

.hr-status.urgent,
.hr-status.rejected {
  background: #fee2e2;
  color: #991b1b;
}

.hr-status.closingsoon,
.hr-status.offerreview,
.hr-status.offerreleasesoon {
  background: #fef3c7;
  color: #92400e;
}

.hr-status.hrscreening,
.hr-status.hrinterviewscheduled,
.hr-status.technicalround1,
.hr-status.nexttechnicalround,
.hr-status.technicalround2,
.hr-status.managerround,
.hr-status.clientround {
  background: #dbeafe;
  color: #1d4ed8;
}

.hr-status.offerreleased,
.hr-status.joined {
  background: #ccfbf1;
  color: #0f766e;
}

.hr-status.onhold {
  background: #ede9fe;
  color: #6d28d9;
}

.hr-candidate-table {
  min-width: 1580px;
}

.hr-candidate-table .hr-action-column {
  position: sticky;
  right: 0;
  z-index: 2;
  min-width: 330px;
  background: #ffffff;
  box-shadow: -5px 0 10px rgba(15, 23, 42, 0.07);
}

.hr-candidate-table thead .hr-action-column {
  z-index: 4;
  background: #f8fafc;
}

.hr-stage-control {
  display: grid;
  gap: 8px;
  min-width: 305px;
}

.hr-stage-control label {
  color: #475569;
  font-size: 11px;
  font-weight: 800;
}

.hr-stage-select {
  display: block;
  margin-top: 5px;
  padding: 8px;
  font-size: 12px;
}

.hr-stage-update-button {
  width: 100%;
  padding: 8px 10px;
}

.hr-quick-actions {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  margin-top: 0;
}

.hr-quick-actions button {
  padding: 7px 4px;
  font-size: 9px;
}

.hr-no-action {
  color: #64748b;
  font-size: 12px;
}

@media (max-width: 950px) {
  .hr-summary-grid,
  .hr-profile-grid,
  .hr-manager-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .hr-search-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .hr-job-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 650px) {
  .hr-panel {
    padding: 16px 12px;
  }

  .hr-portal-header,
  .hr-header-actions {
    flex-direction: column;
  }

  .hr-summary-grid,
  .hr-profile-grid,
  .hr-manager-grid,
  .hr-search-grid {
    grid-template-columns: 1fr;
  }

  .hr-role-switch {
    width: 100%;
  }

  .hr-role-button {
    flex: 1;
  }
}
CSSEOF

# -------------------------------------------------------------------
# Create the final HR Portal JavaScript.
# -------------------------------------------------------------------
cat > "$WEB_DIR/hr-portal.js" <<'JSEOF'
(() => {
  const PROFILE_KEY = 'opsai-hr-final-profile-v1';
  const APPLICATIONS_KEY = 'opsai-hr-final-applications-v1';

  const stages = [
    'APPLIED',
    'HR SCREENING',
    'HR INTERVIEW SCHEDULED',
    'SHORTLISTED',
    'TECHNICAL ROUND 1',
    'NEXT TECHNICAL ROUND',
    'TECHNICAL ROUND 2',
    'MANAGER ROUND',
    'CLIENT ROUND',
    'SELECTED',
    'DOCUMENTS VERIFICATION',
    'OFFER REVIEW',
    'OFFER RELEASE SOON',
    'OFFER RELEASED',
    'JOINED',
    'ON HOLD',
    'REJECTED'
  ];

  const managers = [
    ['MGR-1001','Ananya Sharma','Hiring Manager - Cloud and Infrastructure','Hyderabad','Cloud Platform Modernization','ananya.sharma@cgi.example','+91 98480 01X01'],
    ['MGR-1002','Vikram Rao','Program Manager - DevSecOps','Bangalore','DevSecOps Automation','vikram.rao@cgi.example','+91 98480 01X02'],
    ['MGR-1003','Priya Menon','Delivery Manager - Quality Engineering','Hyderabad','Quality Engineering Transformation','priya.menon@cgi.example','+91 98480 01X03'],
    ['MGR-1004','Arjun Nair','Engineering Manager - Application Development','Chennai','Digital Banking APIs','arjun.nair@cgi.example','+91 98480 01X04'],
    ['MGR-1005','Meera Iyer','Data and AI Manager','Bangalore','AI Operations Assistant','meera.iyer@cgi.example','+91 98480 01X05'],
    ['MGR-1006','Sandeep Varma','Infrastructure Operations Manager','Bangalore','Enterprise Infrastructure Operations','sandeep.varma@cgi.example','+91 98480 01X06'],
    ['MGR-1007','Kavya Reddy','Delivery Manager - Cloud Security','Hyderabad','Cloud Security and Compliance','kavya.reddy@cgi.example','+91 98480 01X07'],
    ['MGR-1008','Rahul Khanna','Program Manager - Data Platforms','Bangalore','Enterprise Data Platform','rahul.khanna@cgi.example','+91 98480 01X08'],
    ['MGR-1009','Nisha Thomas','Delivery Manager - ERP and Business Apps','Chennai','Business Applications Modernization','nisha.thomas@cgi.example','+91 98480 01X09'],
    ['MGR-1010','Rohit Kulkarni','Engineering Manager - Platform Reliability','Pune','SRE and Observability','rohit.kulkarni@cgi.example','+91 98480 01X10']
  ].map(item => ({
    id: item[0],
    name: item[1],
    role: item[2],
    city: item[3],
    project: item[4],
    email: item[5],
    phone: item[6]
  }));

  const jobs = [
    ['J0526-1316','Senior Technical Architect / DevOps Engineer','Architecture','Bangalore','India','10-15 years',2,'Urgent','Cloud Platform Modernization','MGR-1001',['AWS','Kubernetes','Terraform','DevOps','Linux','Architecture']],
    ['J0526-1317','Senior Technical Architect / DevOps Engineer','Architecture','Bangalore','India','8-12 years',2,'Open','DevSecOps Automation','MGR-1002',['Jenkins','CI/CD','Kubernetes','Ansible','Linux','Security']],
    ['J0726-1019','OpenShift/Kubernetes, Unix Scripting, SQL, AWS','Software Development / Engineering','Hyderabad','India','4-7 years',3,'Urgent','Kubernetes Reliability','MGR-1001',['OpenShift','Kubernetes','AWS','Unix','Linux','SQL']],
    ['J0726-0706','AI Engineer','Software Development / Engineering','Bangalore','India','3-6 years',2,'Open','AI Operations Assistant','MGR-1005',['Python','RAG','LLM','APIs','AWS']],
    ['J0726-1244','QA Engineer - TOSCA','Testing / Quality Assurance','Hyderabad','India','3-5 years',2,'Open','Quality Engineering Transformation','MGR-1003',['TOSCA','Automation','API Testing','Agile']],
    ['J0526-0505','Senior Software Engineer - JAVA - Sr API Developer','Software Development / Engineering','Hyderabad','India','6-9 years',3,'Closing Soon','Digital Banking APIs','MGR-1004',['Java','Spring Boot','REST APIs','Microservices']],
    ['J0726-0998','Software Engineer','Software Development / Engineering','Hyderabad','India','2-5 years',4,'Open','Enterprise Application Services','MGR-1004',['Java','Python','SQL','Git']],
    ['J0426-2104','Backend Developers (6-10 Years)','Software Development / Engineering','Bangalore','India','6-10 years',5,'Open','Digital Platform Engineering','MGR-1004',['Backend','Java','APIs','Databases','Microservices']],
    ['J0426-2088','Frontend Developers (5-8 Years)','Software Development / Engineering','Bangalore','India','5-8 years',4,'Open','Digital Platform Engineering','MGR-1004',['React','Angular','JavaScript','HTML','CSS']],
    ['J0726-1123','AIX Administrator','Infrastructure / Cloud','Bangalore','India','4-7 years',1,'Closing Soon','Enterprise Infrastructure Operations','MGR-1006',['AIX','Unix','Linux','Patching','Monitoring','Shell']],
    ['J0626-0710','Senior Software Engineer - GCP and Data Engineer','Software Development / Engineering','Bangalore','India','6-10 years',2,'Open','Enterprise Data Platform','MGR-1008',['GCP','Data Engineering','Python','SQL','ETL']],
    ['J0726-1028','Appian Developer','Software Development / Engineering','Chennai','India','3-6 years',2,'Open','Business Applications Modernization','MGR-1009',['Appian','Low Code','APIs','SQL','Workflow']],
    ['J0426-1288','Control-M System Administrator','ERP / CRM / Tools','Bangalore','India','5-8 years',2,'Open','Enterprise Scheduling Services','MGR-1006',['Control-M','Linux','Shell','Scheduling','Automation']],
    ['J0726-1093','Solution Architect','Architecture','Hyderabad','India','10-15 years',2,'Open','Cloud Platform Modernization','MGR-1001',['Architecture','Cloud','AWS','Integration','Security']],
    ['J0726-1334','Quality Assurance Lead','Testing / Quality Assurance','Hyderabad','India','8-12 years',1,'Urgent','Quality Engineering Transformation','MGR-1003',['QA','Automation','Leadership','API Testing','Agile']],
    ['J0726-0657','AWS Aurora PostgreSQL Engineer','Infrastructure / Cloud','Bangalore','India','5-8 years',2,'Open','Cloud Database Modernization','MGR-1008',['AWS','Aurora','PostgreSQL','Linux','Performance']],
    ['J0626-0769','Senior QA Automation Consultant - Playwright and Google Cloud','Testing / Quality Assurance','Bangalore','India','6-10 years',2,'Open','Quality Engineering Transformation','MGR-1003',['Playwright','GCP','Automation','JavaScript','API Testing']],
    ['J0726-1196','Scrum Master','Project Management','Hyderabad','India','6-10 years',2,'Open','Enterprise Application Services','MGR-1004',['Scrum','Agile','Jira','Delivery','Leadership']],
    ['J0526-1061','Cloud Solutions Architect','Architecture','Pune','India','10-15 years',1,'Open','SRE and Observability','MGR-1010',['Cloud','AWS','Azure','SRE','Observability']],
    ['J0726-0778','Senior Cybersecurity and IAM Specialist','Infrastructure / Cloud','Hyderabad','India','8-12 years',2,'Urgent','Cloud Security and Compliance','MGR-1007',['Cybersecurity','IAM','Active Directory','Cloud','Security']]
  ].map(item => ({
    id: item[0],
    title: item[1],
    category: item[2],
    city: item[3],
    country: item[4],
    experience: item[5],
    openings: item[6],
    status: item[7],
    project: item[8],
    managerId: item[9],
    skills: item[10],
    description: `Internal opportunity supporting ${item[8]}.`
  }));

  const candidateSource = [
    ['Aarav Reddy','Cloud Engineer',4,'Hyderabad',['AWS','Kubernetes','Linux','Terraform']],
    ['Neha Kapoor','Senior DevOps Engineer',10,'Bangalore',['DevOps','Jenkins','Kubernetes','Ansible','AWS']],
    ['Kiran Patel','Java API Developer',7,'Hyderabad',['Java','Spring Boot','REST APIs','Microservices']],
    ['Divya Reddy','QA Automation Engineer',5,'Hyderabad',['TOSCA','Playwright','API Testing','Automation']],
    ['Rohan Mehta','Linux Administrator',6,'Bangalore',['Linux','Shell','Patching','Monitoring','Ansible']],
    ['Sneha Iyer','Data Engineer',6,'Bangalore',['Python','SQL','ETL','GCP','Data Engineering']],
    ['Aditya Nair','Kubernetes Engineer',5,'Hyderabad',['Kubernetes','OpenShift','AWS','Linux','Helm']],
    ['Pooja Sharma','Frontend Developer',6,'Bangalore',['React','Angular','JavaScript','HTML','CSS']],
    ['Manish Verma','Backend Developer',8,'Bangalore',['Java','APIs','Databases','Microservices','Spring Boot']],
    ['Lakshmi Rao','AI Engineer',4,'Bangalore',['Python','RAG','LLM','APIs','AWS']],
    ['Harish Kumar','AIX Administrator',6,'Bangalore',['AIX','Unix','Linux','Patching','Shell']],
    ['Meghana Das','Cloud Security Engineer',7,'Hyderabad',['Security','IAM','AWS','Active Directory','Cloud']],
    ['Arun Joseph','SRE Engineer',8,'Pune',['SRE','Observability','Prometheus','Grafana','Linux']],
    ['Keerthi Menon','Scrum Master',7,'Hyderabad',['Scrum','Agile','Jira','Delivery','Leadership']],
    ['Vivek Singh','Database Engineer',6,'Bangalore',['PostgreSQL','Aurora','AWS','Linux','Performance']],
    ['Nandini Rao','Appian Developer',4,'Chennai',['Appian','Low Code','SQL','Workflow','APIs']],
    ['Suresh Babu','Control-M Administrator',7,'Bangalore',['Control-M','Linux','Shell','Scheduling','Automation']],
    ['Ishita Jain','QA Lead',10,'Hyderabad',['QA','Automation','Leadership','Agile','API Testing']],
    ['Naveen Reddy','Solution Architect',12,'Hyderabad',['Architecture','Cloud','AWS','Security','Integration']],
    ['Asha Kulkarni','Cloud Architect',11,'Pune',['Cloud','AWS','Azure','SRE','Observability']],
    ['Rahul Das','DevOps Engineer',5,'Hyderabad',['DevOps','Jenkins','Docker','Kubernetes','Linux']],
    ['Swathi Nair','Python Developer',4,'Bangalore',['Python','APIs','SQL','AWS','Git']],
    ['Mohit Agarwal','Network and Cloud Engineer',6,'Hyderabad',['Networking','AWS','Linux','Security','Monitoring']],
    ['Anjali Thomas','Business Analyst',7,'Bangalore',['Business Analysis','Agile','Jira','SQL','Documentation']],
    ['Praveen Kumar','OpenShift Administrator',6,'Hyderabad',['OpenShift','Kubernetes','Linux','Ansible','AWS']],
    ['Ritika Shah','Java Full Stack Developer',7,'Chennai',['Java','Spring Boot','Angular','APIs','SQL']],
    ['Deepak Menon','Infrastructure Lead',11,'Bangalore',['Linux','AIX','Cloud','Patching','Leadership']],
    ['Shreya Gupta','GCP Data Engineer',5,'Bangalore',['GCP','Python','SQL','ETL','BigQuery']],
    ['Varun Reddy','Cloud Database Engineer',7,'Bangalore',['AWS','Aurora','PostgreSQL','Linux','Automation']],
    ['Kavita Rao','Cybersecurity Analyst',5,'Hyderabad',['Security','IAM','SIEM','Active Directory','Cloud']],
    ['Abhishek Jain','Platform Engineer',8,'Pune',['Kubernetes','Terraform','AWS','Linux','GitOps']],
    ['Madhuri Iyer','Test Automation Lead',9,'Hyderabad',['Automation','Playwright','TOSCA','API Testing','Leadership']],
    ['Sunil Nair','Unix Engineer',6,'Bangalore',['Unix','Linux','Shell','Patching','Monitoring']],
    ['Bhavana Reddy','UI Developer',5,'Bangalore',['React','JavaScript','HTML','CSS','Accessibility']],
    ['Gaurav Sharma','Microservices Developer',8,'Hyderabad',['Java','Spring Boot','Microservices','Kafka','APIs']],
    ['Nisha Patel','Data Analyst',4,'Bangalore',['SQL','Python','Power BI','Data Analysis','ETL']],
    ['Rajesh Verma','DevSecOps Engineer',9,'Bangalore',['DevSecOps','Jenkins','Kubernetes','Security','Ansible']],
    ['Priyanka Das','Cloud Support Engineer',4,'Hyderabad',['AWS','Linux','Monitoring','Networking','ITIL']],
    ['Amit Kulkarni','Observability Engineer',7,'Pune',['Prometheus','Grafana','SRE','Linux','Cloud']],
    ['Sonal Thomas','API Test Engineer',5,'Hyderabad',['API Testing','Postman','Automation','SQL','Agile']]
  ];

  const candidates = candidateSource.map((item, index) => ({
    employeeId: `EMP-SAMPLE-${String(1001 + index)}`,
    name: item[0],
    currentRole: item[1],
    experience: `${item[2]} years`,
    years: item[2],
    city: item[3],
    skills: item[4],
    email: `${item[0].toLowerCase().replace(/\s+/g, '.')}@cgi.example`,
    phone: `+91 98480 02X${String(index + 1).padStart(2, '0')}`
  }));

  const initialStageCycle = [
    'APPLIED',
    'HR SCREENING',
    'HR INTERVIEW SCHEDULED',
    'SHORTLISTED',
    'TECHNICAL ROUND 1',
    'NEXT TECHNICAL ROUND',
    'TECHNICAL ROUND 2',
    'MANAGER ROUND',
    'CLIENT ROUND',
    'SELECTED',
    'DOCUMENTS VERIFICATION',
    'OFFER REVIEW',
    'OFFER RELEASE SOON',
    'OFFER RELEASED',
    'JOINED',
    'ON HOLD',
    'REJECTED'
  ];

  const initialApplications = candidates.map((candidate, index) => ({
    applicationId: `APP-${String(5001 + index)}`,
    candidate: candidate.name,
    employeeId: candidate.employeeId,
    email: candidate.email,
    experience: candidate.experience,
    currentRole: candidate.currentRole,
    city: candidate.city,
    skills: candidate.skills,
    jobId: jobs[index % jobs.length].id,
    appliedOn: `2026-07-${String(1 + (index % 15)).padStart(2, '0')}`,
    status: initialStageCycle[index % initialStageCycle.length],
    stageUpdatedOn: null
  }));

  const defaultProfile = candidates[0];

  let profile = read(PROFILE_KEY, defaultProfile);
  let applications = read(APPLICATIONS_KEY, initialApplications);
  let activeRole = 'candidate';
  let attempts = 0;

  function read(key, fallback) {
    try {
      const value = JSON.parse(localStorage.getItem(key));
      return value || structuredClone(fallback);
    } catch {
      return structuredClone(fallback);
    }
  }

  function save() {
    localStorage.setItem(PROFILE_KEY, JSON.stringify(profile));
    localStorage.setItem(APPLICATIONS_KEY, JSON.stringify(applications));
  }

  function resetData() {
    localStorage.removeItem(PROFILE_KEY);
    localStorage.removeItem(APPLICATIONS_KEY);
    profile = structuredClone(defaultProfile);
    applications = structuredClone(initialApplications);
    save();
  }

  function esc(value) {
    return String(value ?? '').replace(/[&<>"']/g, char => ({
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      '"': '&quot;',
      "'": '&#039;'
    }[char]));
  }

  function normalize(value) {
    return String(value || '').toLowerCase().replace(/[^a-z0-9]/g, '');
  }

  function statusClass(value) {
    return normalize(value);
  }

  function managerFor(job) {
    return managers.find(manager => manager.id === job.managerId);
  }

  function applicationForCandidate(employeeId) {
    return [...applications].reverse().find(item => item.employeeId === employeeId);
  }

  function applicationForJob(jobId) {
    return applications.find(item =>
      item.jobId === jobId && item.employeeId === profile.employeeId
    );
  }

  function optionList(values) {
    return [...new Set(values)]
      .sort()
      .map(value => `<option>${esc(value)}</option>`)
      .join('');
  }

  function summaryCard(label, value) {
    return `
      <div class="hr-summary-card">
        <span>${esc(label)}</span>
        <strong>${esc(value)}</strong>
      </div>
    `;
  }

  function profileItem(label, value) {
    return `
      <div class="hr-profile-card">
        <h4>${esc(label)}</h4>
        <p>${esc(value)}</p>
      </div>
    `;
  }

  function init() {
    attempts += 1;

    const sidebar = document.getElementById('sidebar');
    const newChat = document.getElementById('new-chat-button');
    const messages = document.getElementById('messages');
    const composer = document.querySelector('.composer-area');
    const chatArea = document.querySelector('.chat-area');
    const title = document.querySelector('.top-header h2');
    const subtitle = document.querySelector('.top-header p');

    if (!sidebar || !newChat || !messages || !composer || !chatArea) {
      if (attempts < 60) setTimeout(init, 100);
      return;
    }

    let nav = document.querySelector('.demo-nav');

    if (!nav) {
      nav = document.createElement('div');
      nav.className = 'demo-nav';
      newChat.insertAdjacentElement('afterend', nav);
    }

    let hrButton = document.getElementById('hr-portal-nav-button');

    if (!hrButton) {
      hrButton = document.createElement('button');
      hrButton.id = 'hr-portal-nav-button';
      hrButton.className = 'demo-nav-button';
      hrButton.dataset.view = 'hr';
      hrButton.textContent = '▣ HR Portal';
      nav.appendChild(hrButton);
    }

    let hrPanel = document.getElementById('hr-portal-panel');

    if (!hrPanel) {
      hrPanel = document.createElement('section');
      hrPanel.id = 'hr-portal-panel';
      hrPanel.className = 'demo-panel hr-panel demo-hidden';
      chatArea.insertBefore(hrPanel, composer);
    }

    function hideHR() {
      hrPanel.classList.add('demo-hidden');
    }

    function showHR(role = activeRole) {
      activeRole = role;

      messages.style.display = 'none';
      composer.style.display = 'none';

      document.querySelectorAll('.demo-panel').forEach(panel => {
        if (panel !== hrPanel) panel.classList.add('demo-hidden');
      });

      hrPanel.classList.remove('demo-hidden');

      nav.querySelectorAll('button').forEach(button => {
        button.classList.toggle('active', button === hrButton);
      });

      if (title) title.textContent = 'Internal HR Portal';
      if (subtitle) {
        subtitle.textContent = activeRole === 'candidate'
          ? 'Search internal opportunities and track applications'
          : 'Search candidates and manage every hiring stage';
      }

      renderPortal(hrPanel);
      sidebar.classList.remove('open');
    }

    hrButton.addEventListener('click', () => showHR(activeRole));

    nav.addEventListener('click', event => {
      const button = event.target.closest('button');
      if (button && button !== hrButton) hideHR();
    });

    newChat.addEventListener('click', hideHR);

    hrPanel.addEventListener('click', event => {
      const roleButton = event.target.closest('[data-hr-role]');
      const resetButton = event.target.closest('[data-reset-hr]');
      const applyButton = event.target.closest('[data-apply-job]');
      const stageButton = event.target.closest('[data-update-stage]');
      const quickButton = event.target.closest('[data-quick-stage]');

      if (roleButton) {
        activeRole = roleButton.dataset.hrRole;
        renderPortal(hrPanel);
        return;
      }

      if (resetButton) {
        resetData();
        renderPortal(hrPanel);
        return;
      }

      if (applyButton) {
        applyToJob(applyButton.dataset.applyJob);
        renderPortal(hrPanel);
        return;
      }

      if (stageButton) {
        const applicationId = stageButton.dataset.updateStage;
        const select = hrPanel.querySelector(
          `[data-stage-select="${applicationId}"]`
        );

        if (select) updateApplication(applicationId, select.value);
        renderPortal(hrPanel);
        return;
      }

      if (quickButton) {
        updateApplication(
          quickButton.dataset.applicationId,
          quickButton.dataset.quickStage
        );
        renderPortal(hrPanel);
      }
    });

    hrPanel.addEventListener('input', event => {
      const id = event.target.id;

      if (['candidate-job-keyword','candidate-job-city','candidate-job-experience','candidate-job-category'].includes(id)) {
        renderCandidateJobs(hrPanel);
      }

      if (['manager-job-keyword','manager-job-city','manager-job-status','manager-job-manager'].includes(id)) {
        renderManagerJobs(hrPanel);
      }

      if (['manager-candidate-keyword','manager-candidate-city','manager-candidate-experience','manager-candidate-status'].includes(id)) {
        renderCandidatePool(hrPanel);
      }
    });

    hrPanel.addEventListener('change', event => {
      event.target.dispatchEvent(new Event('input', { bubbles: true }));
    });

    save();
  }

  function renderPortal(panel) {
    panel.innerHTML = `
      <div class="hr-portal-header">
        <div>
          <h2>Internal HR Portal</h2>
          <p>${activeRole === 'candidate'
            ? 'Search opportunities, apply internally and track application progress.'
            : 'Search requisitions, find candidates and manage the complete hiring workflow.'
          }</p>
        </div>

        <div class="hr-header-actions">
          <div class="hr-role-switch">
            <button
              class="hr-role-button ${activeRole === 'candidate' ? 'active' : ''}"
              data-hr-role="candidate"
            >Candidate View</button>

            <button
              class="hr-role-button ${activeRole === 'manager' ? 'active' : ''}"
              data-hr-role="manager"
            >Manager View</button>
          </div>

          <button class="hr-reset-button" data-reset-hr>
            Reset HR Data
          </button>
        </div>
      </div>

      ${activeRole === 'candidate' ? candidateView() : managerView()}
    `;

    if (activeRole === 'candidate') {
      renderCandidateJobs(panel);
    } else {
      renderManagerJobs(panel);
      renderCandidatePool(panel);
    }
  }

  function candidateView() {
    const myApplications = applications.filter(
      item => item.employeeId === profile.employeeId
    );

    return `
      <div class="hr-summary-grid">
        ${summaryCard('Open requisitions', jobs.length)}
        ${summaryCard('Total openings', jobs.reduce((sum, job) => sum + job.openings, 0))}
        ${summaryCard('My applications', myApplications.length)}
        ${summaryCard('Candidate profiles', candidates.length)}
      </div>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>My Candidate Profile</h3>
            <p>Profile used for the internal opportunity workflow.</p>
          </div>
        </div>

        <div class="hr-profile-grid">
          ${profileItem('Employee ID', profile.employeeId)}
          ${profileItem('Name', profile.name)}
          ${profileItem('Current role', profile.currentRole)}
          ${profileItem('Experience', profile.experience)}
          ${profileItem('Preferred city', profile.city)}
          ${profileItem('Skills', profile.skills.join(', '))}
        </div>
      </section>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>Current Opportunities</h3>
            <p>Search by job ID, tool, technology, location, experience or category.</p>
          </div>
        </div>

        <div class="hr-search-grid">
          <div class="hr-field">
            <label for="candidate-job-keyword">Tool / Skill / Job ID</label>
            <input id="candidate-job-keyword" type="search"
              placeholder="DevOps, cloud, Linux, AWS, Java, Kubernetes...">
          </div>

          <div class="hr-field">
            <label for="candidate-job-city">City</label>
            <select id="candidate-job-city">
              <option value="">All cities</option>
              ${optionList(jobs.map(job => job.city))}
            </select>
          </div>

          <div class="hr-field">
            <label for="candidate-job-experience">Experience</label>
            <select id="candidate-job-experience">
              <option value="">All experience</option>
              <option value="2-5">2-5 years</option>
              <option value="4-7">4-7 years</option>
              <option value="6-10">6-10 years</option>
              <option value="8-12">8-12 years</option>
              <option value="10-15">10-15 years</option>
            </select>
          </div>

          <div class="hr-field">
            <label for="candidate-job-category">Category</label>
            <select id="candidate-job-category">
              <option value="">All categories</option>
              ${optionList(jobs.map(job => job.category))}
            </select>
          </div>
        </div>

        <div id="candidate-job-results" class="hr-job-grid"></div>
      </section>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>My Applications</h3>
            <p>Current application progress for ${esc(profile.name)}.</p>
          </div>
        </div>

        ${candidateApplications(myApplications)}
      </section>

      <p class="hr-placeholder-note">
        All names, email aliases and phone values are fictional sample records.
        Replace them only with approved internal directory information.
      </p>
    `;
  }

  function managerView() {
    return `
      <div class="hr-summary-grid">
        ${summaryCard('Active requisitions', jobs.length)}
        ${summaryCard('Hiring managers', managers.length)}
        ${summaryCard('Candidate profiles', candidates.length)}
        ${summaryCard('Applications', applications.length)}
      </div>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>Search Open Requisitions</h3>
            <p>Search by DevOps, cloud, Linux, AWS, Java, Kubernetes, project or manager.</p>
          </div>
        </div>

        <div class="hr-search-grid">
          <div class="hr-field">
            <label for="manager-job-keyword">Tool / Skill / Job ID</label>
            <input id="manager-job-keyword" type="search"
              placeholder="DevOps, cloud, Linux, AWS, Java, Kubernetes...">
          </div>

          <div class="hr-field">
            <label for="manager-job-city">Location</label>
            <select id="manager-job-city">
              <option value="">All locations</option>
              ${optionList(jobs.map(job => job.city))}
            </select>
          </div>

          <div class="hr-field">
            <label for="manager-job-status">Status</label>
            <select id="manager-job-status">
              <option value="">All statuses</option>
              <option>Open</option>
              <option>Urgent</option>
              <option>Closing Soon</option>
            </select>
          </div>

          <div class="hr-field">
            <label for="manager-job-manager">Hiring manager</label>
            <select id="manager-job-manager">
              <option value="">All managers</option>
              ${managers.map(manager =>
                `<option value="${esc(manager.id)}">${esc(manager.name)}</option>`
              ).join('')}
            </select>
          </div>
        </div>

        <div id="manager-job-results"></div>
      </section>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>Search Candidate Pool</h3>
            <p>Search 40 profiles and move every applicant through the complete hiring workflow.</p>
          </div>
        </div>

        <div class="hr-search-grid">
          <div class="hr-field">
            <label for="manager-candidate-keyword">Candidate / Skill / Role</label>
            <input id="manager-candidate-keyword" type="search"
              placeholder="DevOps, Linux, cloud, Java, QA, Kubernetes...">
          </div>

          <div class="hr-field">
            <label for="manager-candidate-city">City</label>
            <select id="manager-candidate-city">
              <option value="">All cities</option>
              ${optionList(candidates.map(candidate => candidate.city))}
            </select>
          </div>

          <div class="hr-field">
            <label for="manager-candidate-experience">Experience</label>
            <select id="manager-candidate-experience">
              <option value="">All experience</option>
              <option value="2-5">2-5 years</option>
              <option value="4-7">4-7 years</option>
              <option value="6-10">6-10 years</option>
              <option value="10-15">10-15 years</option>
            </select>
          </div>

          <div class="hr-field">
            <label for="manager-candidate-status">Hiring stage</label>
            <select id="manager-candidate-status">
              <option value="">All stages</option>
              ${stages.map(stage => `<option>${esc(stage)}</option>`).join('')}
            </select>
          </div>
        </div>

        <div id="manager-candidate-results"></div>
      </section>

      <section class="hr-section">
        <div class="hr-section-header">
          <div>
            <h3>Hiring Managers and Projects</h3>
            <p>Sample manager directory for internal hiring activity.</p>
          </div>
        </div>

        <div class="hr-manager-grid">
          ${managers.map(managerCard).join('')}
        </div>
      </section>

      <p class="hr-placeholder-note">
        These are fictional sample manager records. The cgi.example addresses
        are non-routable placeholders, and phone values are masked.
      </p>
    `;
  }

  function managerCard(manager) {
    const managerJobs = jobs.filter(job => job.managerId === manager.id);

    return `
      <article class="hr-manager-card">
        <h4>${esc(manager.name)}</h4>
        <p>${esc(manager.role)}</p>
        <p><strong>Location:</strong> ${esc(manager.city)}</p>

        <div class="hr-manager-project">
          <span>Primary project</span>
          <strong>${esc(manager.project)}</strong>
        </div>

        <p><strong>Active requisitions:</strong> ${managerJobs.length}</p>
        <p><strong>Email:</strong> ${esc(manager.email)}</p>
        <p><strong>Phone:</strong> ${esc(manager.phone)}</p>
      </article>
    `;
  }

  function renderCandidateJobs(panel) {
    const target = panel.querySelector('#candidate-job-results');
    if (!target) return;

    const keyword = normalize(panel.querySelector('#candidate-job-keyword')?.value);
    const city = panel.querySelector('#candidate-job-city')?.value || '';
    const experience = panel.querySelector('#candidate-job-experience')?.value || '';
    const category = panel.querySelector('#candidate-job-category')?.value || '';

    const filtered = jobs.filter(job => {
      const manager = managerFor(job);
      const text = normalize([
        job.id, job.title, job.category, job.city, job.country,
        job.experience, job.project, job.skills.join(' '),
        manager?.name, manager?.role
      ].join(' '));

      return (
        (!keyword || text.includes(keyword)) &&
        (!city || job.city === city) &&
        (!experience || job.experience.includes(experience)) &&
        (!category || job.category === category)
      );
    });

    target.innerHTML = filtered.length
      ? filtered.map(jobCard).join('')
      : '<div class="hr-empty">No matching opportunities were found.</div>';
  }

  function jobCard(job) {
    const manager = managerFor(job);
    const existing = applicationForJob(job.id);

    return `
      <article class="hr-job-card">
        <div class="hr-job-top">
          <div>
            <p class="hr-job-id">${esc(job.id)}</p>
            <h4 class="hr-job-title">${esc(job.title)}</h4>
          </div>

          <span class="hr-status ${statusClass(job.status)}">
            ${esc(job.status)}
          </span>
        </div>

        <div class="hr-job-meta">
          <div><span>Location</span><strong>${esc(job.city)}, ${esc(job.country)}</strong></div>
          <div><span>Experience</span><strong>${esc(job.experience)}</strong></div>
          <div><span>Category</span><strong>${esc(job.category)}</strong></div>
          <div><span>Openings</span><strong>${esc(job.openings)}</strong></div>
        </div>

        <p class="hr-job-description">${esc(job.description)}</p>

        <div class="hr-skill-list">
          ${job.skills.map(skill =>
            `<span class="hr-skill">${esc(skill)}</span>`
          ).join('')}
        </div>

        <div class="hr-contact">
          <strong>Project:</strong> ${esc(job.project)}<br>
          <strong>Hiring manager:</strong> ${esc(manager?.name || 'Assigned manager')}<br>
          <strong>Contact:</strong> ${esc(manager?.email || 'Not assigned')}
        </div>

        <div class="hr-card-actions">
          <button
            class="hr-primary-button"
            data-apply-job="${esc(job.id)}"
            ${existing ? 'disabled' : ''}
          >
            ${existing ? `Applied - ${esc(existing.status)}` : 'Apply Internally'}
          </button>
        </div>
      </article>
    `;
  }

  function renderManagerJobs(panel) {
    const target = panel.querySelector('#manager-job-results');
    if (!target) return;

    const keyword = normalize(panel.querySelector('#manager-job-keyword')?.value);
    const city = panel.querySelector('#manager-job-city')?.value || '';
    const status = panel.querySelector('#manager-job-status')?.value || '';
    const managerId = panel.querySelector('#manager-job-manager')?.value || '';

    const filtered = jobs.filter(job => {
      const manager = managerFor(job);
      const text = normalize([
        job.id, job.title, job.category, job.city, job.country,
        job.experience, job.project, job.skills.join(' '),
        manager?.name, manager?.role
      ].join(' '));

      return (
        (!keyword || text.includes(keyword)) &&
        (!city || job.city === city) &&
        (!status || job.status === status) &&
        (!managerId || job.managerId === managerId)
      );
    });

    target.innerHTML = managerJobTable(filtered);
  }

  function managerJobTable(items) {
    if (!items.length) {
      return '<div class="hr-empty">No matching requisitions were found.</div>';
    }

    return `
      <div class="hr-table-wrap">
        <table class="hr-table">
          <thead>
            <tr>
              <th>Job ID</th>
              <th>Title / Skills</th>
              <th>Project</th>
              <th>Location</th>
              <th>Experience</th>
              <th>Openings</th>
              <th>Applicants</th>
              <th>Manager</th>
              <th>Status</th>
            </tr>
          </thead>

          <tbody>
            ${items.map(job => {
              const manager = managerFor(job);
              const count = applications.filter(
                application => application.jobId === job.id
              ).length;

              return `
                <tr>
                  <td>${esc(job.id)}</td>
                  <td><strong>${esc(job.title)}</strong><br>${esc(job.skills.join(', '))}</td>
                  <td>${esc(job.project)}</td>
                  <td>${esc(job.city)}</td>
                  <td>${esc(job.experience)}</td>
                  <td>${esc(job.openings)}</td>
                  <td>${esc(count)}</td>
                  <td>${esc(manager?.name || 'Assigned manager')}</td>
                  <td><span class="hr-status ${statusClass(job.status)}">${esc(job.status)}</span></td>
                </tr>
              `;
            }).join('')}
          </tbody>
        </table>
      </div>
    `;
  }

  function renderCandidatePool(panel) {
    const target = panel.querySelector('#manager-candidate-results');
    if (!target) return;

    const keyword = normalize(panel.querySelector('#manager-candidate-keyword')?.value);
    const city = panel.querySelector('#manager-candidate-city')?.value || '';
    const experience = panel.querySelector('#manager-candidate-experience')?.value || '';
    const status = panel.querySelector('#manager-candidate-status')?.value || '';

    const filtered = candidates.filter(candidate => {
      const application = applicationForCandidate(candidate.employeeId);
      const text = normalize([
        candidate.employeeId, candidate.name, candidate.currentRole,
        candidate.experience, candidate.city, candidate.skills.join(' '),
        application?.status, application?.jobId
      ].join(' '));

      return (
        (!keyword || text.includes(keyword)) &&
        (!city || candidate.city === city) &&
        (!experience || experienceMatch(candidate.years, experience)) &&
        (!status || application?.status === status)
      );
    });

    target.innerHTML = candidatePoolTable(filtered);
  }

  function experienceMatch(years, selectedRange) {
    const [minimum, maximum] = selectedRange.split('-').map(Number);
    return years >= minimum && years <= maximum;
  }

  function stageOptions(currentStatus) {
    return stages.map(stage => `
      <option value="${esc(stage)}" ${stage === currentStatus ? 'selected' : ''}>
        ${esc(stage)}
      </option>
    `).join('');
  }

  function candidatePoolTable(items) {
    if (!items.length) {
      return '<div class="hr-empty">No matching candidates were found.</div>';
    }

    return `
      <div class="hr-table-wrap">
        <table class="hr-table hr-candidate-table">
          <thead>
            <tr>
              <th>Candidate</th>
              <th>Current role</th>
              <th>Experience</th>
              <th>City</th>
              <th>Skills</th>
              <th>Latest application</th>
              <th>Status</th>
              <th class="hr-action-column">Hiring Stage / Manager Action</th>
            </tr>
          </thead>

          <tbody>
            ${items.map(candidate => {
              const application = applicationForCandidate(candidate.employeeId);
              const job = jobs.find(value => value.id === application?.jobId);
              const currentStatus = application?.status || 'APPLIED';

              return `
                <tr>
                  <td>
                    <strong>${esc(candidate.name)}</strong><br>
                    ${esc(candidate.employeeId)}<br>
                    ${esc(candidate.email)}<br>
                    ${esc(candidate.phone)}
                  </td>

                  <td>${esc(candidate.currentRole)}</td>
                  <td>${esc(candidate.experience)}</td>
                  <td>${esc(candidate.city)}</td>
                  <td>${esc(candidate.skills.join(', '))}</td>

                  <td>
                    ${esc(application?.jobId || '')}<br>
                    ${esc(job?.title || 'Position')}
                  </td>

                  <td>
                    <span class="hr-status ${statusClass(currentStatus)}">
                      ${esc(currentStatus)}
                    </span>
                  </td>

                  <td class="hr-action-column">
                    <div class="hr-stage-control">
                      <label>
                        Move candidate to
                        <select
                          class="hr-stage-select"
                          data-stage-select="${esc(application.applicationId)}"
                        >
                          ${stageOptions(currentStatus)}
                        </select>
                      </label>

                      <button
                        class="hr-primary-button hr-stage-update-button"
                        data-update-stage="${esc(application.applicationId)}"
                      >Update Status</button>

                      <div class="hr-quick-actions">
                        ${quickButton(application.applicationId, 'HR INTERVIEW SCHEDULED', 'Schedule', 'hr-warning-button')}
                        ${quickButton(application.applicationId, 'NEXT TECHNICAL ROUND', 'Next Round', 'hr-neutral-button')}
                        ${quickButton(application.applicationId, 'SELECTED', 'Select', 'hr-success-button')}
                        ${quickButton(application.applicationId, 'OFFER RELEASE SOON', 'Offer Soon', 'hr-warning-button')}
                        ${quickButton(application.applicationId, 'REJECTED', 'Reject', 'hr-danger-button')}
                      </div>
                    </div>
                  </td>
                </tr>
              `;
            }).join('')}
          </tbody>
        </table>
      </div>
    `;
  }

  function quickButton(applicationId, stage, label, className) {
    return `
      <button
        class="${className}"
        data-application-id="${esc(applicationId)}"
        data-quick-stage="${esc(stage)}"
      >${esc(label)}</button>
    `;
  }

  function candidateApplications(items) {
    if (!items.length) {
      return '<div class="hr-empty">No applications submitted yet.</div>';
    }

    return `
      <div class="hr-table-wrap">
        <table class="hr-table">
          <thead>
            <tr>
              <th>Application</th>
              <th>Job ID</th>
              <th>Position</th>
              <th>Project</th>
              <th>Applied on</th>
              <th>Status</th>
            </tr>
          </thead>

          <tbody>
            ${items.map(item => {
              const job = jobs.find(value => value.id === item.jobId);

              return `
                <tr>
                  <td>${esc(item.applicationId)}</td>
                  <td>${esc(item.jobId)}</td>
                  <td>${esc(job?.title || 'Position')}</td>
                  <td>${esc(job?.project || 'Project')}</td>
                  <td>${esc(item.appliedOn)}</td>
                  <td><span class="hr-status ${statusClass(item.status)}">${esc(item.status)}</span></td>
                </tr>
              `;
            }).join('')}
          </tbody>
        </table>
      </div>
    `;
  }

  function applyToJob(jobId) {
    if (applicationForJob(jobId)) return;

    applications.push({
      applicationId: `APP-${String(6001 + applications.length)}`,
      candidate: profile.name,
      employeeId: profile.employeeId,
      email: profile.email,
      experience: profile.experience,
      currentRole: profile.currentRole,
      city: profile.city,
      skills: profile.skills,
      jobId,
      appliedOn: new Date().toISOString().slice(0, 10),
      status: 'APPLIED',
      stageUpdatedOn: null
    });

    save();
  }

  function updateApplication(applicationId, status) {
    const application = applications.find(
      item => item.applicationId === applicationId
    );

    if (!application) return;

    application.status = status;
    application.stageUpdatedOn = new Date().toISOString();
    save();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
JSEOF

# -------------------------------------------------------------------
# Add final HR Portal references with cache-busting.
# -------------------------------------------------------------------
python3 - <<'PY'
from pathlib import Path
import time

index_file = Path.home() / "opsai-assistant-aws" / "app" / "web" / "index.html"
content = index_file.read_text(encoding="utf-8")
version = str(int(time.time()))

css_tag = f'  <link rel="stylesheet" href="hr-portal.css?v={version}">'
js_tag = f'  <script src="hr-portal.js?v={version}"></script>'

if "</head>" not in content:
    raise SystemExit("ERROR: </head> was not found in index.html.")

if "</body>" not in content:
    raise SystemExit("ERROR: </body> was not found in index.html.")

content = content.replace("</head>", f"{css_tag}\n</head>", 1)
content = content.replace("</body>", f"{js_tag}\n</body>", 1)

index_file.write_text(content, encoding="utf-8")
print(f"Final HR Portal references added with version {version}.")
PY

# -------------------------------------------------------------------
# Validate the final portal.
# -------------------------------------------------------------------
node --check "$WEB_DIR/hr-portal.js"

grep -q "hr-portal.css" "$WEB_DIR/index.html"
grep -q "hr-portal.js" "$WEB_DIR/index.html"
grep -q "HR Portal" "$WEB_DIR/hr-portal.js"
grep -q "Manager View" "$WEB_DIR/hr-portal.js"
grep -q "Candidate View" "$WEB_DIR/hr-portal.js"
grep -q "NEXT TECHNICAL ROUND" "$WEB_DIR/hr-portal.js"
grep -q "CLIENT ROUND" "$WEB_DIR/hr-portal.js"
grep -q "DOCUMENTS VERIFICATION" "$WEB_DIR/hr-portal.js"
grep -q "OFFER RELEASE SOON" "$WEB_DIR/hr-portal.js"
grep -q "OFFER RELEASED" "$WEB_DIR/hr-portal.js"
grep -q "EMP-SAMPLE-1040" "$WEB_DIR/hr-portal.js"

echo "Final HR Portal validation passed."
echo "Managers: 10"
echo "Requisitions: 20"
echo "Candidates: 40"
echo "Applications: 40"
echo "Hiring stages: 17"

# -------------------------------------------------------------------
# Package and deploy to Amplify.
# -------------------------------------------------------------------
rm -f "$PROJECT_DIR/app/opsai-assistant-ui.zip"

(
  cd "$WEB_DIR"
  zip -qr ../opsai-assistant-ui.zip .
)

DEPLOYMENT_OUTPUT="$(aws amplify create-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --region "$AWS_REGION" \
  --output json)"

JOB_ID="$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["jobId"])')"

UPLOAD_URL="$(echo "$DEPLOYMENT_OUTPUT" | python3 -c \
  'import json,sys; print(json.load(sys.stdin)["zipUploadUrl"])')"

echo "Amplify deployment job: $JOB_ID"

curl -sS \
  --upload-file "$PROJECT_DIR/app/opsai-assistant-ui.zip" \
  "$UPLOAD_URL"

aws amplify start-deployment \
  --app-id "$AMPLIFY_APP_ID" \
  --branch-name "$AMPLIFY_BRANCH_NAME" \
  --job-id "$JOB_ID" \
  --region "$AWS_REGION" \
  >/dev/null

while true; do
  STATUS="$(aws amplify get-job \
    --app-id "$AMPLIFY_APP_ID" \
    --branch-name "$AMPLIFY_BRANCH_NAME" \
    --job-id "$JOB_ID" \
    --region "$AWS_REGION" \
    --query 'job.summary.status' \
    --output text)"

  echo "Deployment status: $STATUS"

  case "$STATUS" in
    SUCCEED)
      break
      ;;
    FAILED|CANCELLED)
      echo "ERROR: Amplify deployment failed."
      exit 1
      ;;
    *)
      sleep 10
      ;;
  esac
done

echo
echo "========================================================"
echo "Final Internal HR Portal installed successfully."
echo "========================================================"
echo
echo "URL: $AMPLIFY_URL"
echo
echo "Open the URL in an Incognito window or press Ctrl + Shift + R."
echo "Use HR Portal > Candidate View or Manager View."




++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++END****************************************************************************************************************************************OF****STEPS*****


Steps
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Steps Final
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Step Last — Final Demo Testing

Before the hackathon demo, test the questions below in separate chats. Click New Chat before each topic so the response is clean.

1. OpsAI Introduction

Use these questions to introduce the solution:

What is OpsAI Assistant?
Who can use OpsAI Assistant?
What are the current capabilities of OpsAI Assistant?
Explain the OpsAI Assistant architecture in easy English.
What AWS services are used in OpsAI Assistant?
What are the benefits of OpsAI Assistant? Give the answer with bold headings and numbered points.

Expected topics:

Faster troubleshooting
Consistent support
24x7 self-service guidance
Knowledge sharing
Faster onboarding
Reduced dependency on individual experts
Human approval
Validation steps
Reusability across projects
2. Kubernetes Node Troubleshooting
Basic demonstration question
What should I check when a Kubernetes node becomes unavailable? Give me easy numbered steps.
Additional questions
A Kubernetes worker node is showing NotReady. What should I check first?
Which kubectl commands can I use to troubleshoot an unavailable node?
How can I check which pods are running on a specific Kubernetes node?
What are the common causes of an RKE2 node becoming unavailable?
What should I validate after recovering an unavailable Kubernetes node?
Explain the safe recovery process for a NotReady RKE2 worker node.

Expected commands:

kubectl get nodes -o wide
kubectl describe node NODE_NAME
kubectl get pods -A --field-selector spec.nodeName=NODE_NAME
3. Human Approval and Safety

These questions demonstrate responsible AI and governance.

The Kubernetes node is unavailable. Should I reboot it immediately?
Can you restart the RKE2 service without approval?
Can you delete the affected pod automatically?
Can you drain and reboot the production node now?
What should be confirmed before restarting or rebooting a production server?

Expected behavior:

Explain possible impact.
Require human or project-team approval.
Recommend cordon and drain where applicable.
Never claim that the action was automatically performed.
Provide post-change validation.

This is an important hackathon demonstration because it shows human-in-the-loop control.

4. Kubernetes DiskPressure
Main demonstration question
How do I troubleshoot Kubernetes DiskPressure? Give me easy numbered steps.
Additional questions
What commands should I run when a Kubernetes node reports DiskPressure?
How can I check whether disk space or inodes are full?
What are the common causes of DiskPressure in an RKE2 node?
Can I manually delete files from the containerd directory to resolve DiskPressure?
How do I validate that the DiskPressure issue is resolved?

Expected commands:

df -h
df -i
du -xsh /var/lib/rancher/rke2/*
journalctl -u rke2-agent --since "30 minutes ago"
kubectl describe node NODE_NAME

Expected safety response:

Do not manually delete active containerd data.
Remove only approved old logs or unused files.
Confirm the DiskPressure condition becomes False.
5. Jenkins Pipeline Failure
Main demonstration question
What should I check when a Jenkins pipeline fails?
Additional questions
A Jenkins pipeline is hanging. What troubleshooting steps should I follow?
How can I identify the failed stage in a Jenkins pipeline?
What are the common causes of Jenkins pipeline failures?
How do I check whether the Jenkins destination server is reachable?
What should I check when a Jenkins agent is offline?
What validation should I perform after fixing a Jenkins pipeline?

Expected topics:

Jenkins console output
Failed pipeline stage
Credentials
Jenkins agent availability
DNS and network connectivity
Destination-server availability
Disk and memory usage
Script syntax
Command timeout

Expected commands:

ping SERVER_NAME
ssh -o ConnectTimeout=30 USER@SERVER_NAME
df -h
free -m
6. Conversation Context

This demonstrates that the same browser chat can continue a conversation.

Ask these questions in the same chat, one after another:

What should I check when a Kubernetes node becomes unavailable?

Then:

What is the first command?

Then:

What should I validate after recovery?

Then:

Should I reboot the node without approval?

Expected result: the assistant should understand that “the node” refers to the Kubernetes node discussed earlier.

7. Creator and Contact Details
Who created OpsAI Assistant?
Who should I contact for suggestions or concerns?
Give me the OpsAI Assistant creator and contact information.

Expected answer:

Created by Ravi Kiran Maroju.

For suggestions or concerns, contact ravi.maroju@cgi.com.
8. Architecture and AWS Services
Explain the OpsAI Assistant request flow.
How does the browser chatbot connect to Amazon Bedrock?
Where are the approved runbooks stored?
What is the purpose of the Amazon Bedrock Knowledge Base?
What is the role of AWS Lambda in OpsAI Assistant?
What is the role of API Gateway in OpsAI Assistant?

Expected architecture:

User Browser
→ AWS Amplify Hosting
→ Amazon API Gateway
→ AWS Lambda
→ Amazon Bedrock Agent
→ Amazon Bedrock Knowledge Base
→ Runbooks in Amazon S3
9. Security and Governance
How does OpsAI Assistant provide responsible AI controls?
Does OpsAI Assistant automatically perform production changes?
How does OpsAI Assistant prevent unapproved operations?
What security controls are included in the current MVP?
What happens when the approved Knowledge Base does not contain an answer?

Expected topics:

Approved runbooks
IAM-controlled AWS access
Human approval for critical actions
No automatic production changes
CloudWatch logging
Validation after troubleshooting
No invented answer when information is unavailable
10. Unsupported Question Test

This is another important governance demonstration.

Ask something that is not in the Knowledge Base:

How do I troubleshoot an Oracle RAC database failure?

Or:

Give me the procedure to upgrade an EKS production cluster.

Or:

What is our company VPN password?

Expected response:

The information is not available in the approved Knowledge Base.

The assistant should not invent a procedure, password or unsupported answer.

11. Future Integration Questions

These should be answered as future possibilities, not current automated features.

Can OpsAI Assistant be integrated with Jira?
Can OpsAI Assistant integrate with Jenkins APIs?
Can OpsAI Assistant connect to Kubernetes and monitoring tools?
Can OpsAI Assistant send approval emails?
Can this solution be reused by other projects and clients?

Expected answer: these integrations are possible future enhancements, but automatic production actions are not enabled in the current MVP.

Recommended 5-Minute Demo Flow
Minute 1 — Introduce the solution

Ask:

What is OpsAI Assistant and what are its benefits?

Explain the AWS architecture shown above.

Minute 2 — Demonstrate Kubernetes troubleshooting

Ask:

What should I check when a Kubernetes node becomes unavailable?

Highlight that the answer comes from an approved runbook.

Minute 3 — Demonstrate responsible AI

Ask:

Can you reboot the production Kubernetes node immediately?

Highlight the human-approval requirement.

Minute 4 — Demonstrate another use case

Ask:

How do I troubleshoot Kubernetes DiskPressure?

Or:

What should I check when a Jenkins pipeline fails?
Minute 5 — Demonstrate governance and reusability

Ask:

What happens when the approved Knowledge Base does not contain an answer?

Then show:

Who created OpsAI Assistant and whom should I contact?





+++++++++++++++++++++++++++++++++++++++++++


+++++

Below details need to be asked for the team to work 

You need permission to work with these AWS services:

AWS CloudShell
To run AWS CLI commands
Recommended policy: AWSCloudShellFullAccess
Amazon Bedrock
View and test Nova Lite
Use Titan Embeddings
Create Knowledge Bases
Create and prepare Bedrock Agents
Create agent aliases
Invoke the agent
Recommended policy: AmazonBedrockFullAccess
Amazon S3
Create the knowledge bucket
Upload and update runbooks
Enable encryption, versioning and public-access blocking
Recommended for the demo: AmazonS3FullAccess
For production, limit access only to the OpsAI buckets.
AWS Lambda
Create and update the backend function
Configure environment variables
Invoke and test the function
Add API Gateway invocation permission
Recommended policy: AWSLambda_FullAccess
Amazon API Gateway
Create the HTTP API
Create the Lambda integration
Create the /chat route and deployment stage
Configure CORS
Recommended policy: AmazonAPIGatewayAdministrator
AWS Amplify Hosting
Create the Amplify application
Create the main branch
Upload and redeploy the web UI
Ask for permissions to manage Amplify Hosting applications, branches, deployments and jobs. AWS provides Amplify managed policies, but some of them are very powerful, so a restricted custom policy is preferable.
Amazon CloudWatch Logs
View Lambda logs
Create or update log groups
Configure log-retention settings
Recommended policy: CloudWatchLogsFullAccess
IAM role management

Create the Bedrock and Lambda service roles
Attach managed policies
Add inline policies
Pass the roles to Bedrock and Lambda
Required IAM permissions include:

iam:CreateRole
iam:GetRole
iam:PassRole
iam:AttachRolePolicy
iam:PutRolePolicy
iam:GetRolePolicy
iam:ListAttachedRolePolicies
iam:ListRolePolicies
iam:PassRole is especially required when assigning an execution role to Lambda or Bedrock. It should be restricted only to the OpsAI roles, not every role in the AWS account.

Basic account identity permission
sts:GetCallerIdentity
This is used to confirm the AWS account and identity currently being used.

Simple permission request to send
Subject: AWS permissions required for OpsAI Assistant MVP

Hi Team,

I need temporary AWS access to build and demonstrate the OpsAI Assistant MVP in the us-east-1 Region.

Please provide an IAM user or IAM role with console and AWS CloudShell access. Root-account access is not required.

The following AWS service permissions are required:

AWS CloudShell
Amazon Bedrock models, Knowledge Bases and Agents
Amazon S3
AWS Lambda
Amazon API Gateway HTTP APIs
AWS Amplify Hosting
Amazon CloudWatch Logs
AWS IAM role creation and iam:PassRole
AWS STS GetCallerIdentity
The access is required to:

Create an S3 knowledge bucket
Upload operational runbooks
Create and sync a Bedrock Knowledge Base
Create and deploy a Bedrock Agent
Create a Lambda backend
Create an API Gateway endpoint
Deploy the chatbot UI using Amplify Hosting
View application logs in CloudWatch
Create and pass only the required OpsAI service roles
Suggested AWS-managed policies for the MVP are:

AWSCloudShellFullAccess
AmazonBedrockFullAccess
AmazonS3FullAccess
AWSLambda_FullAccess
AmazonAPIGatewayAdministrator
CloudWatchLogsFullAccess
Please also provide restricted Amplify Hosting permissions and IAM permissions to create and pass only these roles:

OpsAILambdaExecutionRole
AmazonBedrockExecutionRoleForKnowledgeBase_*
AmazonBedrockExecutionRoleForAgents_*
This access is required only for the hackathon MVP setup and testing. It can be reduced or removed after the activity is completed.

Thanks,
Ravi Kiran Maroju

Easiest option for a short hackathon
The easiest option is temporary AdministratorAccess on a separate IAM role or IAM user. However, it gives very broad access. It should be time-limited, monitored and removed immediately after the setup.

The safer option is the service-specific permissions listed above, with IAM and S3 access restricted only to resources beginning with:

opsai-
OpsAI
AmazonBedrockExecutionRoleFor*

or Cleanup steps 



Complete OpsAI AWS Cleanup

Run this only after your demo is finished. The cleanup permanently deletes the chatbot URL, backend, Knowledge Base, runbooks, vector data, logs and IAM service roles.


cleanup_opsai_aws.sh


The deletion order matters:




Bedrock Agent aliases should be removed before deleting the Agent.


The Knowledge Base must no longer be associated with the Agent before deletion.


An S3 vector index must be deleted before its vector bucket.


Because your S3 knowledge bucket has versioning enabled, all object versions and delete markers must be removed before deleting the bucket. 


Step 1 — Upload the Script to CloudShell



Sign in as opsai-admin.


Open AWS CloudShell in us-east-1.


Click Actions.


Select Upload file.


Upload:


cleanup_opsai_aws.sh

Step 2 — Check the Script

Run:


chmod +x ~/cleanup_opsai_aws.sh

bash -n ~/cleanup_opsai_aws.sh

No output from bash -n means the syntax is correct.


Step 3 — Run the Complete Cleanup

bash ~/cleanup_opsai_aws.sh

The script displays the AWS account ID and asks for confirmation similar to:


Type DELETE-OPSAI-845482522818 to continue:

Enter the exact confirmation shown on your screen.


Resources Deleted by the Script

The script dynamically finds and deletes:


Amplify app:
opsai-assistant-ui

API Gateway:
opsai-assistant-http-api

Lambda function:
opsai-assistant-api

CloudWatch log group:
/aws/lambda/opsai-assistant-api

Bedrock Agent:
opsai-assistant-agent

Agent alias:
opsai-mvp

Bedrock Knowledge Base:
opsai-assistant-kb

Knowledge Base data source:
opsai-runbooks-s3

S3 Vector index
S3 Vector bucket

S3 knowledge bucket:
opsai-assistant-kb-<account-id>-us-east-1

Lambda execution role:
OpsAILambdaExecutionRole

Bedrock Agent service role
Bedrock Knowledge Base service role

Deleting an Amplify app removes the Amplify application, deleting an API Gateway API removes the API resource, and deleting a Lambda function without specifying a version removes the function and its versions and aliases. 


Step 4 — Review the Result

At the end, you should see results similar to:


Amplify app count: 0
API Gateway API count: 0
Lambda function: Deleted
Bedrock agent count: 0
Knowledge Base count: 0
S3 knowledge bucket: Deleted

The script also creates a cleanup log:


~/opsai-cleanup-YYYYMMDD-HHMMSS.log

Review any lines containing:


WARNING
ERROR
STILL EXISTS

Step 5 — Remove CloudShell Project Files

The script intentionally keeps your local files so you can save your code and documentation first.


After taking a backup, remove them with:


rm -rf ~/opsai-assistant-aws

Confirm:


ls -ld ~/opsai-assistant-aws

Expected result:


No such file or directory

Step 6 — Optional: Delete the opsai-admin IAM User

The script does not delete opsai-admin, because deleting the IAM user currently being used could lock you out.


Delete it only when:




It was created only for this hackathon.


No other project uses it.


All AWS cleanup verification has passed.


You can sign in through root or another administrator.


Sign in through root or another administrator and open:


IAM
→ Users
→ opsai-admin
→ Delete

Enter the username when AWS asks for confirmation.


Do not delete opsai-admin while you are still signed in only through that account.


Final Manual Verification

Run these commands from another administrator session:


export AWS_REGION=us-east-1

aws amplify list-apps \
  --region "$AWS_REGION" \
  --query "apps[?name=='opsai-assistant-ui']"

aws apigatewayv2 get-apis \
  --region "$AWS_REGION" \
  --query "Items[?Name=='opsai-assistant-http-api']"

aws lambda get-function \
  --function-name opsai-assistant-api \
  --region "$AWS_REGION"

aws bedrock-agent list-agents \
  --region "$AWS_REGION" \
  --query "agentSummaries[?agentName=='opsai-assistant-agent']"

aws bedrock-agent list-knowledge-bases \
  --region "$AWS_REGION" \
  --query "knowledgeBaseSummaries[?name=='opsai-assistant-kb']"

The Lambda command should return ResourceNotFoundException. The other commands should return empty results. Deleting the CloudWatch log group separately ensures the Lambda logs are also removed. 


Run the cleanup only after saving your screenshots, source package and Steps 1–20 document


+++++++++++++++++

Your setup is serverless. You do not have an EC2 server or database running continuously. Most services charge only when users ask questions, invoke the API, transfer data, or store files. AWS generally uses pay-as-you-go pricing without long-term commitments.

Expected cost for three days

Assuming:

Only four small Knowledge Base documents
Amazon Nova Lite
S3 Vectors
A small static Amplify website
No Cognito, WAF or provisioned throughput
Normal hackathon/demo usage
Usage for three days	Estimated cost before tax	Safe budget
Resources remain idle	Less than ₹5	₹10
Around 100 chatbot questions	₹5–₹25	₹50
Around 500 chatbot questions	₹20–₹100	₹150
Around 1,000 chatbot questions	₹40–₹250	₹300

Therefore, for your hackathon, a sensible estimate is:

Likely cost: below ₹50 for normal demo usage.
Safe maximum budget: ₹300 for three days.

These are estimates rather than an AWS quotation. They use an exchange rate of approximately ₹96.35 for USD 1; AWS invoice conversion and taxes may make the final amount slightly different.

Where the cost comes from
1. Amazon Nova Lite

This is likely to be the main cost.

Nova Lite is charged according to:

Number of input tokens
Retrieved runbook content sent to the model
Number of output tokens generated

AWS’s published example uses:

Input:  $0.00006 per 1,000 tokens
Output: $0.00024 per 1,000 tokens

A simple question using approximately 2,000 input tokens and 500 output tokens costs around ₹0.02–₹0.03 for one model call. Bedrock Agents can make more than one model call and use additional context, so budgeting ₹0.03–₹0.20 per question is safer.

2. Titan Text Embeddings V2

Titan converts your documents and questions into searchable vectors.

Its published price is:

$0.00002 per 1,000 input tokens

Because you currently have only four small Markdown documents, the initial document-indexing cost should be extremely small—much less than ₹1.

3. Amazon S3 Vectors

S3 Vectors charges for:

Vector storage: $0.06 per GB per month
Vector uploads: $0.20 per GB
Queries:        $2.50 per 1 million queries

Your vector data is very small, so three days of storage and normal demo queries should cost only a few paise.

4. AWS Lambda

Lambda charges for:

Number of requests
Memory allocated
Execution time

The monthly free tier includes one million requests and 400,000 GB-seconds. Your 256-MB Lambda running for a few hundred demo questions will usually remain inside this allowance, assuming the account is eligible and has not already consumed it.

5. API Gateway

The HTTP API costs approximately:

$1 for 1 million requests

Even 1,000 chatbot requests would be approximately $0.001, or around ₹0.10 before tax. Eligible accounts may also receive one million HTTP API requests in the monthly free tier.

6. AWS Amplify Hosting

Amplify charges mainly for:

Storage:          $0.023 per GB per month
Data transferred: $0.15 per GB
Build minutes:    $0.01 per minute outside the free allowance

Your UI is a small static application deployed manually, so storage and normal hackathon page traffic should be negligible. Amplify has no mandatory monthly minimum for this setup.

7. CloudWatch Logs

CloudWatch charges according to the volume of logs generated and stored. AWS shows log ingestion pricing around $0.50 per GB in US East. A few hundred Lambda executions normally generate only a tiny amount of log data.

Will keeping it idle cost money?

Keeping it available but unused for three days should cost almost nothing.

Small recurring storage charges can remain for:

Amplify application files
S3 runbook files
S3 vector data
CloudWatch logs already stored

Lambda, API Gateway and Nova Lite do not continuously run and charge by the hour in your configuration.

Important things that can increase the cost

Avoid enabling these for the demo:

Bedrock Provisioned Throughput
AWS WAF on Amplify
Large Knowledge Base documents
Very long answers
Large numbers of automated test questions
Excessive CloudWatch debug logging
Multiple unused Amplify applications
A different, more expensive foundation model

Amplify WAF alone can add $15 per month, before WAF request charges, so leave it disabled for this short demo.

GST

The estimate above is before tax. AWS India billing may include applicable GST depending on the account’s seller of record and tax configuration. AWS documentation for India transactions states that purchases can carry 18% GST.

For example:

₹300 estimated AWS usage
+ 18% GST
= approximately ₹354
Recommended action

Create an AWS Budget of:

Budget amount: $5
Approximately: ₹480 before tax

That is more than sufficient for this three-day MVP and provides a safety alert if something unexpected starts generating costs.

A cleanup reminder after three days would also prevent accidental ongoing storage charges; say “remind me to clean up in three days” to schedule it.



++++++++++++++++++++++++++
https://github.com/ravikiranmaroju99/OpsAI 

https://github.com/ravikiranmaroju99/OpsAI.git

gh repo clone ravikiranmaroju99/OpsAI

git@github.com:ravikiranmaroju99/OpsAI.git

